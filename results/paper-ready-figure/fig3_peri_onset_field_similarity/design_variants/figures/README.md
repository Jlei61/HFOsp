### epilepsiae_1146_peri_onset_field_similarity_paper_ready_journal_clean.png

E1146 的无标题期刊版 peri-onset shared-gradient similarity trajectory。图内不显示 panel 字母、病例数灰色说明或 panel 标题；两个 panel 分别使用 `Time (s)`，y 轴简写为 `Field similarity` 和 `Signed field similarity, r`，曲线、IQR、时间范围和 seizure 集合与 canonical E1146 版本一致。画布采用压扁横向布局，并隐藏上、右边框。

**关注点**：检查双面板没有残留标题、panel 编号及上/右边框，每个 panel 各有一个简洁的 `Time (s)`。
