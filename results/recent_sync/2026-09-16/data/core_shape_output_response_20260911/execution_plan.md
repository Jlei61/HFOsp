# core形状、离核输出与参数—观测响应

用户2026-09-11授权继续实验并加入可能螺旋波观测。目的首先是患者双模式及双杆传播恢复，其次用同网络、同噪声的直接对照建立参数响应，并在新拓扑/噪声重演检验。当前不是已经恢复的工作点。

1. 形状/范围：原端点位置、左核上移3mm两个布局，各13条件×2噪声＝52条60秒。圆核；A核4:1/9:1等E成员数并匹配阈值降低总量的椭圆；4:1正交方向；半径2.5mm（原降幅/匹配原总降阈值量）；A→外E原范围重采样；横向×1.5/2、纵向×1.5、方向±15度；椭圆4:1＋输出横向×2。最后一项是联合干预，相对椭圆未重采样对照不能单独归因为范围效应。
2. 参数响应：上移3mm的圆核与椭圆4:1，各14干预×2噪声＝56条60秒。核内EE=.75/1、离核EE=.75/1.25、同核EI=.75/1.25、IE=1.25、II=.75、左核降阈值倍数=.75/1.25、核心均值=1、两核OU相关性=.5/1、离核实际入度=1.5。完整固定参数见plan.json；没有指定“必须产生TA/TB”的驱动。
3. 确认：8条件×拓扑2611/2612×噪声847201/847202＝32条60秒。原位圆/椭圆、上移圆/椭圆、上移离核重采样对照/横向×2、上移扩大半径/扩大且剂量匹配。对照预先指定，不按结果挑选正响应。

初始拓扑2511；噪声847101/847102。完整预算140正式轨迹，另4条500ms应用canary；首轮应用检查发现核参数字典字段读取错误，在实际仿真前修复，失败产物及计划保留在application_repair_001。修复后4条均通过，圆核静态身份及500ms动态前缀与历史60秒输出一致。物理代码与候选表派发前哈希冻结；从不可变全局图缓存派生每个候选。

**参数含义**：匹配阈值总量并非匹配全部物理因素。椭圆改变核心成员的空间身份和局部I邻域；扩大半径还增加随机输入支持的神经元数。局部离核重采样保留每个A源的出度与总权重，但改变目标输入和距离时延；其余边块保留。权重、入度、空间分配为独立操作。两核E阈值均≤对应背景18mV，11mV下限记录；不引入升阈值尾部。

**共同物理**：空间OU、慢I、Z/M和定向刺激关闭；GABA18ms；核外E和全部I为确定的期望外部到达量，核E具有OU＋Poisson随机输入。不禁止核外递归发放。每个拓扑单元内位置、潜在阈值、基线图固定；同seed不是参数改变后严格逐时刻共同随机创新。

**观测与判断**：冻结主事件资格、1.5秒burn-in和250ms窗；全检测另作诊断。FIT患者自然分布、原L_off＋参与联合分布损失用于排序，N<16不正式排名；原D16、A均值项、B有限事件减项保留。TA/TB只组织生成事件后的条件比较。参与、rank、成对顺序/时差及条件支持、局部t10–90宽度、跨触点t10跨度、两核质量时间差、原生场面积/空间轴/并行连通域、核心早期活动份额、6秒段模式量、旋转候选分别报告。各接触、各模式保留均值/中位数、方差和5–95%范围；没有总体恢复率。

**旋转观测**：两张GPU分配完整时间×空间场的Hilbert相位与绕转，CPU原算法一致性检查通过。主3–50Hz，另3–30Hz及不空间平滑；连续相位轨迹≥40ms，原生活动固定2/3/4mm环，半周、振幅和方向一致性筛查。保留转角、持续时间、位置及占用时间。轨迹可能分段，不能当作独立螺旋事件。合成双波源已证明假阳性可能：见历史spiral_wave_detection/synthetic_ring_check.json。旋转不进入拟合loss，完整field GIF复核后才解释。短于20秒的提前停止仅不进入本轮完整旋转统计，物理runaway单独保留。

**图件**：实际Fig2C患者STFT与模型发放密度包络按同毫秒轴和固定15行分杆显示；不把模型包络称为HFO频谱。各类自身均值附近示例、多事件及固定连续时段原生field GIF同时展示；对照按同拓扑、同噪声，不能把事件当成网络复本。参数×观测图保留真实量纲和缺失值，新拓扑逐图保留。

**执行**：最多20个CPU worker，按进程树RSS与可用内存派发；保留50GiB，单树18GiB上限，严重余量不足停止新派发并记录。CPU跑原引擎，两GPU流式接收新轨迹做旋转筛查；不人为制造空算维持利用率。物理runaway和工程失败分别记录，工程失败停止新派发并收尾。各批自动衔接，输出自动分析、图、表。140条全部完成后停在科学审阅点，不自动扩大预算、冻结或进入Fig5。
