### refine_midpoint_EE075@2511_patient_spectra_model_envelopes.png

中点位置四个参数条件的完整两噪声结果；模型示例取原primary内最接近自身模式均值的事件。患者为Fig2C的实际STFT，模型为发放密度，时间仅平移。**关注点**：上杆参与与时序是否同时改善，罕见模式是否代表完整恢复。

### refine_midpoint_EE075_A115@2511_patient_spectra_model_envelopes.png

中点位置四个参数条件的完整两噪声结果；模型示例取原primary内最接近自身模式均值的事件。患者为Fig2C的实际STFT，模型为发放密度，时间仅平移。**关注点**：上杆参与与时序是否同时改善，罕见模式是否代表完整恢复。

### refine_midpoint_EE085_A115_mean095@2511_847101_same_network.png

中点位置四个参数条件的完整两噪声结果；模型示例取原primary内最接近自身模式均值的事件。患者为Fig2C的实际STFT，模型为发放密度，时间仅平移。**关注点**：上杆参与与时序是否同时改善，罕见模式是否代表完整恢复。

### refine_midpoint_EE085_A115_mean095@2511_847102_same_network.png

中点位置四个参数条件的完整两噪声结果；模型示例取原primary内最接近自身模式均值的事件。患者为Fig2C的实际STFT，模型为发放密度，时间仅平移。**关注点**：上杆参与与时序是否同时改善，罕见模式是否代表完整恢复。

### refine_midpoint_EE085_A115_mean095@2511_patient_spectra_model_envelopes.png

中点位置四个参数条件的完整两噪声结果；模型示例取原primary内最接近自身模式均值的事件。患者为Fig2C的实际STFT，模型为发放密度，时间仅平移。**关注点**：上杆参与与时序是否同时改善，罕见模式是否代表完整恢复。

### refine_midpoint_EE085_mean095@2511_patient_spectra_model_envelopes.png

中点位置四个参数条件的完整两噪声结果；模型示例取原primary内最接近自身模式均值的事件。患者为Fig2C的实际STFT，模型为发放密度，时间仅平移。**关注点**：上杆参与与时序是否同时改善，罕见模式是否代表完整恢复。
