### native_extremes.gif

显示同一批事后择优事件的全部原生场；静态图为标注时间区间内全部2ms帧平均，GIF为原生2ms逐帧播放，均保留核外活动。选例是事后能力诊断，既不是该类常见事件，也不是独立验证；各事件的实际观测支持量与选择距离见selection。**关注点**：是否曾出现较相容传播，以及好看的个例与常见事件分布之间仍有多大差距。

### native_full_window_bins.pdf

显示同一批事后择优事件的全部原生场；静态图为标注时间区间内全部2ms帧平均，GIF为原生2ms逐帧播放，均保留核外活动。选例是事后能力诊断，既不是该类常见事件，也不是独立验证；各事件的实际观测支持量与选择距离见selection。**关注点**：是否曾出现较相容传播，以及好看的个例与常见事件分布之间仍有多大差距。

### native_full_window_bins.png

显示同一批事后择优事件的全部原生场；静态图为标注时间区间内全部2ms帧平均，GIF为原生2ms逐帧播放，均保留核外活动。选例是事后能力诊断，既不是该类常见事件，也不是独立验证；各事件的实际观测支持量与选择距离见selection。**关注点**：是否曾出现较相容传播，以及好看的个例与常见事件分布之间仍有多大差距。

### patient_compatible_extremes.pdf

左列真实Fig2C STFT；右列为同一条件两条噪声各自到患者同类邻域距离最小的原合格事件，模型为发放密度包络，未拉伸时间。选例是事后能力诊断，既不是该类常见事件，也不是独立验证；各事件的实际观测支持量与选择距离见selection。**关注点**：是否曾出现较相容传播，以及好看的个例与常见事件分布之间仍有多大差距。

### patient_compatible_extremes.png

左列真实Fig2C STFT；右列为同一条件两条噪声各自到患者同类邻域距离最小的原合格事件，模型为发放密度包络，未拉伸时间。选例是事后能力诊断，既不是该类常见事件，也不是独立验证；各事件的实际观测支持量与选择距离见selection。**关注点**：是否曾出现较相容传播，以及好看的个例与常见事件分布之间仍有多大差距。
