### ALL_primary_paired_changes.png

同一拓扑、同一噪声种子下，将明确的一项参数改变前后作差；颜色区分EE、阈值、输入均值和位置。圆方等形状区分实际运行，事件数列出前后值，未观察到模式时对应分布指标留空；没有把事件当作独立网络，也没有显著性检验。位置及输入均值改变时，实际随机输入不保证相同；primary与全部检测开发集合分开。**关注点**：一个参数能否在两条重演中产生一致响应，以及招募、顺序误差与事件量是否存在取舍。

### ALL_primary_paired_changes.pdf

同一拓扑、同一噪声种子下，将明确的一项参数改变前后作差；颜色区分EE、阈值、输入均值和位置。圆方等形状区分实际运行，事件数列出前后值，未观察到模式时对应分布指标留空；没有把事件当作独立网络，也没有显著性检验。位置及输入均值改变时，实际随机输入不保证相同；primary与全部检测开发集合分开。**关注点**：一个参数能否在两条重演中产生一致响应，以及招募、顺序误差与事件量是否存在取舍。

### TA_primary_paired_changes.png

同一拓扑、同一噪声种子下，将明确的一项参数改变前后作差；颜色区分EE、阈值、输入均值和位置。圆方等形状区分实际运行，事件数列出前后值，未观察到模式时对应分布指标留空；没有把事件当作独立网络，也没有显著性检验。位置及输入均值改变时，实际随机输入不保证相同；primary与全部检测开发集合分开。**关注点**：一个参数能否在两条重演中产生一致响应，以及招募、顺序误差与事件量是否存在取舍。

### TA_primary_paired_changes.pdf

同一拓扑、同一噪声种子下，将明确的一项参数改变前后作差；颜色区分EE、阈值、输入均值和位置。圆方等形状区分实际运行，事件数列出前后值，未观察到模式时对应分布指标留空；没有把事件当作独立网络，也没有显著性检验。位置及输入均值改变时，实际随机输入不保证相同；primary与全部检测开发集合分开。**关注点**：一个参数能否在两条重演中产生一致响应，以及招募、顺序误差与事件量是否存在取舍。

### TB_primary_paired_changes.png

同一拓扑、同一噪声种子下，将明确的一项参数改变前后作差；颜色区分EE、阈值、输入均值和位置。圆方等形状区分实际运行，事件数列出前后值，未观察到模式时对应分布指标留空；没有把事件当作独立网络，也没有显著性检验。位置及输入均值改变时，实际随机输入不保证相同；primary与全部检测开发集合分开。**关注点**：一个参数能否在两条重演中产生一致响应，以及招募、顺序误差与事件量是否存在取舍。

### TB_primary_paired_changes.pdf

同一拓扑、同一噪声种子下，将明确的一项参数改变前后作差；颜色区分EE、阈值、输入均值和位置。圆方等形状区分实际运行，事件数列出前后值，未观察到模式时对应分布指标留空；没有把事件当作独立网络，也没有显著性检验。位置及输入均值改变时，实际随机输入不保证相同；primary与全部检测开发集合分开。**关注点**：一个参数能否在两条重演中产生一致响应，以及招募、顺序误差与事件量是否存在取舍。

### ALL_all_detected_paired_changes.png

同一拓扑、同一噪声种子下，将明确的一项参数改变前后作差；颜色区分EE、阈值、输入均值和位置。圆方等形状区分实际运行，事件数列出前后值，未观察到模式时对应分布指标留空；没有把事件当作独立网络，也没有显著性检验。位置及输入均值改变时，实际随机输入不保证相同；primary与全部检测开发集合分开。**关注点**：一个参数能否在两条重演中产生一致响应，以及招募、顺序误差与事件量是否存在取舍。

### ALL_all_detected_paired_changes.pdf

同一拓扑、同一噪声种子下，将明确的一项参数改变前后作差；颜色区分EE、阈值、输入均值和位置。圆方等形状区分实际运行，事件数列出前后值，未观察到模式时对应分布指标留空；没有把事件当作独立网络，也没有显著性检验。位置及输入均值改变时，实际随机输入不保证相同；primary与全部检测开发集合分开。**关注点**：一个参数能否在两条重演中产生一致响应，以及招募、顺序误差与事件量是否存在取舍。

### TA_all_detected_paired_changes.png

同一拓扑、同一噪声种子下，将明确的一项参数改变前后作差；颜色区分EE、阈值、输入均值和位置。圆方等形状区分实际运行，事件数列出前后值，未观察到模式时对应分布指标留空；没有把事件当作独立网络，也没有显著性检验。位置及输入均值改变时，实际随机输入不保证相同；primary与全部检测开发集合分开。**关注点**：一个参数能否在两条重演中产生一致响应，以及招募、顺序误差与事件量是否存在取舍。

### TA_all_detected_paired_changes.pdf

同一拓扑、同一噪声种子下，将明确的一项参数改变前后作差；颜色区分EE、阈值、输入均值和位置。圆方等形状区分实际运行，事件数列出前后值，未观察到模式时对应分布指标留空；没有把事件当作独立网络，也没有显著性检验。位置及输入均值改变时，实际随机输入不保证相同；primary与全部检测开发集合分开。**关注点**：一个参数能否在两条重演中产生一致响应，以及招募、顺序误差与事件量是否存在取舍。

### TB_all_detected_paired_changes.png

同一拓扑、同一噪声种子下，将明确的一项参数改变前后作差；颜色区分EE、阈值、输入均值和位置。圆方等形状区分实际运行，事件数列出前后值，未观察到模式时对应分布指标留空；没有把事件当作独立网络，也没有显著性检验。位置及输入均值改变时，实际随机输入不保证相同；primary与全部检测开发集合分开。**关注点**：一个参数能否在两条重演中产生一致响应，以及招募、顺序误差与事件量是否存在取舍。

### TB_all_detected_paired_changes.pdf

同一拓扑、同一噪声种子下，将明确的一项参数改变前后作差；颜色区分EE、阈值、输入均值和位置。圆方等形状区分实际运行，事件数列出前后值，未观察到模式时对应分布指标留空；没有把事件当作独立网络，也没有显著性检验。位置及输入均值改变时，实际随机输入不保证相同；primary与全部检测开发集合分开。**关注点**：一个参数能否在两条重演中产生一致响应，以及招募、顺序误差与事件量是否存在取舍。

### native_group_rate_paired_changes.png

每个参数前后，在同一图和噪声种子下计算左核E、右核E、核外E和全体I的平均放电率差；每个细胞等权，使用完整启动排除后记录的原生发放，无电极读出或事件筛选。颜色代表参数类别，符号代表实际重演；正负只表示活动增加或减少，不是患者拟合优劣。**关注点**：局部参数是否引起非局部活动重排，以及这种响应是否跨图/噪声保留。

### native_group_rate_paired_changes.pdf

每个参数前后，在同一图和噪声种子下计算左核E、右核E、核外E和全体I的平均放电率差；每个细胞等权，使用完整启动排除后记录的原生发放，无电极读出或事件筛选。颜色代表参数类别，符号代表实际重演；正负只表示活动增加或减少，不是患者拟合优劣。**关注点**：局部参数是否引起非局部活动重排，以及这种响应是否跨图/噪声保留。
