# 多网络参数响应曲线

### circle_EI_ALL_participation_timing.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### circle_EI_ALL_native_time_space.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### circle_EI_ALL_support_rotation.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### circle_EI_TA_participation_timing.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### circle_EI_TA_native_time_space.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### circle_EI_TA_support_rotation.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### circle_EI_TB_participation_timing.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### circle_EI_TB_native_time_space.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### circle_EI_TB_support_rotation.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### ellipse4_EI_ALL_participation_timing.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### ellipse4_EI_ALL_native_time_space.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### ellipse4_EI_ALL_support_rotation.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### ellipse4_EI_TA_participation_timing.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### ellipse4_EI_TA_native_time_space.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### ellipse4_EI_TA_support_rotation.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### ellipse4_EI_TB_participation_timing.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### ellipse4_EI_TB_native_time_space.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### ellipse4_EI_TB_support_rotation.png
颜色为网络，实线圆点/虚线三角为两次噪声，每条线固定网络与噪声。各观测保留实际量纲，患者参考只用于可比的接触统计。
**关注点**：参数曲线形状是否跨网络重演；参与改善是否伴随时差、模式支持或原生场的代价。

### bridge_pair0_ALL_participation_timing.png
左核位置：原位置与左移0.75mm；每条线为固定基础拓扑种子和噪声下的两个条件。固定向外EE×1.25、EI×1；平移改变core成员及对应连接分块。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair0_ALL_native_time_space.png
左核位置：原位置与左移0.75mm；每条线为固定基础拓扑种子和噪声下的两个条件。固定向外EE×1.25、EI×1；平移改变core成员及对应连接分块。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair0_ALL_support_rotation.png
左核位置：原位置与左移0.75mm；每条线为固定基础拓扑种子和噪声下的两个条件。固定向外EE×1.25、EI×1；平移改变core成员及对应连接分块。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair0_TA_participation_timing.png
左核位置：原位置与左移0.75mm；每条线为固定基础拓扑种子和噪声下的两个条件。固定向外EE×1.25、EI×1；平移改变core成员及对应连接分块。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair0_TA_native_time_space.png
左核位置：原位置与左移0.75mm；每条线为固定基础拓扑种子和噪声下的两个条件。固定向外EE×1.25、EI×1；平移改变core成员及对应连接分块。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair0_TA_support_rotation.png
左核位置：原位置与左移0.75mm；每条线为固定基础拓扑种子和噪声下的两个条件。固定向外EE×1.25、EI×1；平移改变core成员及对应连接分块。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair0_TB_participation_timing.png
左核位置：原位置与左移0.75mm；每条线为固定基础拓扑种子和噪声下的两个条件。固定向外EE×1.25、EI×1；平移改变core成员及对应连接分块。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair0_TB_native_time_space.png
左核位置：原位置与左移0.75mm；每条线为固定基础拓扑种子和噪声下的两个条件。固定向外EE×1.25、EI×1；平移改变core成员及对应连接分块。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair0_TB_support_rotation.png
左核位置：原位置与左移0.75mm；每条线为固定基础拓扑种子和噪声下的两个条件。固定向外EE×1.25、EI×1；平移改变core成员及对应连接分块。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair1_ALL_participation_timing.png
离核输出：增权50%与增边50%；每条线为固定基础拓扑种子和噪声下的两个条件。两者仅名义总剂量接近；增边改变实际邻接和时延，实际权重总量见应用记录。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair1_ALL_native_time_space.png
离核输出：增权50%与增边50%；每条线为固定基础拓扑种子和噪声下的两个条件。两者仅名义总剂量接近；增边改变实际邻接和时延，实际权重总量见应用记录。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair1_ALL_support_rotation.png
离核输出：增权50%与增边50%；每条线为固定基础拓扑种子和噪声下的两个条件。两者仅名义总剂量接近；增边改变实际邻接和时延，实际权重总量见应用记录。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair1_TA_participation_timing.png
离核输出：增权50%与增边50%；每条线为固定基础拓扑种子和噪声下的两个条件。两者仅名义总剂量接近；增边改变实际邻接和时延，实际权重总量见应用记录。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair1_TA_native_time_space.png
离核输出：增权50%与增边50%；每条线为固定基础拓扑种子和噪声下的两个条件。两者仅名义总剂量接近；增边改变实际邻接和时延，实际权重总量见应用记录。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair1_TA_support_rotation.png
离核输出：增权50%与增边50%；每条线为固定基础拓扑种子和噪声下的两个条件。两者仅名义总剂量接近；增边改变实际邻接和时延，实际权重总量见应用记录。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair1_TB_participation_timing.png
离核输出：增权50%与增边50%；每条线为固定基础拓扑种子和噪声下的两个条件。两者仅名义总剂量接近；增边改变实际邻接和时延，实际权重总量见应用记录。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair1_TB_native_time_space.png
离核输出：增权50%与增边50%；每条线为固定基础拓扑种子和噪声下的两个条件。两者仅名义总剂量接近；增边改变实际邻接和时延，实际权重总量见应用记录。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。

### bridge_pair1_TB_support_rotation.png
离核输出：增权50%与增边50%；每条线为固定基础拓扑种子和噪声下的两个条件。两者仅名义总剂量接近；增边改变实际邻接和时延，实际权重总量见应用记录。
**关注点**：相同参数改变是否跨网络重演，参与和时序有无代价；缺失点不代表零响应。
