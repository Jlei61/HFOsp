# 固定网络患者与模型比较

### bridge_circle_out125_847401_continuous_field_readout.gif
患者为Fig2C原始STFT，模型为发放密度包络，15行分杆且固定，真实毫秒轴。示例靠近模型自身模式均值；GIF按时间取各类前三例和固定连续片段，均含全部E活动。
**关注点**：两类传播是否都恢复，而非只获得两个标签；不按患者相似程度挑示例。

### bridge_circle_out125_847401_multievent_field_readout.gif
患者为Fig2C原始STFT，模型为发放密度包络，15行分杆且固定，真实毫秒轴。示例靠近模型自身模式均值；GIF按时间取各类前三例和固定连续片段，均含全部E活动。
**关注点**：两类传播是否都恢复，而非只获得两个标签；不按患者相似程度挑示例。

### bridge_circle_out125_847401_multievent_field_readout_preview.png
患者为Fig2C原始STFT，模型为发放密度包络，15行分杆且固定，真实毫秒轴。示例靠近模型自身模式均值；GIF按时间取各类前三例和固定连续片段，均含全部E活动。
**关注点**：两类传播是否都恢复，而非只获得两个标签；不按患者相似程度挑示例。

### bridge_circle_out125_847401_same_network.png
患者为Fig2C原始STFT，模型为发放密度包络，15行分杆且固定，真实毫秒轴。示例靠近模型自身模式均值；GIF按时间取各类前三例和固定连续片段，均含全部E活动。
**关注点**：两类传播是否都恢复，而非只获得两个标签；不按患者相似程度挑示例。

### bridge_circle_out125_847402_same_network.png
患者为Fig2C原始STFT，模型为发放密度包络，15行分杆且固定，真实毫秒轴。示例靠近模型自身模式均值；GIF按时间取各类前三例和固定连续片段，均含全部E活动。
**关注点**：两类传播是否都恢复，而非只获得两个标签；不按患者相似程度挑示例。

### bridge_circle_out125_patient_spectra_model_envelopes.png
患者为Fig2C原始STFT，模型为发放密度包络，15行分杆且固定，真实毫秒轴。示例靠近模型自身模式均值；GIF按时间取各类前三例和固定连续片段，均含全部E活动。
**关注点**：两类传播是否都恢复，而非只获得两个标签；不按患者相似程度挑示例。

