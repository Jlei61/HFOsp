# 2026-09-13：参数响应曲线与多网络重演

**07:26调度修订**：旧续跑降至3条物理轨迹后，本批早期并发上限8→10；同一共享进程树增长预留与硬保护不变，实际派发仍由余量决定。已接管原8条物理轨迹，未重启；按冻结顺序补上网络2711、噪声847402的原位置／左移对照。见`early_ten_resource_amendment.json`及`early_ten_handoff_verification.json`，其他参数、时长、顺序及108预算不变。

**06:20调度修订**：旧批运行期间上限调整至8，其余阶段16/18不变；依据逐进程积分阶段与实际RSS采用建图9GiB、积分6GiB的增长估计，硬保护和主机余量保持。原6个物理worker未重启，新增两条恰为冻结顺序中847402噪声的原位置／左移对照；见`early_eight_resource_amendment.json`与`early_eight_handoff_verification.json`。本条及已记录的阶段内存修订替代下方旧并发数字，不改变108个参数条件身份或预算。

**02:28调度修订**：60秒仿真耗时数小时，本夜优先在2511/2711/2712三张网络分别完成同一个位置对照，然后进入另一噪声和参数族。全部108个身份不变，只有尚未派发的顺序改变；旧顺序保留在`formal_units_initial_network_major.json`。旧批运行期间本批上限由2增至6，但同一内存预留和停止规则不放宽，实际并发由资源决定。控制器已接管原2个存活worker，PID与轨迹连续，0个物理worker重启。见`network_order_amendment.json`；本条替代下方01:42的并发上限和顺序。

**01:42调度修订（用户已授权自主8小时）**：三个应用检查通过后，内存余量允许时可提前并行最多2条正式新运行，不再严格等待上一批全部结束。派发同时预留上一批所有真实存活worker和本批worker各自增长至9GiB的空间，再留40GiB主机余量；原批次的运行和调度器不动。本批先按同网络同噪声配对派发原位置与左移0.75mm，再继续EI与增边对照，全部108个执行身份及顺序在首次正式派发前固定。上一批物理运行结束后，本批并发上限恢复18。只调整时间顺序和资源调度，不改变参数、损失、种子或108条预算；实际记录见`scheduling_amendment_8h.json`和`dispatch.jsonl`。下文原“等待”描述保留版本来源，以本条为准。

用户已授权继续表现较好的方向、补充多参数点及seed响应。当前批次独立于上一轮：先完成新网络构建与3条500ms应用检查；正式仿真等待 `core_recruitment_tradeoff_followup_20260912/simulation_complete.json`，随后自动派发。已有批次、候选选择和损失不更改。

## 科学问题与比较

当前已观察到TA时序、SCL参与对核内E→I和向外E→E有不同响应，且组合可能从漏招募跨到过度招募。下一步要判断：这些是单点偶然、可重复的参数过渡，还是只在某张网络上成立；不能先要求每个条件都恢复双模式。

|响应族|本次变化点|固定背景|
|---|---|---|
|圆核：核内E→I|0.75、0.8125、0.875、0.9375、1|向外EE=1|
|左核椭圆4:1：核内E→I|同上5点|向外EE=1，E成员数与降阈值总量匹配|
|圆核：向外E→E原边权重|1、1.0625、1.125、1.1875、1.25|核内EI=0.875|
|位置响应|原位与左核x−0.75mm|圆核，向外EE=1.25、EI=1|
|增边／增权对照|向外边数×1.5 vs 原边权重×1.5|圆核，EI=1，其余相同|

前三族共享一个条件，合计14个唯一条件；后两组4个条件，总计18。上述EE/EI倍率为相应连接分块的实际配置倍率，核内EE始终0.85；不是改变全图所有EI连接。增边与增权仅为名义总兴奋量接近的比较，实际取整边数、权重剂量逐网络记录，不能称为精确剂量匹配或纯连接数量干预。

18条件×网络2511/2711/2712×噪声847401/847402 = **108条新60秒正式运行**。2511是开发网络，2711/2712是本系列新图；两个噪声编号完全交叉。3条500ms应用检查独立于正式样本，无事件数或传播方向验收条件。没有新的自适应搜索或额外polish；所有条件和派发顺序提前固定。

## 物理与随机性

复用已冻结的物理执行器，不修改旧脚本或缓存。两个E-core仅降阈值/保持背景；核内E接受随机OU/Poisson，核外E与所有I接受确定期望到达和递归突触输入。Z/M、空间OU、慢I状态、定向刺激关闭，GABA18ms固定。

基底为左核上移3mm的端点几何先验：左核(4.19921432,12.12890135)mm，右核(16.47920304,3.96551153)mm，圆核半径1.75257829mm。粗端点及方向先验已经参与设计，不能再用它们作独立验证；不把指定TA/TB细路线输入loss或刺激。椭圆方向继承既定端点几何轴。

同一候选、同一拓扑的两噪声重演要求实际静态位置、成员、阈值、边、权重与时延身份一致；逐运行应用日志与分析检查负责验证。动态噪声改变，轨迹不要求相同。跨候选共用seed不宣称严格逐时刻共同随机创新。旧结果不混入108条新样本。

## 观测、作图与结论边界

每条60秒运行剔除前1.5秒；主合格事件及全检测诊断分别保存。保持原 `L_search`、去自配对统计量、N≥16的排名约定，不加入指定路线、宽度或旋转损失。

运行是参数效应的实验单位；事件是运行内样本。ALL、TA、TB分别保留：事件数/比例，逐接触参与、rank均值与散布，成对先后概率/时差，跨杆质心时差分布，局部t10–t90宽度与跨接触t10跨度，核间先后、早期核内份额、原生活动面积/离散度/连通域，以及旋转候选占用时间与圈数。旋转筛查存在非旋转多波源叠加假阳性。

主曲线每条线固定网络与噪声，颜色为网络，实线圆点/虚线三角为两条噪声。不先对事件混池求置信带。逐family直接对照差输出实际量纲、各网络方向、两噪声范围和事件支持量；3张网络×2次噪声只够初步可重复性，不足以声称人群层显著机制。

患者Fig2C真实STFT与模型发放密度包络使用同一真实毫秒轴，固定15行、按SCL和ICL分杆、缺失行保留、杆间不连线；不能宣称信号等价。预先指定6个直接对照候选在每张网络上输出同网四联图和多事件GIF，示例取模型自身均值附近，GIF取各模式时间上最早三例和固定连续片段，原生场包含全部E活动。未展示条件的全部事件数组仍保存。

响应的可重复性、患者分布恢复、传播机制分别验收。若TA参与改善而TB时差/路径仍偏离，就保留对应参数响应，不接受完整双模式恢复；若跨网络消失，报告网络依赖；若仅事件数不足，报告可观测支持，不自动称为机制失败。

## 资源、复现和收尾

最多18个正式worker，由主机可用内存、进程树实际RSS及增长预留决定实时并发；不把RLIMIT_AS当物理内存预算。不与上一批正式仿真争抢worker；图缓存准备及单个短程检查可并行。静态图新增2711/2712，各子进程监测18GiB RSS和主机余量。GPU0/1继续消费新轨迹做旋转诊断，图表自动更新；没有GPU任务时不空烧无关任务。

完成108条正式运行、逐运行分析、旋转诊断和预定可估计媒体后，状态写 `ROUND_COMPLETE_PENDING_SCIENTIFIC_REVIEW`，停在审阅点，不自动追加、冻结模型或进入Fig5。工程失败停止新派发并收尾，runaway作为物理状态记录，不冒充工程失败。重启复用已完成有效输出，不重新抽条件；如controller意外退出，先核查仍活跃的子进程再恢复，避免重复派发。

入口：`plan.json` / `formal_units.json`、`status.json`、`logs/controller.log`；图与逐运行结果在 `analysis/`，PDF为 `analysis/parameter_response_report.pdf`。当前进度和人工验收以实际文件为准。


03:33共享资源修订：额外计入本夜全局轴探针的真实进程树及增长余量；为该有界探针临时保留两个派发名额，因此旧F未结束时N上限暂为4，F结束后暂为16。探针完成、失败或09:32截止后恢复6/18上限；实际并发始终受可用内存约束。N现有4个物理worker原PID接管，未重启，108身份与顺序/参数/时长不变。记录见optional_probe_resource_amendment.json。
