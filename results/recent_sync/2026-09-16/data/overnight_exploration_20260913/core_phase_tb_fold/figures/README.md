# TB内部的core活动时序与接触路径

### core_t10_conditional_observations.png
固定工作点与噪声逐运行展示，分层依据已有事件窗内core累计发放的10%或50%时间，不是因果起源。患者参考为完整TB事件的接触统计。
**关注点**：TB标签内部仍可能混合不同原生过程；局部触点先后改善与完整传播恢复应分开判断。

### core_t50_conditional_observations.png
固定工作点与噪声逐运行展示，分层依据已有事件窗内core累计发放的10%或50%时间，不是因果起源。患者参考为完整TB事件的接触统计。
**关注点**：TB标签内部仍可能混合不同原生过程；局部触点先后改善与完整传播恢复应分开判断。

### tb_core_timing_pair_lag.png
固定工作点与噪声逐运行展示，分层依据已有事件窗内core累计发放的10%或50%时间，不是因果起源。患者参考为完整TB事件的接触统计。
**关注点**：TB标签内部仍可能混合不同原生过程；局部触点先后改善与完整传播恢复应分开判断。


### selected_tb_core_traces.png
同一网络和噪声中两个TB事件，分别代表两个核累计发放先后亚组；示例按亚组自身特征均值选取。显示原始1ms两核放电率、窗口内累计质量及固定15行接触包络。
**关注点**：波形与窗口是否支持分层的含义；不把核累计时间解释为因果起源。
