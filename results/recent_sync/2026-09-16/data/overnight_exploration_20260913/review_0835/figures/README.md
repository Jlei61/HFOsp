### geometry_parameter_response.png
固定基础网络和噪声，中心/范围探针减各自直接对照；横线只是连接两次噪声结果。各列独立量纲，未完成点不填0。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### all_parameters_TA.png
原140条中的14种局部参数干预，按固定形状和噪声配对。颜色为形状组合，点形为噪声，不把事件数视为网络重复。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### all_parameters_TB.png
原140条中的14种局部参数干预，按固定形状和噪声配对。颜色为形状组合，点形为噪声，不把事件数视为网络重复。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### radius_participation_and_activity.png
范围参数的三个离散点在两工作点内分别配对，显示TA出现、参与、两类时序与左核群体发放量。匹配降阈值总量并未匹配随机输入支持或连接分块；缺失点不填0。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### prior_confirmation_paired_geometry_effects.png
原32条几何确认按两张网络、两次噪声拆开比较；某些TA条件摘要的几何效应跨网络反向。此处固定EI=1、向外EE=1，不能替代新局部参数候选的复测。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### prior_confirmation_absolute_timing_by_network.png
原32条几何确认按两张网络、两次噪声拆开比较；某些TA条件摘要的几何效应跨网络反向。此处固定EI=1、向外EE=1，不能替代新局部参数候选的复测。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### prior_confirmation_core_contact_timing.png
原8条圆核／椭圆确认的TA事件，核心累计先后与电极杆间时差并排比较。160个TA均为左核窗口t10较早，154个可测杆间差；领先core未交换，电极时序仍改变。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### circle_patient_comparison.png
患者固定Fig2C真实STFT与模型自身模式均值附近的事件比较。SCL/ICL按固定15行展示，未参与保留灰色，不拉伸时间。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### ellipse_patient_comparison.png
患者固定Fig2C真实STFT与模型自身模式均值附近的事件比较。SCL/ICL按固定15行展示，未参与保留灰色，不拉伸时间。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### ellipse_yplus1_two_noise_patient_comparison.png
椭圆左核再上移1mm条件的两条完整噪声，与患者固定Fig2C真实STFT同列比较。例子按模型自身均值选取；两噪声是同一网络重演，不是两张独立网络。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### new_network_TA.png
已完整分析的新噪声和拓扑中的左移效应，各线固定基础网络与噪声。未完成条件保持缺失，不能用部分结果提前宣称全网一致。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### new_network_TB.png
已完整分析的新噪声和拓扑中的左移效应，各线固定基础网络与噪声。未完成条件保持缺失，不能用部分结果提前宣称全网一致。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### position_development_and_new_noise.png
圆核向外EE×1.25下，左核左移0.75mm的作用，在开发噪声和已完整的新噪声中逐条配对。固定基础网络2511；灰线是开发参照，彩线是新噪声，同位置静态数组已核对一致。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### first_new_noise_topology2511_patient_comparison.png
已完成的首条新噪声中，左移工作点与患者Fig2C的两类过程对照。第二条噪声尚未纳入本页并明确留空；单次重演不代表完整双噪声确认。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### global_axis_contact_timing_order.png
仅展示已完成20秒方向条件，保持原三点横轴与缺失状态。角度改变实际EE图和距离时延；开发噪声下的响应不等于新拓扑确认。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### global_axis_sampling_support.png
仅展示已完成20秒方向条件，保持原三点横轴与缺失状态。角度改变实际EE图和距离时延；开发噪声下的响应不等于新拓扑确认。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### global_axis_minus15_patient_comparison.png
患者固定Fig2C真实STFT与模型自身模式均值附近的事件比较。SCL/ICL按固定15行展示，未参与保留灰色，不拉伸时间。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### global_axis_plus15_patient_comparison.png
患者固定Fig2C真实STFT与模型自身模式均值附近的事件比较。SCL/ICL按固定15行展示，未参与保留灰色，不拉伸时间。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### core_contact_sampling.png
冻结输出的核心时序、空间采样或全场活动诊断。核心后验时序不等于因果起源，几何权重不等于事件信号贡献。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### TB_core_timing.png
冻结输出的核心时序、空间采样或全场活动诊断。核心后验时序不等于因果起源，几何权重不等于事件信号贡献。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。

### TB_native_example.png
冻结输出的核心时序、空间采样或全场活动诊断。核心后验时序不等于因果起源，几何权重不等于事件信号贡献。
**关注点**：参数是否同时改善参与和实际传播，及改善是否跨运行保留；不能仅凭较低loss或两个标签接受双模式恢复。
