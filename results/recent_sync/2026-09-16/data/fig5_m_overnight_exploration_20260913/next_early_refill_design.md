# 早期Z恢复支路：当前实际执行方案

两条来源固定为ηM=0.005、τM=1秒的e0_t0_s9108401/2，原40条任务仍不变。2026-09-13约03:04启动事件对齐版本监督器28233，worker30171/31978于随后启动。来源均为正式轨迹已保存的10秒完整checkpoint，此时无首次进入或恢复；原engine在准备分支后重新加载逐字节摘要一致，前10秒原观测块仅链接，不重放或伪造。

分支保持同一方程、参数、逐细胞M、快状态和OU/RNG。只将原60秒原生返回观察缩短为2秒：若首次高态确认2秒后仍未达到既定低活动返回标准，下一10ms边界开始1秒线性补充E细胞Z到1，随后释放原生Z。若已经原生返回则不补充。没有清零M、突触或其他状态。

先等待首次进入最多180秒；释放Z后或原生恢复后最多60秒观察再次进入，第二次高态确认后保留10秒，绝对上限243.5秒含0.5秒checkpoint边界。未出现再次进入则保留缺测，不能填原Fig5中的⑤；最长60秒复发观察不与主队列180秒窗混成同一种概率估计。完整布局自动生成后仍需Agent和用户目视，E2与患者的增强一致性独立判断。

初版计划等待“首次高态至少2秒后的下一个10秒checkpoint”再分支，实际会导致恢复延迟依赖存盘点且高态仿真缓慢。修订前尚无支路运行，现已缓存两个进入前的10秒状态，用与原60秒队列同一事件触发逻辑，仅改变观察时长为2秒。初版保存在early_z_refill_branches/plan_checkpoint_trigger_v1.json；当前以plan.json和trigger_qa.json为准。触发QA已通过：满足低活动原生返回时不人工恢复Z，高态持续时在规定边界触发，不影响原物理源文件。

最多两条，不进入原F增加独立样本。09:40后不再派发新探索，已经开始的有限轨迹保留完整终点并记录实际状态。
