# 方向探针参数响应

### contact_participation.png
三点为全局EE轴角度偏移，颜色和线型区分两噪声，逐运行和逐模式展示。其他参数固定，实际EE图随方向重采样。
**关注点**：TB固定触点时序是否改善，TA和参与结构是否付出代价；事件数与原生场不可省略。

### contact_timing_order.png
三点为全局EE轴角度偏移，颜色和线型区分两噪声，逐运行和逐模式展示。其他参数固定，实际EE图随方向重采样。
**关注点**：TB固定触点时序是否改善，TA和参与结构是否付出代价；事件数与原生场不可省略。

### native_space.png
三点为全局EE轴角度偏移，颜色和线型区分两噪声，逐运行和逐模式展示。其他参数固定，实际EE图随方向重采样。
**关注点**：TB固定触点时序是否改善，TA和参与结构是否付出代价；事件数与原生场不可省略。

### native_support.png
三点为全局EE轴角度偏移，颜色和线型区分两噪声，逐运行和逐模式展示。其他参数固定，实际EE图随方向重采样。
**关注点**：TB固定触点时序是否改善，TA和参与结构是否付出代价；事件数与原生场不可省略。

### native_timing.png
三点为全局EE轴角度偏移，颜色和线型区分两噪声，逐运行和逐模式展示。其他参数固定，实际EE图随方向重采样。
**关注点**：TB固定触点时序是否改善，TA和参与结构是否付出代价；事件数与原生场不可省略。

### sampling_support.png
三点为全局EE轴角度偏移，颜色和线型区分两噪声，逐运行和逐模式展示。其他参数固定，实际EE图随方向重采样。
**关注点**：TB固定触点时序是否改善，TA和参与结构是否付出代价；事件数与原生场不可省略。

