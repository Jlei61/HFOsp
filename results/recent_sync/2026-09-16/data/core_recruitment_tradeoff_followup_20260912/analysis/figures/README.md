# 参数—观测响应

### confirmation_ALL_native_space_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### confirmation_ALL_paired_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### confirmation_TA_native_space_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### confirmation_TA_paired_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### confirmation_TB_native_space_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### confirmation_TB_paired_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### factorial_circle_ALL.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### factorial_circle_TA.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### factorial_circle_TB.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### factorial_ellipse4_ALL.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### factorial_ellipse4_TA.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### factorial_ellipse4_TB.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### response_ALL_native_space_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### response_ALL_paired_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### response_TA_native_space_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### response_TA_paired_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### response_TB_native_space_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### response_TB_paired_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### rotation_and_mode_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### shape_range_ALL_native_space_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### shape_range_ALL_paired_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### shape_range_TA_native_space_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### shape_range_TA_paired_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### shape_range_TB_native_space_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

### shape_range_TB_paired_response.png
固定拓扑与噪声配对，参数含义与复用身份见计划和CSV。组合图颜色区分EI倍数，线型区分噪声；其余响应图沿用原比较量纲。
**关注点**：参与和时序能否同时改善；事件数与新噪声支持并列。

