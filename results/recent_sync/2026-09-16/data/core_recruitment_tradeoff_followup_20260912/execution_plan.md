# 140条完成后的有界续跑：形状×离核EE×局部EI

用户于2026-09-12明确授权“这140条完成后继续接上”。此授权替代旧批次结束后不得自动开启下一轮的限制，仅适用于本次另立的最多64条新运行；旧140条的候选、物理、损失和结果不倒改。

**启动条件**：旧140条全部完成，完整逐运行分析及旋转筛查结束，保存完整旧批次审阅快照后启动。工程失败则不派发；不把患者拟合差或未恢复双模式当成工程失败。旧确认结果此后属于已看过的开发证据，不再作为本续跑未触碰的验证集。

**科学依据与问题**：离核EE增强可提前TA的两杆质心时差，但圆核条件下上部SCL参与下降；新完成的椭圆核＋EI0.75同时保留约81–85%上部参与并改变时差，TB仍有残差。因此需要交叉比较形状、输出强度与局部抑制招募，而不是把单一低分当成恢复。条件标签变化可能改变类内事件构成，故必须同时报告不分类、TA、TB的参与与时序，并结合原生场。

1. **组合响应**：圆核/椭圆4:1 × 离核EE倍数1/1.125/1.25 × 同核EI倍数1/0.875/0.75，共18条件。6个与旧轮完全相同的条件各复用两噪声，共12条旧轨迹；新增12条件×2噪声＝24条。固定EI比较EE、固定EE比较EI、同参数比较形状，分别写入factorial_simple_effects.csv，不能只展示最优组合。
2. **有界几何微调**：圆核＋离核EE1.25、椭圆4:1＋EI0.75两个工作点，各比较x±0.75mm、y±1mm、半径2.05/2.35mm。12新条件×2噪声＝24条。两核中心仍以患者端点几何先验为基础；不加入命名TA/TB路线或事件特定刺激。扩大半径匹配旧半径的降阈值总量，但core内随机输入支持人数会变，这不是所有输入总剂量匹配。
3. **新噪声重演**：仅以冻结训练目标、两个训练噪声均N≥16为可评分约定，选择两个最好的新增条件，连同各自直接对照（最多四个条件）。在2611/2612两张已有确认拓扑，用新噪声847301/847302运行，最多16条；若不足两个可评分新增条件，补预定的圆核EE1.25、椭圆EI0.75及直接基线，最多四条件。不用旋转或细TB路线选择，不将少数模式缺失自动判为机制失败。

总上限64条新60秒运行，另复用12条已完成60秒直接对照。初轮拓扑2511、噪声847101/847102；确认使用旧两张不同拓扑的新噪声，不能宣称是未见拓扑泛化。原生图缓存从不可变基线读取，各候选独立应用权重；6个复用条件逐项核对物理参数、种子、时长与原数组完整性。

**保持原合同**：两核E只降阈值或保持背景；限核OU＋Poisson，核外E/全部I为确定均值；空间OU、慢I、Z/M、定向刺激关闭，GABA18ms不变。原观测器、1.5秒burn-in、250ms合格窗、参与与分布联合loss不变。Doff负值保留；原D16/A/B项作诊断。细传播路线、局部宽度和旋转不成为新训练惩罚。

**交付**：逐运行的参与、rank、顺序概率、成对时差、局部宽度、招募跨度、原生空间活动与旋转候选；分杆固定15行的患者真实STFT—模型发放包络、原生场多事件和连续时段GIF；同参数/同噪声配对图和组合响应曲线。旋转是筛查候选，双波源叠加可有假阳性，不作为已建立稳定螺旋。新噪声重演与实际事件支持共同决定可解释范围。

**执行资源与结束点**：最多20个CPU worker，保留50GiB，按进程树实际内存派发；两GPU接原生场旋转筛查，复用已完成的旧筛查。工程错误或资源异常停止新派发并收尾；自动汇总、出图、报告。最多64条新运行完成后停在科学审阅，不再自动加第三轮，不自动冻结模型或进入Fig5。用户无需在夜间再次确认已授权的本批次。
