### circle_EE_EI_original.png
圆核：向外兴奋 × 核内抑制，展示原图三项观测；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝核内 E→I 倍数；同一条线只改变向外 E→E，每点为60秒运行，未完成及不可估计点不补零。
参与误差：15触点参与概率的平均绝对差；顺序相关：参与内归一化rank的逐触点均值与患者的Spearman相关。 整杆缺失只要求四个SCL触点均未参与；它接近0不代表整杆完整参与或内部顺序恢复。误差0 / 相关1是参照值，不是验收门槛。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### circle_EE_EI_time.png
圆核：向外兴奋 × 核内抑制，展示窗内毫秒时间尺度；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝核内 E→I 倍数；同一条线只改变向外 E→E，每点为60秒运行，未完成及不可估计点不补零。
杆间差：每事件参与SCL质心中位数 − 参与ICL质心中位数，再取事件中位数；负值表示SCL较早。 t10跨度：参与触点累计包络质量10%时刻的极差；局部宽度：事件内各触点t90−t10中位数，再跨事件取中位数。后两项仅模型包络，无患者HFO等价线。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### circle_EE_EI_scl.png
圆核：向外兴奋 × 核内抑制，展示SCL 内部相邻顺序；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝核内 E→I 倍数；同一条线只改变向外 E→E，每点为60秒运行，未完成及不可估计点不补零。
每个概率只使用对应两个触点共同参与的事件，质心完全并列计0.5；不要求四个SCL触点全部参与。 先后概率是时序观测，不等于因果传播路径；黑点线保留患者自身的顺序变异，不强制0或1。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### circle_EE_EI_scl_lag.png
圆核：向外兴奋 × 核内抑制，展示SCL 内部毫秒时差；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝核内 E→I 倍数；同一条线只改变向外 E→E，每点为60秒运行，未完成及不可估计点不补零。
每点为共同参与事件的质心时差中位数；正值表示编号较小触点更晚。固定显示±20ms，越界以边缘三角与实际数值标注。 患者与模型使用相同触点对；质心时差不等于起始时差或局部持续时间。逐运行均值、方差、5–95%范围另保留于pairs.csv。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### circle_EE_EI_support.png
圆核：向外兴奋 × 核内抑制，展示参与与事件支持；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝核内 E→I 倍数；同一条线只改变向外 E→E，每点为60秒运行，未完成及不可估计点不补零。
上部SCL参与＝SCL9与SCL8参与概率的平均；模式占比以该运行全部合格事件为分母。 TA/TB由冻结分类器组织比较，标签也用于既有分布训练目标；两类标签存在不等于患者传播恢复。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### circle_geometry_original.png
圆核：左核位置与范围，展示原图三项观测；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝两条噪声；固定基础网络 2511，每点为60秒运行，未完成及不可估计点不补零。
参与误差：15触点参与概率的平均绝对差；顺序相关：参与内归一化rank的逐触点均值与患者的Spearman相关。 整杆缺失只要求四个SCL触点均未参与；它接近0不代表整杆完整参与或内部顺序恢复。误差0 / 相关1是参照值，不是验收门槛。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### circle_geometry_time.png
圆核：左核位置与范围，展示窗内毫秒时间尺度；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝两条噪声；固定基础网络 2511，每点为60秒运行，未完成及不可估计点不补零。
杆间差：每事件参与SCL质心中位数 − 参与ICL质心中位数，再取事件中位数；负值表示SCL较早。 t10跨度：参与触点累计包络质量10%时刻的极差；局部宽度：事件内各触点t90−t10中位数，再跨事件取中位数。后两项仅模型包络，无患者HFO等价线。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### circle_geometry_scl.png
圆核：左核位置与范围，展示SCL 内部相邻顺序；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝两条噪声；固定基础网络 2511，每点为60秒运行，未完成及不可估计点不补零。
每个概率只使用对应两个触点共同参与的事件，质心完全并列计0.5；不要求四个SCL触点全部参与。 先后概率是时序观测，不等于因果传播路径；黑点线保留患者自身的顺序变异，不强制0或1。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### circle_geometry_scl_lag.png
圆核：左核位置与范围，展示SCL 内部毫秒时差；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝两条噪声；固定基础网络 2511，每点为60秒运行，未完成及不可估计点不补零。
每点为共同参与事件的质心时差中位数；正值表示编号较小触点更晚。固定显示±20ms，越界以边缘三角与实际数值标注。 患者与模型使用相同触点对；质心时差不等于起始时差或局部持续时间。逐运行均值、方差、5–95%范围另保留于pairs.csv。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### circle_geometry_support.png
圆核：左核位置与范围，展示参与与事件支持；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝两条噪声；固定基础网络 2511，每点为60秒运行，未完成及不可估计点不补零。
上部SCL参与＝SCL9与SCL8参与概率的平均；模式占比以该运行全部合格事件为分母。 TA/TB由冻结分类器组织比较，标签也用于既有分布训练目标；两类标签存在不等于患者传播恢复。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### ellipse4_EE_EI_original.png
左核椭圆 4∶1：向外兴奋 × 核内抑制，展示原图三项观测；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝核内 E→I 倍数；同一条线只改变向外 E→E，每点为60秒运行，未完成及不可估计点不补零。
参与误差：15触点参与概率的平均绝对差；顺序相关：参与内归一化rank的逐触点均值与患者的Spearman相关。 整杆缺失只要求四个SCL触点均未参与；它接近0不代表整杆完整参与或内部顺序恢复。误差0 / 相关1是参照值，不是验收门槛。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### ellipse4_EE_EI_time.png
左核椭圆 4∶1：向外兴奋 × 核内抑制，展示窗内毫秒时间尺度；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝核内 E→I 倍数；同一条线只改变向外 E→E，每点为60秒运行，未完成及不可估计点不补零。
杆间差：每事件参与SCL质心中位数 − 参与ICL质心中位数，再取事件中位数；负值表示SCL较早。 t10跨度：参与触点累计包络质量10%时刻的极差；局部宽度：事件内各触点t90−t10中位数，再跨事件取中位数。后两项仅模型包络，无患者HFO等价线。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### ellipse4_EE_EI_scl.png
左核椭圆 4∶1：向外兴奋 × 核内抑制，展示SCL 内部相邻顺序；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝核内 E→I 倍数；同一条线只改变向外 E→E，每点为60秒运行，未完成及不可估计点不补零。
每个概率只使用对应两个触点共同参与的事件，质心完全并列计0.5；不要求四个SCL触点全部参与。 先后概率是时序观测，不等于因果传播路径；黑点线保留患者自身的顺序变异，不强制0或1。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### ellipse4_EE_EI_scl_lag.png
左核椭圆 4∶1：向外兴奋 × 核内抑制，展示SCL 内部毫秒时差；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝核内 E→I 倍数；同一条线只改变向外 E→E，每点为60秒运行，未完成及不可估计点不补零。
每点为共同参与事件的质心时差中位数；正值表示编号较小触点更晚。固定显示±20ms，越界以边缘三角与实际数值标注。 患者与模型使用相同触点对；质心时差不等于起始时差或局部持续时间。逐运行均值、方差、5–95%范围另保留于pairs.csv。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### ellipse4_EE_EI_support.png
左核椭圆 4∶1：向外兴奋 × 核内抑制，展示参与与事件支持；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝核内 E→I 倍数；同一条线只改变向外 E→E，每点为60秒运行，未完成及不可估计点不补零。
上部SCL参与＝SCL9与SCL8参与概率的平均；模式占比以该运行全部合格事件为分母。 TA/TB由冻结分类器组织比较，标签也用于既有分布训练目标；两类标签存在不等于患者传播恢复。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### ellipse4_geometry_original.png
左核椭圆 4∶1：左核位置与范围，展示原图三项观测；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝两条噪声；固定基础网络 2511，每点为60秒运行，未完成及不可估计点不补零。
参与误差：15触点参与概率的平均绝对差；顺序相关：参与内归一化rank的逐触点均值与患者的Spearman相关。 整杆缺失只要求四个SCL触点均未参与；它接近0不代表整杆完整参与或内部顺序恢复。误差0 / 相关1是参照值，不是验收门槛。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### ellipse4_geometry_time.png
左核椭圆 4∶1：左核位置与范围，展示窗内毫秒时间尺度；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝两条噪声；固定基础网络 2511，每点为60秒运行，未完成及不可估计点不补零。
杆间差：每事件参与SCL质心中位数 − 参与ICL质心中位数，再取事件中位数；负值表示SCL较早。 t10跨度：参与触点累计包络质量10%时刻的极差；局部宽度：事件内各触点t90−t10中位数，再跨事件取中位数。后两项仅模型包络，无患者HFO等价线。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### ellipse4_geometry_scl.png
左核椭圆 4∶1：左核位置与范围，展示SCL 内部相邻顺序；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝两条噪声；固定基础网络 2511，每点为60秒运行，未完成及不可估计点不补零。
每个概率只使用对应两个触点共同参与的事件，质心完全并列计0.5；不要求四个SCL触点全部参与。 先后概率是时序观测，不等于因果传播路径；黑点线保留患者自身的顺序变异，不强制0或1。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### ellipse4_geometry_scl_lag.png
左核椭圆 4∶1：左核位置与范围，展示SCL 内部毫秒时差；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝两条噪声；固定基础网络 2511，每点为60秒运行，未完成及不可估计点不补零。
每点为共同参与事件的质心时差中位数；正值表示编号较小触点更晚。固定显示±20ms，越界以边缘三角与实际数值标注。 患者与模型使用相同触点对；质心时差不等于起始时差或局部持续时间。逐运行均值、方差、5–95%范围另保留于pairs.csv。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### ellipse4_geometry_support.png
左核椭圆 4∶1：左核位置与范围，展示参与与事件支持；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝两条噪声；固定基础网络 2511，每点为60秒运行，未完成及不可估计点不补零。
上部SCL参与＝SCL9与SCL8参与概率的平均；模式占比以该运行全部合格事件为分母。 TA/TB由冻结分类器组织比较，标签也用于既有分布训练目标；两类标签存在不等于患者传播恢复。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_circle_EI_original.png
圆核：核内 E→I，展示原图三项观测；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
参与误差：15触点参与概率的平均绝对差；顺序相关：参与内归一化rank的逐触点均值与患者的Spearman相关。 整杆缺失只要求四个SCL触点均未参与；它接近0不代表整杆完整参与或内部顺序恢复。误差0 / 相关1是参照值，不是验收门槛。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_circle_EI_time.png
圆核：核内 E→I，展示窗内毫秒时间尺度；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
杆间差：每事件参与SCL质心中位数 − 参与ICL质心中位数，再取事件中位数；负值表示SCL较早。 t10跨度：参与触点累计包络质量10%时刻的极差；局部宽度：事件内各触点t90−t10中位数，再跨事件取中位数。后两项仅模型包络，无患者HFO等价线。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_circle_EI_scl.png
圆核：核内 E→I，展示SCL 内部相邻顺序；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
每个概率只使用对应两个触点共同参与的事件，质心完全并列计0.5；不要求四个SCL触点全部参与。 先后概率是时序观测，不等于因果传播路径；黑点线保留患者自身的顺序变异，不强制0或1。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_circle_EI_scl_lag.png
圆核：核内 E→I，展示SCL 内部毫秒时差；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
每点为共同参与事件的质心时差中位数；正值表示编号较小触点更晚。固定显示±20ms，越界以边缘三角与实际数值标注。 患者与模型使用相同触点对；质心时差不等于起始时差或局部持续时间。逐运行均值、方差、5–95%范围另保留于pairs.csv。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_circle_EI_support.png
圆核：核内 E→I，展示参与与事件支持；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
上部SCL参与＝SCL9与SCL8参与概率的平均；模式占比以该运行全部合格事件为分母。 TA/TB由冻结分类器组织比较，标签也用于既有分布训练目标；两类标签存在不等于患者传播恢复。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_ellipse4_EI_original.png
椭圆：核内 E→I，展示原图三项观测；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
参与误差：15触点参与概率的平均绝对差；顺序相关：参与内归一化rank的逐触点均值与患者的Spearman相关。 整杆缺失只要求四个SCL触点均未参与；它接近0不代表整杆完整参与或内部顺序恢复。误差0 / 相关1是参照值，不是验收门槛。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_ellipse4_EI_time.png
椭圆：核内 E→I，展示窗内毫秒时间尺度；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
杆间差：每事件参与SCL质心中位数 − 参与ICL质心中位数，再取事件中位数；负值表示SCL较早。 t10跨度：参与触点累计包络质量10%时刻的极差；局部宽度：事件内各触点t90−t10中位数，再跨事件取中位数。后两项仅模型包络，无患者HFO等价线。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_ellipse4_EI_scl.png
椭圆：核内 E→I，展示SCL 内部相邻顺序；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
每个概率只使用对应两个触点共同参与的事件，质心完全并列计0.5；不要求四个SCL触点全部参与。 先后概率是时序观测，不等于因果传播路径；黑点线保留患者自身的顺序变异，不强制0或1。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_ellipse4_EI_scl_lag.png
椭圆：核内 E→I，展示SCL 内部毫秒时差；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
每点为共同参与事件的质心时差中位数；正值表示编号较小触点更晚。固定显示±20ms，越界以边缘三角与实际数值标注。 患者与模型使用相同触点对；质心时差不等于起始时差或局部持续时间。逐运行均值、方差、5–95%范围另保留于pairs.csv。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_ellipse4_EI_support.png
椭圆：核内 E→I，展示参与与事件支持；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
上部SCL参与＝SCL9与SCL8参与概率的平均；模式占比以该运行全部合格事件为分母。 TA/TB由冻结分类器组织比较，标签也用于既有分布训练目标；两类标签存在不等于患者传播恢复。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_position_original.png
左核左移：三张网络的配对响应，展示原图三项观测；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
参与误差：15触点参与概率的平均绝对差；顺序相关：参与内归一化rank的逐触点均值与患者的Spearman相关。 整杆缺失只要求四个SCL触点均未参与；它接近0不代表整杆完整参与或内部顺序恢复。误差0 / 相关1是参照值，不是验收门槛。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_position_time.png
左核左移：三张网络的配对响应，展示窗内毫秒时间尺度；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
杆间差：每事件参与SCL质心中位数 − 参与ICL质心中位数，再取事件中位数；负值表示SCL较早。 t10跨度：参与触点累计包络质量10%时刻的极差；局部宽度：事件内各触点t90−t10中位数，再跨事件取中位数。后两项仅模型包络，无患者HFO等价线。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_position_scl.png
左核左移：三张网络的配对响应，展示SCL 内部相邻顺序；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
每个概率只使用对应两个触点共同参与的事件，质心完全并列计0.5；不要求四个SCL触点全部参与。 先后概率是时序观测，不等于因果传播路径；黑点线保留患者自身的顺序变异，不强制0或1。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_position_scl_lag.png
左核左移：三张网络的配对响应，展示SCL 内部毫秒时差；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
每点为共同参与事件的质心时差中位数；正值表示编号较小触点更晚。固定显示±20ms，越界以边缘三角与实际数值标注。 患者与模型使用相同触点对；质心时差不等于起始时差或局部持续时间。逐运行均值、方差、5–95%范围另保留于pairs.csv。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_position_support.png
左核左移：三张网络的配对响应，展示参与与事件支持；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
上部SCL参与＝SCL9与SCL8参与概率的平均；模式占比以该运行全部合格事件为分母。 TA/TB由冻结分类器组织比较，标签也用于既有分布训练目标；两类标签存在不等于患者传播恢复。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_EI_endpoints_original.png
降低核内 E→I：三张网络的端点比较，展示原图三项观测；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
参与误差：15触点参与概率的平均绝对差；顺序相关：参与内归一化rank的逐触点均值与患者的Spearman相关。 整杆缺失只要求四个SCL触点均未参与；它接近0不代表整杆完整参与或内部顺序恢复。误差0 / 相关1是参照值，不是验收门槛。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_EI_endpoints_time.png
降低核内 E→I：三张网络的端点比较，展示窗内毫秒时间尺度；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
杆间差：每事件参与SCL质心中位数 − 参与ICL质心中位数，再取事件中位数；负值表示SCL较早。 t10跨度：参与触点累计包络质量10%时刻的极差；局部宽度：事件内各触点t90−t10中位数，再跨事件取中位数。后两项仅模型包络，无患者HFO等价线。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_EI_endpoints_scl.png
降低核内 E→I：三张网络的端点比较，展示SCL 内部相邻顺序；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
每个概率只使用对应两个触点共同参与的事件，质心完全并列计0.5；不要求四个SCL触点全部参与。 先后概率是时序观测，不等于因果传播路径；黑点线保留患者自身的顺序变异，不强制0或1。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_EI_endpoints_scl_lag.png
降低核内 E→I：三张网络的端点比较，展示SCL 内部毫秒时差；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
每点为共同参与事件的质心时差中位数；正值表示编号较小触点更晚。固定显示±20ms，越界以边缘三角与实际数值标注。 患者与模型使用相同触点对；质心时差不等于起始时差或局部持续时间。逐运行均值、方差、5–95%范围另保留于pairs.csv。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_EI_endpoints_support.png
降低核内 E→I：三张网络的端点比较，展示参与与事件支持；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
上部SCL参与＝SCL9与SCL8参与概率的平均；模式占比以该运行全部合格事件为分母。 TA/TB由冻结分类器组织比较，标签也用于既有分布训练目标；两类标签存在不等于患者传播恢复。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_edge_vs_weight_original.png
离核输出：增加边数与增加权重，展示原图三项观测；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
参与误差：15触点参与概率的平均绝对差；顺序相关：参与内归一化rank的逐触点均值与患者的Spearman相关。 整杆缺失只要求四个SCL触点均未参与；它接近0不代表整杆完整参与或内部顺序恢复。误差0 / 相关1是参照值，不是验收门槛。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_edge_vs_weight_time.png
离核输出：增加边数与增加权重，展示窗内毫秒时间尺度；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
杆间差：每事件参与SCL质心中位数 − 参与ICL质心中位数，再取事件中位数；负值表示SCL较早。 t10跨度：参与触点累计包络质量10%时刻的极差；局部宽度：事件内各触点t90−t10中位数，再跨事件取中位数。后两项仅模型包络，无患者HFO等价线。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_edge_vs_weight_scl.png
离核输出：增加边数与增加权重，展示SCL 内部相邻顺序；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
每个概率只使用对应两个触点共同参与的事件，质心完全并列计0.5；不要求四个SCL触点全部参与。 先后概率是时序观测，不等于因果传播路径；黑点线保留患者自身的顺序变异，不强制0或1。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_edge_vs_weight_scl_lag.png
离核输出：增加边数与增加权重，展示SCL 内部毫秒时差；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
每点为共同参与事件的质心时差中位数；正值表示编号较小触点更晚。固定显示±20ms，越界以边缘三角与实际数值标注。 患者与模型使用相同触点对；质心时差不等于起始时差或局部持续时间。逐运行均值、方差、5–95%范围另保留于pairs.csv。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。

### multiseed_edge_vs_weight_support.png
离核输出：增加边数与增加权重，展示参与与事件支持；上排TB=M0、下排TA=M1，沿用用户旧图的类别行序。颜色＝网络；实线圆点 / 虚线三角＝噪声 847401 / 847402，每点为60秒运行，未完成及不可估计点不补零。
上部SCL参与＝SCL9与SCL8参与概率的平均；模式占比以该运行全部合格事件为分母。 TA/TB由冻结分类器组织比较，标签也用于既有分布训练目标；两类标签存在不等于患者传播恢复。
**关注点**：参数改变对两类事件是否有取舍，以及响应是否在同网噪声重演和不同网络中保留。
