### patient_actual_N_reference.png
训练网络按模型实际N，抽取同一患者CAL块内的连续N事件；每次保留自然标签比例。灰条描述患者窗口统计量的变化，既非独立样本置信区间，也非全患者变异的完整覆盖；短于N的块不贡献窗口。训练与确认分别成图，避免运行标签重叠。
**关注点**：不能把低事件数当成机制失败；同时检查模式比例、杆间时差的中心及散布，而非只看loss。

### patient_actual_N_reference.pdf
训练网络按模型实际N，抽取同一患者CAL块内的连续N事件；每次保留自然标签比例。灰条描述患者窗口统计量的变化，既非独立样本置信区间，也非全患者变异的完整覆盖；短于N的块不贡献窗口。训练与确认分别成图，避免运行标签重叠。
**关注点**：不能把低事件数当成机制失败；同时检查模式比例、杆间时差的中心及散布，而非只看loss。

- 候选1：左核X-0.68mm / 左核Y-0.49mm / 右核X-0.70mm / 右核Y+0.46mm / 向外EE+0.02 / 轴偏移+3.94°（`g2_b01_p01`）
- 候选2：左核X-0.48mm / 左核Y-0.59mm / 右核X-1.06mm / 右核Y+0.40mm / 向外EE-0.02 / 轴偏移+6.77°（`g2_b04_p01`）
### patient_actual_N_confirmation_reference.png
新网络确认按模型实际N，抽取同一患者CAL块内的连续N事件；每次保留自然标签比例。灰条描述患者窗口统计量的变化，既非独立样本置信区间，也非全患者变异的完整覆盖；短于N的块不贡献窗口。训练与确认分别成图，避免运行标签重叠。
**关注点**：不能把低事件数当成机制失败；同时检查模式比例、杆间时差的中心及散布，而非只看loss。

### patient_actual_N_confirmation_reference.pdf
新网络确认按模型实际N，抽取同一患者CAL块内的连续N事件；每次保留自然标签比例。灰条描述患者窗口统计量的变化，既非独立样本置信区间，也非全患者变异的完整覆盖；短于N的块不贡献窗口。训练与确认分别成图，避免运行标签重叠。
**关注点**：不能把低事件数当成机制失败；同时检查模式比例、杆间时差的中心及散布，而非只看loss。

- 候选1：左核X-0.68mm / 左核Y-0.49mm / 右核X-0.70mm / 右核Y+0.46mm / 向外EE+0.02 / 轴偏移+3.94°（`g2_b01_p01`）
