### noise_847401_TB_native_sequence.png
同一拓扑上，第一个噪声重演最早三个合格 TB 事件的原生 E 活动实际帧；每行一个事件，列为观察窗起点后的 40、80、120、160、200、240 ms。直接从既有 GIF 的场区域提取，沿用源 GIF 色标；不是新仿真，也不是按患者相似度挑选。**关注点**：右侧 core 附近活动、向左上扩展及较晚 SCL 区域活动的先后。

### noise_847402_TB_native_sequence.png
相同参数和拓扑，更换动力学噪声后的最早三个合格 TB 事件，坐标和帧时刻与上一图一致。局部 A 区域与 B 侧扩展活动有时并存；两张图的强度沿用各自原 GIF 的整运行色标，因此用于比较过程而非跨噪声绝对亮度。**关注点**：TB 标签相同仍可包含不同的原生时序；这些采样帧不证明碰撞、螺旋或因果起源。
