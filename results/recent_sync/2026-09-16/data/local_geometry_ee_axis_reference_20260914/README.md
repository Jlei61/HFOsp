# 局部优化参考工作点：背景参数已固定

2026-09-14，按用户“先把之前最好的工作点的其他参数固定”执行。参考点为 `bridge_circle_out125_xminus075`，中文简称“左移圆核＋向外EE增强”。此处固定的是后续局部优化的出发点，患者完整双模式恢复尚未接受。

## 选择依据与局限

本次核对多网络响应的完整结果：该点在已完成全部六次重复的六个条件中，按运行等权的既有综合分数最低（2.9273）。六次为3张拓扑2511/2711/2712 × 2条噪声847401/847402，每次60秒；排除启动期和不完整事件窗后，共924个事件，TA 470、TB 454。这些都是已看过的开发证据，不作为新的独立验证。

选择它是为了收敛工作点，不能称为全参数空间最优或已复现患者机制。TB每条运行的杆间质心差中位数仍约+35 ms，患者约+1.19 ms；SCL内部和其他局部顺序亦有残差。杆间差指每事件参与SCL触点质心的中位数减去参与ICL触点质心的中位数，再在事件间汇总；不把924个事件当924个网络重复。

## 保存的参数

所有倍数均相对同一历史纠正网络基线，×1并不表示EE、EI等通路绝对强度相等。

| 参数 | 本次固定/保存值 | 后续约定 |
|---|---|---|
| 两核中心，mm | 左核(3.44921432, 12.12890135)；右核(16.47920304, 3.96551153) | 保留为XY微调起点；本次未移动 |
| 两核形状与半径 | 圆形；均为1.75257829056 mm | 固定 |
| E阈值规则 | 背景18 mV；原始潜在值均值17.5、标准差1 mV；只保留降低部分；降幅系数两核均1；下限11 mV | 固定规则和潜在随机身份；允许部分core细胞等于背景，不允许升阈值 |
| 核内 E→E | ×0.85 | 固定 |
| 核内 E→核外 E | ×1.25 | 后续“EE强度”维度；此处指两个核向外的权重，不是全局EE一起缩放 |
| 核外 E→E | ×1.00 | 固定 |
| 核内 E→I、I→E、I→I | 各×1.00 | 固定 |
| 向外EE连接数 | ×1.00 | 固定；XY变化引起通路成员改变时自然变化，不另做数量补偿 |
| EE连接范围 | 平行尺度0.537401 mm，横向0.403051 mm；实际尺度比4:3 | 固定；旧配置AR=2是横向扩大前的参考值 |
| EE方向 | −22.805384°，相对当前参考轴偏移0° | 后续方向微调起点 |
| 膜时间常数 | E 20 ms；I 10 ms | 固定 |
| AMPA/GABA上升与衰减 | AMPA 0.7/3.5 ms；GABA 1/18 ms | 固定 |
| 核内平均外部输入 | 背景输入率的0.95倍 | 固定 |
| 随机输入 | 仅core E的OU＋Poisson；两核OU相关系数0，时间常数150 ms，原强度参数3.3 | 固定；两核各自OU，不是全场共享OU |
| 核外E及全部I输入 | 确定的期望到达量，保留递归响应 | 固定 |
| Z、M、外加慢I、空间OU、定向刺激 | 全关闭 | 固定 |
| 几何与规模 | 20×20 mm；32,000 E＋8,000 I；dt=0.1 ms | 固定 |
| 电极、读出与事件窗口 | 原15触点坐标/映射；原观测器；250 ms窗；正式分析排除前1,500 ms及不完整窗 | 固定 |

完整值见[实际解析参数](resolved_runtime.json)与[冻结合同](reference_contract.json)。前者保留继承参数供追溯，执行时以合同覆盖顺序为准：例如继承的`engine.core_r=1.5`不是实际半径，旧父配置中的慢I不能重新启用。

## 下一步的允许变化与图

只留下三组共六个标量：两核XY四个坐标、核内E→核外E权重倍数、全局EE方向偏移。其他物理字段固定；本次不确定新搜索范围、算法、预算或新的三组损失权重，也未派发仿真。这里的方向操作会按新方向重建EE边和距离时延，不能称为同一张邻接图上只改角度。移动XY也会改变core成员、总降阈值量和通路归属；这些是几何变化的实际后果，应记录而非暗中补偿。

未来每次比较仍需展示患者TA/TB两个模板，并报告三组观测：平均rank模板、杆内触点对的顺序/毫秒差及杆间时差、触点和两杆联合参与。顺序相似不等于实际时间恢复。固定显示行SCL9→SCL6、ICL11→ICL1；内部数据列保持原身份，以显式映射重排；未参与行保留。模型发放包络与患者HFO频谱不等价。新三组目标尚未实施，本次只保留历史loss作为选点来源。

## 已完成核对

六条运行的候选物理参数完全一致，三张拓扑各自的两条噪声具有相同静态数组；全部无升阈值E细胞、无边界截断，关闭项与实际输入值一致。重新构建2511/847401的静态模型，九项位置/阈值/成员/连接/权重摘要与原运行完全一致；没有重跑动力学。

原worker的primary数包含尚未执行完整窗筛选的事件，分析数使用窗完整落在[1,500,60,000] ms的事件。本次逐运行从事件记录重算，确认与分析表的924事件一致。[验证记录](validation.json)、[逐运行观测](evidence/observations.csv)、[六条件排序](evidence/ranking_complete_conditions.json)、[文件清单](manifest.json)均已保存；原大轨迹保持原位并记录来源。

共享入口为[主目录配置](/home/honglab/leijiaxin/HFOsp/config/topic4_local_geometry_ee_axis_reference_v1.json)。它是带校验的配置指针，不是旧worker可以直接执行的新批次配置；所有消费者应读取完整合同及解析参数。
