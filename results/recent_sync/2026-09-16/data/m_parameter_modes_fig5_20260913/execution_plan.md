# M参数组合与不同模式的完整Fig5候选

用户在2026-09-13明确授权：启动M不同参数组合的搜索，并制作不同模式的完整新版Fig5。此前Z扫描147条和reset长程对照继续原队列；它们的未完成不阻塞本次独立授权，也不能被本次替换。

## 问题与固定条件

在同一历史手放双核和C快网络上，改变M的单次放电增益与记忆时间，是否分别产生反复有限事件、进入后持续高率、自主返回、人工补充Z后返回、再次进入或强适应下低活动？这些都是要观察的结果，不按期望预设存在，也不将高率平台称为持续振荡。

固定40,000细胞（32,000E/8,000I）、原始手放XY和核范围、连接及GABA参数、原生全局/空间OU与Poisson输入、dt=0.1ms、拓扑6101。实际E阈值降低781/等于背景31219/升高0。Z固定τZ=5秒、耗竭阈值95.19851312666987；没有把其他Topic4工作树的限核OU、形状或连接搜索混入本底物。

M对所有E细胞按spike加1并以τM衰减，电流为ηM·M，I细胞M=0。扫描ηM={0.005,0.01,0.02,0.04}和τM={1,2,4,8,20}秒，两个配对噪声9108401/9108402，共40条正式轨迹。全体从Z=1/M=0开始，相同噪声种子跨参数配对。固定ηM改变τM也改变稳态增益；ηM×τM秒相同的组合用于区分记忆时间与长期平均剂量，例如(0.04,1)、(0.02,2)、(0.01,4)、(0.005,8)。这不是四个机制完全等价的假设。

## 连续协议

1. 首次进入最多等待180秒；判据沿用全E每10ms放电率≥200Hz持续200ms，保存高段起点及确认时间。窗内未进入是右删失，不能推成永远不会进入。
2. 进入后至少给原生方程60秒的返回观察窗，覆盖本轮最大τM的三倍。返回判据为连续两秒中，每秒平均E率<50Hz且低于5Hz的10ms箱比例≥20%；低于200Hz本身不足以称为恢复。再独立检查恢复后有限事件和局部核状态。
3. 若60秒结束时仍未建立返回，下一10ms边界启动一次外部Z补充，1秒线性升至1，随后立刻释放原生Z。全程不清零M，不重置膜、突触、延迟或随机历史。这个时刻按首次进入对齐，与旧示例75.5秒的较早补充不同，不能把两种干预当作相同轨迹。
4. 若原生已经返回，则不补充Z。无论自主还是外部返回，确认恢复后继续观察180秒是否再次进入。再次进入后保留10秒；绝对上限430秒。未返回者同样在430秒结束，不无限运行。停止输出对齐0.5秒完整checkpoint边界，因此恢复后的名义180秒和再次进入后的10秒可多记录不足0.5秒；使用实际end_s及实际恢复确认时刻报告观察窗，不把这部分写成严格等长的复发概率实验。首次进入F的180秒截止本身恰好对齐，不受此影响。

在图上显式区分native与external恢复。若低活动验证窗口与干预重叠，保守记为外部恢复，不能冒称自主。多个高率段之间只有建立了低活动返回才计作再次进入；高平台上的短暂波动不拆成多次发作。

## 同一轨迹的完整读出

每条轨迹按10秒分块保存：15触点原始LFP电流proxy（2kHz）、固定80细胞raster（0.1ms）、E/I及两核/核外放电计数（1ms）、20×20原生二维放电场（1ms）、Z和M及实际抑制电流（5ms）。完整checkpoint携带所有快慢状态及随机历史，分块边界不重置模型。

每个完成条件自动生成完整Fig5布局：A为核实的原电流proxy及固定带通/尺度；B连续raster与A/C对齐；C同框Z左轴、实际ηM·M右轴；D为①–⑤对应真实50ms空间活动；E1为Z/实际GABA/E率三维轨迹、连续时间色条在右侧；E2为实际模型早期原生功率及接触读出，配原样正式Fig3C；F为本次M二维扫描。未出现的阶段留空，不能用后期普通事件代替⑤。

E2模型取首次高段起点后1秒，1–150Hz接触CAR log-power robust-z，基线为初始化后[1,min(30,onset−2)]秒，最少3秒。原生二维场显示该窗口的spike-rate带功率并归一化，和接触proxy明确区分。患者原样使用Fig3C：E1146/SZ3、临床0–10秒及其原冻结基线/几何/色条。二者时间尺度、基线和观测量不相同；模型与15患者触点的相关只作已选示例描述，不能预设一致，更不使用患者自身TA相关充当模型匹配。基线不足或功率方差不足如实保留缺测。

F使用本轮ηM×τM的180秒限制平均首次进入时间及进入比例，每格两个噪声样本。灰色为未完成两种子，斜线为存在删失，不填补或平滑虚构网格。旧Z扫描只适用于原M=0.02/2秒，不能换标签充当其他M条件的F。

完整布局和完整①–⑤科学过程是两回事；缺少返回或再进入的图是负结果候选，不宣称通过原完整Fig5验收。各模式代表只按距离原M工作点的参数距离、固定种子和编号选择，不按患者匹配挑例；结束时刷新模式代表的完整F，形成模式图包。

## 检查、资源与结束

启动前通过1秒真实SNN读出canary，含0.5秒完整checkpoint恢复，对照已有M-on轨迹的raster、二维场、LFP、Z/M逐值一致；20个M组合核对真实更新和电流；协议逻辑另用明确标为测试的率输入核对native/external和再次进入，测试输入不充当研究数据。新完整布局先用既有真实轨迹目视检查，不冒充本次新实验结果。

输出放/data并从本结果目录链接，避免根分区被长轨迹填满。新扫描最多32worker，两轮参数扫描同时活跃的worker合计上限40；当前旧扫描占32时新批次最多8，随旧批次结束自动增加。每worker预留4GiB、机器保留60GiB可用内存；不干预其他任务的进程。出现失败停止新派发并收尾，保留所有已完成和可恢复状态。

40条完成后自动生成逐条件图、参数表、模式代表图和科学审阅，停在审阅点。不自动再加网格、冻结患者模型、声称Hopf或临床发作，仍需Agent及用户目视检查。

## 2026-09-13 夜间图件与调度修订

本次自主8小时授权后，原9条在跑M轨迹已保持PID及状态连续接管，仅优先派发既定40条中ηM=0.005、τM=1秒的两种子。两轮扫描合计派发目标43、M最多32worker，另加CPU忙碌85%限制，60GiB内存余量与每worker4GiB预算保持；调度目标允许旧队列补位时短暂超过，实际保护依靠内存与CPU门槛。具体见scheduling_amendment_20260913.json；未增加原40条条件、未改变种子、方程或终点。

离线审查发现旧例子原E2全局高率起点后1秒，触点带功率全部下降，尽管与Fig3C的空间相关很高。新版E2保留原始触点robust-z及其窗口不变，用零居中色标明确区分增强（蓝）与降低（红），避免负值也画深蓝制造与患者正增强图相似的错觉。原生场新增相对同一模型基线的1–150Hz功率变化（dB），原绝对归一化功率仍保存于元数据；基线中位功率为零的格点视为不可估计并遮罩，不把数值下限产生的巨额比值当作增强。正式Fig3C原图及色条不变。

E1在无返回的条件下显示直到实际观测末尾，在无高态的条件下显示完整时程；有明确返回的轨迹仍用已约定转变放大范围。以上属于观测表达修订，不是重新挑取更有利于患者匹配的时间窗口。

## 首次终点与完整轨迹分开汇总（2026-09-13夜间）

F的统计量、180秒窗口、200Hz/200ms定义与两个原配对噪声均不改变。新增从原40条任务的已闭合1ms放电计数前缀独立确认首次进入：该终点一旦发生，不必等返回/再进入随访结束才能进入F。若尚未进入且闭合前缀不足180秒，仍为PENDING；只有真正观测满180秒才可记右删失。轨迹整体完成数与首次终点已确定数分别报告，两个种子首次终点齐备才着色该F格点。外部早期恢复支路和旧90秒轨迹不增加F样本。

验证覆盖19/20高箱边界、跨数据块的200ms高段、179/180秒删失边界及原始计数与分区守恒。这里只改善已确定终点的可见性，没有改变条件、阈值、刺激、方程或挑选结果。
