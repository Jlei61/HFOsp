# 2026-09-11夜间执行修订

用户授权设置goal、自主8–10小时，先审查最新执行版，未恢复患者两模式则继续。120条原始扫描全部完整；原孤立250 ms窗口资格保留，额外全检测仅作开发诊断。不会把观察样本改多后直接称为同一训练合同。

当前已启动8个固定组合×两条噪声×20秒，保持原物理和观测源码。随后最多8条件×两噪声×60秒及4条件×两新拓扑×两新噪声×60秒；本夜新增不超过48条，含旧首批不超过168条。长重演用于实际支持量与稳定性，不期待时长本身改变传播形态。

第一批原计划DE缺少双噪声可评分父代，因此改为有记录的残差驱动组合探针，不声称自动优化。改动参数、直接出发点和输出路径见plan.json与wave1_units.json。两个局部阈值偏置探针成对保留，不根据标签预设哪个core应先发。

起始00:12，08:12后停止新派发、10:12硬上限，最多8worker，已测单运行峰值5.90 GiB，保留40 GiB系统余量、18 GiB进程树保护。没有改变双核降阈值、核内随机输入、核外确定均值、固定GABA18 ms及禁用外加状态/定向刺激的合同。

正式状态在status.json；阳性阴性均交付，自动图解码不代替患者双模式科学接受。


## 第二阶段修订：2026-09-11 01时，首批16条完成后

首批1125次检测、120次原primary；减弱核内EE至0.75的条件有21/22个primary，两核完整平均率约5Hz，但仍以TB为主。强弱阈值偏置分别产生几乎单一TA或TB，尚非完整双模式。下一批将原16条单纯长重演改为8条件×2噪声×60秒：靠近上部SCL与端点原位上移3mm两布局，各比较核内EE0.75、EE0.85、EE0.85加左核降幅1.15、EE0.85加核内输入均值0.95。每个参数效应有同几何60秒对照；总48条新增/168条正式上限及时间限制不变。

输入均值是独立版本的小对照，经已有external_e_rate_drive接口仅向E-core加入常数速率偏移。条件Poisson方差随均值自然改变，OU参数、核外确定均值、GABA及原观察器不变。3条500ms实施检查通过，scale1与原引擎62/62数组完全相同；这不是传播阳性证据。正式source与参数在tonic_input_plan.json、long_selection.json中冻结。

对照图的层次也予以明确：此前全触点黑底图包含患者未参与触点的亮信号，是全窗口QC；Fig2C主图实际只使用参与且质心可用的触点。补充参与定义匹配的显示，同时保留全触点QC和完整模型原生场；不更改训练mask/资格，不把Fig2C方向筛选示例当自然模式全分布。


## 03:55：末16条先分成两个最多8条的阶段

完整第二批16条仍未恢复患者两类传播：近SCL的增强左核条件TA占主导但ICL缺段；中间位置EE0.75补回TA参与，却有SCL系统性偏晚。中间位置EE0.85和左核降幅1.15仍主要为TB；均值0.95增加合格事件量而TA稀少。因此末16条先分8条在两个已测位置的几何中点做局部精调，保留EE0.75与EE0.85加均值0.95两种有事件支持的参数组合，各比较左核降幅1和1.15。两套组合的差异不能归因于单一EE因素。

第一阶段固定4条件×2原噪声×60秒，拓扑2511。第二阶段最多8条，等待首8条完整结果；传播有实质改善再做新图重演，否则继续有边界的针对性组合。不是16条已拟合候选确认，也不是DE；先前未执行的2×2新图确认生成器仅为草案。全夜48条新增/168条正式、最多8 worker及时间上限不变。


## 04:45：预备核心OU相关性对照，尚未选择正式末批

完整长记录中，固定参数确实偶尔产生较相容TA/TB时序，但常见事件仍有系统性残差。实际输入核查显示两个核心的OU调制条件到达率完全相同（最大浮点差约4.4e-16/ms），独立性只来自逐细胞Poisson。这提示一个可检验的输入访问假设，不意味着原因已被识别。

新增独立执行器与两核心OU混合输入模块，不改当前final_A、旧worker或观测器。拟议公式eta_k=sqrt(rho)*xi_global+sqrt(1-rho)*xi_k；三条OU过程具有相同150ms时间常数、sigma_n=3.3和零初态，因此每核边际OU规律不变，仅改变两核慢输入相关性。随机输入仍只作用E-core；无患者标签、事件检测、路径刺激、神经元Z/M或外加慢I。不同rho的实际随机创新不同，不宣称逐步输入匹配。

利用一条final_A完成后释放的名额，按顺序运行3条500ms工程canary（rho=1/0.5/0），同时最多8条物理运行，峰值树RSS约2.65GiB。rho1与原tonic500ms对照62/62数组精确一致；三条实际核内速率公式逐步核对误差为0，核外E及全部I均值不变，未发生中间/最终负速率截断。这三条共1.5秒是额外工程检查，单列计数，不占用/增加48条正式预算。

正式末批仍未选择或派发。必须先完成当前8条中点位置精调的传播审阅，再决定余下最多8条用于新图确认、相关性对照或其他有依据的针对性实验；不能由canary通过自动提名或启动搜索。冻结来源与实际检查分别在core_ou_correlation_plan.json、core_ou_correlation_canary_audit.json。


## 05:04：完整中点审阅后，冻结最后8条核输入相关性实验

中点4条件×2噪声全部完整：1035个全检测、651个原primary。EE0.75两条件的TA仍有约100ms的SCL偏晚；EE0.85加均值0.95和左核降幅1.15使TA杆间中位转负，但常见示例失去左端ICL及上部SCL，不能将换了参与集合后的杆间中位解释为完整恢复。因此没有提名中点工作点。末8条回到同一上移3mm布局的两个完整既有条件：EE0.75有两噪声中的少见完整TA，EE0.85加均值0.95曾出现更相容的TB个例；这些是能力诊断而非恢复验收。检验共享核输入是否限制不同传播响应的出现机会。

每个背景只改变两核慢输入OU相关系数ρ=0.5/0，ρ=1参照已完成；2背景×2相关系数×2原噪声×60秒。逐核OU边际规律不增加方差，核外无随机输入，无路由或标签刺激。静态图和阈值保持父条件身份，相同seed不冒称相同实际Poisson输入。3条500ms工程检查已通过，ρ=1与原执行器62项数组完全相同。完整观察器审计与全分布图继续并行产出；没有等待其排版完成才派发，也未放宽原primary规则。全夜48新增/168正式上限不变；本轮没有新图确认。
