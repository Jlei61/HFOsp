# M开启工作点：Figure 5 F的Z动力学参数响应面

用户授权（2026-09-12）：今晚启动M开启工作点的多参数搜索，目标为此前右下角F图；完整图仍按既定Figure 5验收。

## 科学问题和固定条件

在同一手放双核、相同连接与原生OU输入规律下，弱而快的M开启时，Z动力学参数如何影响从初始化到第一次进入runaway的时间及180秒内进入机会？这与正在运行的Z reset后复发定位是两个不同终点，后者继续原1000秒和条件分支，不被本扫描替代。

沿用手放Core A (4.1992, 9.1289) mm、Core B (16.4792, 3.9655) mm；40,000神经元，32,000 E、8,000 I，dt=0.1 ms，拓扑6101及当前C快网络载体。实际E阈值降低781个、等于背景31219个、升高0个；不改XY、核尺度、连接、GABA时间常数或输入空间规律。此底物不是其他任务新近限核OU/改形状的患者传播搜索底物。

M固定ηM=0.02、τM=2秒，全体E细胞逐神经元开启。每条轨迹从Z=1、M=0开始，保留正常膜/突触/噪声初始化；没有kick、Z refill或M reset。

## 扫描、统计单位和时间定义

7×7联合网格：τZ为2.5–10秒，等比节点包含5秒；耗竭电流阈值I_th为75–120 mV equivalent，分段等距节点包含95.19851312666987。精确节点在[protocol.json](protocol.json)。每格三个相同配对噪声种子9108401/9108402/9108403，共147条正式轨迹，另有2秒分段恢复canary；没有复用M-off扫描数值。

τZ同时控制耗竭和恢复。I_th是原始GABA电流使Z朝零演化的门槛，不是GABA连接强度，也不是按每次间期事件扣减固定资源。实际方程为τZ·dz/dt=1[I_GABA<I_th]−z，dm/dt=−m/τM+spikes；E膜电流为IE−Z·II−0.02M，I细胞Z=1/M=0。

每条从t=0初始化开始观察，最多180秒。全E每10毫秒分箱放电率≥200 Hz且连续200毫秒为操作性进入；同时保存该高段起点和200毫秒结束时的确认时间。F沿用旧F的确认时间，已知原工作点同一种子确认73.68秒（高段起点73.48秒）。确认后继续0.5秒再结束，以保留进入之后的短过程；这不检验持续振荡或自主返回。

颜色为三条轨迹min(T确认,180秒)的均值；观察窗结束仍未进入者是右删失。完整格点另报告180秒内进入比例和逐条结果；不能预设所有条件必定进入、只差早晚。尚未完成三条的格点显示缺测灰色，与删失斜线区分，不插值伪造更密网格。每格三个噪声样本、一个固定拓扑；患者和单次间期事件不作为本实验独立统计单位。

## 检查、资源和自动交付

先验证2秒canary的原生E/I放电计数、Z/M与现有M-on轨迹逐值一致，包括1秒完整状态checkpoint后恢复；另检查9个极端/中心参数组合的实际Euler方程与进入判据边界。中心正式轨迹完成后再与原73.68秒首次进入的完整前缀逐值核对。

最多32个单线程worker，为每个worker预留4 GiB，系统可用内存保留60 GiB；磁盘剩余不足20 GiB暂停派发。20秒保存包含膜、不应期、突触、延迟历史、Z/M及两种随机历史的checkpoint，可恢复而不重新初始化。全网放电/分区计数用10毫秒记录，Z/M/实际电流用100毫秒记录；不为整个扫描保存全40,000神经元的稠密raster。原reset长程任务不改动。

完成后自动汇总CSV/JSON、F的PNG/PDF及配套“进入时间×进入比例”图，保留删失与同噪声配对核查。首个完整网格出现后开始输出阶段图。失败停止新增派发、保留已运行任务收尾；完成147条后停止在科学审阅点，不自动追加网格或选择一个新工作点。

完整图的A–E仍服从[既定Figure 5验收标准](../reset_state_diagnosis_20260911/figure_acceptance.md)。本F候选的完成不替代①–⑤复发轨迹、对应SEEG/raster/ZM/真实二维场及Fig3C比较；图件还需Agent自查和用户目视审阅。
