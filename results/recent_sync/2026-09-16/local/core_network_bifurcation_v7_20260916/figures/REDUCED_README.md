### reduced_1_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_2_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_3_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_4_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_5_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_6_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_7_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_8_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_9_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_10_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_11_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_12a_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_12b_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_13_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_14_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_15a_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_15b_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_16a_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_16b_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_17_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_18_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_19_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_20a_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。

### reduced_20b_waveform.png / .pdf
与同编号原生SNN条件使用相同J。这里的线是确定性模型周期边值解，a/b表示同J的不同周期族；不生成或伪造raster。
**关注点**：核对同编号、参数、模型层与两核读出。
