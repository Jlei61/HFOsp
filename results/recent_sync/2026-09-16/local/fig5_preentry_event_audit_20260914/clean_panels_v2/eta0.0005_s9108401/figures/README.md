### fig5.png / .pdf
保留A–F编号，去掉所有描述性标题；时间轴统一Time (s)，B图例右上角竖排。A框出两个完整300ms放大窗口，Core A/B明确为E细胞，顶端I独立显示；C状态2同一事件后移20ms，其余实际数据保持。
F只展示用户指定的B先行模型模板与同一次早期未滤波功率增量，A模板保留在metadata中。E绘图区为正方形，色条与绘图区等高；参数与色条均为log轴，色条以10的幂标注；灰格为计算未完成，斜线为300秒观察窗内有右删失。
**关注点**：状态切片不等于整个事件，F的ΔP包含电流均值升高，E是有限时间首次进入图，候选待人工检查。
