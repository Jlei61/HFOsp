# results/topic4_sef_hfo/observation_layer/figures

### increment1_gate.png

增量 1「已知方向玩具波合同门」结果。左图：对 0/30/60/90/135° 五个已知传播方向的合成
平滑行波，虚拟电极读出的通道先后顺序与真方向投影顺序的 Spearman（虚线 = 通过线 0.9）。
右图：endpoint-centroid 轴与真方向的夹角误差（虚线 = 25° 容差）。两图都越过线 = 观测
层能把已知方向干净读回来。负对照（C1 居中径向 = 无轴、C2 同起同落幅度差 = 无假序）的
裁决在 `../increment1_toywave/gate_verdict.json`。

**关注点**：五个 wave 必须全部 Spearman ≥ 0.9 且角误差 < 25°；任何一个不过 = 观测层
被几何污染，停下来调链路（绝不放宽阈值）。

---

## 双电极读出（论文级模型图，一模型一张，不画总图）

同一套虚拟电极 + 同一个画图函数（`src/sef_hfo_plot.py::two_electrode_readout`）画两遍：
一根电极平行于 E→E 连接长轴、一根垂直；两个模型都把事件种子放在一端，事件单向传过去。
每张图三栏：**A** 几何（事件 + 两根电极 + 种子）；**B** 平行电极每个触点录到的信号波形（按位置叠放）；
**C** 垂直电极的波形。核心读法看 B/C 里把各触点波峰连起来的黑虚线（"波峰轨迹"）：平行电极
**斜着走**（波依次扫过各触点 = 读出了方向），垂直电极**竖直**（各触点几乎同时到 = 读不出方向）。

三处口径约定（2026-06-08 用户校正）：① B/C 灰带 = **这根电极把事件录全的时间跨度**（第一个到最后一个
参与触点的波峰），不是"场事件窗"——场事件窗（整片像素过阈的比例）比波扫过整根电极的时间**窄**，
若把波峰限制在场窗内，最先/最后被扫到的触点会被钉在窗边，斜线尾巴变假；改用真实到达后斜线全真。
② A 的 colorbar 是**相对时间**（0 = 踢了之后第一个激活），不是绝对仿真时刻。③ A 的电极标注：率场触点稀疏
（4mm 间距）逐个标；SNN 触点密（亚毫米、两杆中心触点重合），只标每根杆的两端（P0/P8、Q0/Q8），
中间触点按杆上顺序对应 B/C 的波形叠放顺序。连接轴只画箭头、不写"connectivity axis"文字。

### two_electrode_readout_rate.png

率场（连续场、24mm、真实 4mm 触点间距）。A：每像素**激活后相对时间**（颜色，紫早黄晚）——E→E 兴奋
连接沿 45° 长轴的传播范围是垂直向的 2 倍（各向同性短程抑制），所以激活被漏斗到这条长轴、成一条
对角带（"只有长轴被点亮"=连接长轴被显形，是机制本身不是 bug）；两根电极叠加（实心 = 录到事件、
空心 = 没被波扫到）。B：平行电极各触点录到的发放率密度波形叠放，波峰轨迹斜着走 = 行波依次扫过
（P0 在种子端最先 ~19ms、P5 在远端最后 ~145ms；内部 P1→P3 是 33→70→105ms 的干净扫过）。
C：垂直电极各触点波峰对齐成竖线 = 同时到、读不出方向。率场是均场量，所以这里的"信号"是
**发放率密度，不是电流型 LFP**（图中已标 NOT a current）。

**关注点**：B 的波峰黑虚线是否斜、C 是否竖；波峰是否落在灰带内部（不再钉在边缘）；A 里电极是否压在
传播带上、colorbar 是否相对时间；信号标注是否写明"非电流"。

### two_electrode_readout_snn.png

脉冲网络（3mm 小片，会点火；触点按片缩放到亚毫米）。A：每个神经元**发放后相对时间**（颜色），
两根电极只标四个杆端（P0/P8、Q0/Q8；两杆中心触点重合，全标会糊），红星 = 种子。B/C：用**正式电流型
LFP**前向模型（|I_E|+|I_I|，在 ~2mm 小尺度验证过）录每个触点随时间的电信号。平行电极波峰轨迹斜着走
（峰时随位置单调移动 ~4ms = 行波扫过：**离种子最近的 P8 最先峰（184.8ms），最远的 P0 最后峰
（188.7ms）**——用户问的"O8 最早"=标注糊了误读的 P8，因为它最贴种子），垂直电极同跨度的触点波峰
几乎对齐（跨度 ~0.5ms = 同时到）。平行 ~4ms vs 垂直 ~0.5ms 的 8 倍反差才是"读出方向"的证据，不是
单看 P8 近。（幅度反而非单调：最近的 P8 峰最小，时序才是干净的读出。）

**关注点**：B 斜 / C 竖的对比、∥ 与 ⊥ 峰时跨度的反差；电信号是**电流型 LFP**（不是发放率密度）；
colorbar 是相对时间；杆端标注是否清晰；触点亚毫米、**非真实 SEEG 间距**（机制示意）；事件在片中心点火、
电极放在朝 [0,0] 的那条单向传播臂上；真实 4mm 尺度的定量读出由 rate 图承担，SNN 在 cm 尺度仍点不着。

### liou_bolton_lifecycle.png / liou_bolton_ablation.png / liou_bolton_threshold.png（v2，当前版本）

将 Liou-Abbott eLife 2020 的机制骨架重新实现，修正了 v1 的两个结构缺陷（1）局部/全局抑制混在同一个高斯核里——v2 拆成：**局部 I 核**（宽 σ_L=1.5mm > σ_E=0.54mm，形成 Mexican-hat）+ **慢全局抑制池 G**（标量，按时间常数 τ_G 积累总活动均值）；（2）抑制效能 z 只有空间均值——v2 保存全空间 z(x,t) 场，可显示局部/全域的耗竭梯度。**全部是"可激区"能力探索（w_ee_mult≈2），不是数据锁定的 SEF-HFO 工作点。**

当前版本的诚实边界：空间域是1-D 周期边界（FFT 卷积），刺激覆盖前12%，整域在几百毫秒内全部激活——因此目前 wavefront_speed=0（无波前检测；波前扩张需要非周期域 + 亚阈外围组织）。z 场在 τ_z=100s 时间常数下 14s 内只有 ~13% 耗竭，空间梯度弱但可见。下一结构步骤：有限域 + 亚阈外围 + SOZ 斑块。

- `liou_bolton_lifecycle.png` — 全模型（全部机制）的4个空间场随时间演化（行为率 / 阈值 / 抑制效能 / sAHP 电流）+ 三个空间位置的单点发放率(t)。可激区中 w_ee_mult=2.0，周期≈340ms 的爆发节律；z 场显示全域缓慢均匀耗竭（空间梯度后续步骤改进）。
- `liou_bolton_ablation.png` — 四条件消融：**去掉动态阈值（dphi=0）→ 退化成 372Hz 均匀强直（确认动态阈值是爆发的必要驱动）**；去掉 sAHP → 仍爆发、周期几乎不变（次要）；去掉 Mexican-hat surround（σ_I=σ_E）→ 仍爆发但**周期 830ms（2.4×更慢）**，说明宽抑制核加速了爆发节律。
- `liou_bolton_threshold.png` — 刺激强度阈值扫描（w_ee_mult=1.7，近阈值可激区）：amp=0,1 → 单次短暂事件（**间期样，自终止**）；amp≥2 → 5~7 次多发爆发（**发作样，自终止**）。两侧各一张空间栅图对比。

**关注点**：ablation 图是核心——**动态阈值是必要机制**，surround 塑形节律；threshold 图揭示了从**间期到发作的连续谱**，两者形态不同但均自终止（sAHP 积累是自终止机制）；所有标注均注明"可激区能力探索、非锁定工作点"。

### liou_bolton_main.png / liou_bolton_sweep.png（v1，历史参考）

v1 版本用单个高斯核（l_inh=1.6）同时承担局部和全局抑制，缺乏机制拆分；sweep 图探索了参数空间（Δφ × w_ee_mult 相图 + τφ vs 周期）。爆发只活在"够兴奋 × 够强阈值适应"的对角带里，周期随 τφ 单调增长。v2 取代 v1 作为主要参考。

### liou_bolton_chloride.png（历史参考）

eq 8（抑制效能 z 全域快速耗竭，τ_z=5s）：z→0 去抑制 → clonic 节律崩成强直 tonic（发作加重）。v2 的 z 机制已整合到新模型（τ_z=100s 慢耗竭版）。
