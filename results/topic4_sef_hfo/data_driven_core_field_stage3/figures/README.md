# Stage 3 图说明

## 复现入口（逐字可粘贴）

```bash
# Leg A 位置扫描（49 个位置 × 4 张网络，无优化器）
python scripts/run_topic4_core_field_stage3_sweep.py --workers 10 --sigma 1.2
python scripts/analyze_topic4_core_field_stage3_sweep.py
python scripts/run_topic4_core_field_stage3_confirm.py --workers 6
python scripts/plot_topic4_core_field_stage3_sweep.py

# 逐事件目标函数：先给已有的场打分，再拟合灵活场
python scripts/run_topic4_core_field_stage3_profile_round1.py
python scripts/run_topic4_core_field_stage3_fit.py --workers 10 --seeds-per-candidate 2 --max-gens 10
# 最优场跑一次仿真（seed 5，与已发表的手放版同一张网络）
python scripts/run_sef_hfo_subject_snn.py --subject epilepsiae_1146 --montage narrow \
  --placement gradient_shared --core-r 1.5 --seed 5 --T 8000 --k-dir 2 \
  --stage3-field results/topic4_sef_hfo/data_driven_core_field_stage3/fit/confirmation_K3_r0.json \
  --tag epilepsiae_1146_stage3_flexible_field_s5
python scripts/plot_topic4_core_field_stage3_flexible.py
```

---

### stage3_flexible_field

回答：**把病理场从"必须沿轴铺"里解放出来、让它在整张片子上自由定位和变形之后，
它落在哪、跟病人有多接近、跑出来的事件长什么样。**

**这一版换掉了打分方式。** 旧的做法是把每个事件压成一个方向正负号、再在同号里求平均得到两条模板 ——
V 形顶点的位置在这一步被抹掉，而那正是区分"源在中段"和"源在一头"的信息。
新做法是**逐事件**取一个无量纲的量：把"名次 vs 触点在轴上的位置"拟合一条直线，
取 `符号 × 拟合优度`（−1 = 反向单调，0 = 中段 V 形，+1 = 正向单调），
比较模型和病人**这个量的整体分布**。病人侧用的是全部 **46,682 个事件**，不是两条平均模板。

四列：

- **distance to the patient**——同一把尺子画的两样东西。底图：把一块固定大小的异质性放在
  49 个位置，每处到病人的距离（浅=近）。红色轮廓：**学出来的灵活场自己的等值区**。
  灰格是那里事件太少、算不出分布。**关注点：学出来的场压在"固定块放这里最接近病人"的那条亮带上。**
- **model forward / model reverse**——同一次跑里代表性的正反事件。点云颜色是相对点火先后
  （深紫=最早，黄=最晚），红带 = 学出来的场本身，白星 = 该事件实际起火点（最早点火那 1% 细胞的质心）。
  **这两格没有任何手放核的标记 —— 这次跑根本没有手放核。**
- **model virtual-SEEG**——**同一次跑**的连续 30–80 Hz 虚拟 SEEG，不是病人真实记录。

## 数字

| | 到病人的距离 |
|---|---|
| 病人自己两半（噪声地板） | **0.031** |
| **灵活场（6 张独立网络确认）** | **0.400** |
| **灵活场（留出的病人记录段）** | **0.425** |
| 死板家族最好（单块） | 0.591 |
| Stage 2 的贯通细丝 | 0.686 |
| 手放两个核 | 0.855 |

拟合过程中最好是 0.341，在**没有参与挑选它的 6 张网络**上重算是 0.400
（winner's curse +0.058，正是评估噪声预测的量级）。留出的病人记录段 0.425 跟训练段贴得很近 ⇒
**病人这一侧没有过拟合**。

学出来的场是**两个分量**（第三个权重被压到 0.04）：一条 4.4 × 0.5 mm 的细带在 (7.1, 6.4)，
加一个 0.4 × 0.7 mm 的紧凑点在 (15.4, 4.1)。后者离手放的"汇"核只有 1.1 mm。

**关注点**：把场解放开之后，到病人的距离从手放双核的 0.855 走到 0.400 ——
**把到噪声地板的距离缩掉了一半以上**。这是"当前 SNN 设计下网络有能力表示病人传播倾向"的充分性证据。

## ⚠️ 三条不能夸大的

**① 这不是"匹配上了"。** 0.400 仍是噪声地板（0.031）的 13 倍。模型还产生不出病人那种事件形状的分布。

**② 优化没有收敛，0.400 不是这个家族的上限。** sigma 从第 5 代起在**放大**（0.783 → 0.860），
种群中位数在 0.51 和 1.20 之间来回摆，候选反复漂回"完全不出事件"的死区。
**0.400 是 100 个候选达到的水平，不是这个家族能达到的水平。**

**③ ⚠️⚠️ 两个方向不是镜像。** 同一 seed 上的对照：

| | 事件 | 干净 | 正/反 | 两方向相关（组织层 / 触点层） |
|---|---|---|---|---|
| 手放两个核 | 34 | 5 | 1/4 | **−0.748 / −0.685** |
| Stage 2 细丝 | 5 | 5 | 2/3 | **−0.863 / −0.755** |
| **Stage 3 灵活场** | 20 | **18** | 14/4 | **+0.700 / +0.651** |

灵活场的正负号配比（14:4）确实接近病人（73%:27%），**但标成正反的那两批事件是同一套模式**，
不是两个相反的传播 —— 正相关。病人自己的两套模板是**负相关（−0.464）**，模型没有复现这一点。

**原因很直接**：新目标函数只比"带符号单调度的**边缘分布**"。一个坐在阵列中段的生成器，
靠噪声决定哪个触点先响，就能产生两个符号 —— 边缘分布对得上，但那不是两个生成器。
⇒ **匹配边缘分布不够，还要匹配"事件会聚成两个彼此相反的簇"这个联合结构。**
这条**不能**靠把验收门塞进目标函数来解决（那会让通过失去证据力），
需要一个描述联合结构的观测量。

**禁止**写成"数据驱动的场复现了双向 repertoire" —— 两个方向正相关。
能写的是"逐事件剖面形状的分布上明显更接近病人，但两个方向不构成相反的两套模式"。
