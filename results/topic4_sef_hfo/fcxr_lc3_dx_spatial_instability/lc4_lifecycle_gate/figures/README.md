### lc4_functional_baseline_gate.png

这张四联诊断图只展示实际完成的 F0 配对基线实验：A 是同一底座和噪声下的群体活动，B 是四项间期统计相对 control 的比值，C 是执行器实际泄漏电流，D 是逐条硬门。n=6 和 n=8 都数值安全且泄漏远低于上限，但 n=6 把事件变少并增大 IEI 变异，n=8 又把事件变多，因此锁定家族没有合格候选；F1 onset surface 和 F2 lifecycle 均未获授权、没有作图。

**关注点**：绿色泄漏结果不能替代功能基线；真正阻断是事件统计越界，而不是数值爆炸或高态误触发。
