### stage0d_local_basin_replication_v1_1.png

A 显示冻结 phase source 与四个选定相位。B 明确显示 `none passed locked gate`：中心点没有任何轨迹通过最终锁定 dt/2 gate，未用邻点或较早阶段轨迹替代。 C 显示 3×3 邻域每点的 off-orbit final survivor 数；D 汇总全部 180 条历史的最终锁定状态。

**关注点**：Figure B 只接受中心点 `final_status=candidate_survives` 的 dt/2 轨迹；空面板文本是科学结果，不是缺图。
