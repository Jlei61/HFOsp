### dual_core_node_pathway_factorization.png
冻结双 core Node 后，在相同 12 张网络上比较 Node、+EE、+E-to-I 和 +EE+EI。灰线连接同一网络，彩色点为 equal-network mean，误差条为 90% network bootstrap；星号只表示相对 Node 的配对区间排除 0。

**关注点**：OOD 是主端点；模式占比和 natural KMeans 只用于判断连接重分配改变了双模式可达性还是仅改变事件产率。
