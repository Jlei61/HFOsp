# H2b（间期状态 → 发作转移）技术报告

状态：**development；两条主任务结论不同——B2 可估计且未见增量，B1 在当前估计量下不可估计**
日期：2026-09-02　分支：`codex/topic5-group-event-state-v02-b`（base `f0c9e075`）
registry：`group_event_state_v0_2_registry_1`

---

## 0. 一句话

在只用间期群体事件学到的状态上，**没有看到它比"这位病人的发作平时长什么样"这个静态模板
更能指出下一次发作的早期空间场**（12–13 位病人、61–81 个留出发作事件，各提前量中位增量
落在 −0.006 ~ +0.002，符号检验均不显著）。**离发作还有多久这一条在当前估计量下不可估计**
（133 格里只有 11 格通过"不得差于纯截距"的闸门）。

---

## 1. 分母（B0）

| 项 | 数 |
|---|---:|
| 匹配上的发作（逐 onset 零误差） | 274 |
| 归并成簇后的独立事件 | **209** |
| 留出事件（每人前 ⌈n/2⌉ 训练） | **99** |
| 有锚点：5min / 30min / 2h / 6h | 98 / 89 / 79 / 75 |
| 发作能量场成功 / 丢弃 | 264 / 10（10 条全为窗口越出块边界） |

- crosswalk 按录音编号连接并逐 onset 核对：**零** onset 落在自己录音之外、零歧义、零重复。
- 5 位 Yuquan 病人 0 条发作记录 = **未检出**，非无发作（v0.1 数据合同 §11）。
- 成簇归并的依据：一位病人 8 次"发作"落在同一块内 3.6 min 内、时长全 0 → 一次被切成八条。
- 不应期敏感性 30/60/120 min → 留出 106/99/80；**60 min 主口径不在悬崖上**（2h 档最敏感）。
- 锚点新鲜度：上一次间期事件中位早 5–14 s，几乎无超 1 h → 新鲜度不是瓶颈。

## 2. 目标正确性（B0.3）

发作能量场按**脑电起点**锚定（非临床起点、非缓存 `relt=0`；topic5 caveat 9）。

对拍已验收缓存（168 次发作 / 11 人）：

| 比较 | 中位 ρ |
|---|---:|
| 同锚点重切（重建是否忠实） | **+0.9977** |
| 缓存原样发布口径（临床锚） | +0.8655 |

- 通道顺序**处处一致**。
- 脑电起点在 **145/168** 次发作里早于临床起点，中位 **5.0 s**、p90 40.8 s、**最大 86.2 s**。
- ⚠️ **未结清**：8 次（全在 `epilepsiae_922`）同锚点对拍 < 0.8。已排除信号强弱、电极数、
  基线长度、时间平移四种解释；同病人对照发作在零平移处仍达 +0.996（机器本身对齐无误）。
- 坐标：Epilepsiae 215 次全 `mni152_1mm`；3 位 Yuquan 病人无坐标（13 次发作）→ 偏侧性不可算。
- `block_id` 修复 2 条（指向比自己发作早 ~14 h 结束的块），各救回 1 次发作。

## 3. 预注册的判读基准（在读任何模型之前定死）

**静态"病人平均场"基线**（不含状态）预测留出发作头 5 s：**中位 ρ=+0.41，最高 +0.93**。
这是状态必须越过的线。

单次发作自身的可复现度中位 +0.30（6/12 人 < +0.30）。**它不是上限**——
平均能消掉单次噪声，静态基线在 9/12 人身上反超它。（此处曾把它写成"上限"，已在交付物内更正。）

## 4. B2：发作早期空间场（承重终点）

估计量：用该病人**自己**训练侧发作的场做加权平均；权重 = 状态余弦相似度的 softmax。
两臂**同一批场**，只差权重是否由状态给；均匀权重 = 静态基线，是状态臂温度→∞ 的严格特例。
温度按 **TRAIN 内留一**选定后冻结。

按病人先合并种子，再跨病人取中位：

| producer | lead | 病人 | 中位增量 | 正向 | 符号 p | 留一中位范围 | 留出事件 |
|---|---|---:|---:|---:|---:|---|---:|
| `P_local` | 5min | 13 | +0.0013 | 7 | 1.000 | [+0.0006,+0.0019] | 81 |
| `P_local` | 30min | 13 | −0.0058 | 5 | 0.581 | [−0.0073,−0.0049] | 73 |
| `P_local` | 2h | 12 | −0.0003 | 6 | 1.000 | [−0.0014,+0.0007] | 65 |
| `P_local` | 6h | 12 | −0.0014 | 5 | 0.774 | [−0.0015,−0.0014] | 61 |
| `P_slow` | 5min | 13 | −0.0017 | 5 | 0.581 | [−0.0037,−0.0013] | 81 |
| `P_slow` | 30min | 13 | −0.0026 | 5 | 0.581 | [−0.0027,−0.0014] | 73 |
| `P_slow` | 2h | 12 | −0.0027 | 5 | 0.774 | [−0.0035,−0.0019] | 65 |
| `P_slow` | 6h | 12 | −0.0003 | 5 | 0.774 | [−0.0005,+0.0000] | 61 |
| `P_memoryless` | 5min | 13 | −0.0013 | 6 | 1.000 | [−0.0016,−0.0005] | 81 |
| `P_memoryless` | 30min | 13 | +0.0005 | 7 | 1.000 | [−0.0024,+0.0009] | 73 |
| `P_memoryless` | 2h | 12 | +0.0019 | 7 | 0.774 | [+0.0007,+0.0031] | 65 |
| `P_memoryless` | 6h | 12 | −0.0006 | 5 | 0.774 | [−0.0010,−0.0001] | 61 |

格状态：**350 可估计** / 105 训练侧锚点不足 / 77 无可打分留出。

要点：

1. 所有中位增量的量级是 **10⁻³**，而基线本身在 0.4 量级——即增量约为基线的 **1%**。
2. 符号检验**无一显著**（最小 p=0.581），留一中位范围**全部窄且贴零**。
3. **无记忆对照 `P_memoryless` 与两个带记忆的 producer 表现一致**——这条最有信息：
   如果递归状态真带来跨任务信息，它应当与无记忆对照分开；实测分不开。

## 5. B1：离发作还有多久

**当前估计量下不可估计。** 133 格中：

| 状态 | 格数 |
|---|---:|
| `unusable_fit_worse_than_intercept` | **94** |
| `ok_but_no_events_in_eval` | 21 |
| `insufficient_rows` | 7 |
| `ok` | **11** |

19 位病人里 **15 位零可用格**；11 个可用格中 6 个来自同一位病人（`epilepsiae_916`）。
按工程附录「远坏于截距基线的拟合标为不可估计」，这些格**不得**读成"状态有害"。

已做的三处 TRAIN-only 修正（标准化、岭系数按 TRAIN 内时序 CV、状态维度同法选）
把每行 1–2 nat 的外推压到 10⁻² 量级，但仍不足以让多数病人越过截距闸门。
**结论：这是仪器问题，不是生物学结论。**

## 6. 缺口

- **`B_multiscale` 整条 arm 缺失**：它 27 人齐、`complete`，但只存分数、不存 111 维逐时刻特征。
  按合同记 `not_available`，**未**用本线的时钟基线顶替（那是 spec §3 第 1 条，另一条 arm）。
  → **无法回答"增量是否超过一条认真的可解释基线"**（issue 已提）。
- `epilepsiae_922` 的 8 次对拍偏低未结清。
- B1 估计量需重做（更强收缩 / 状态摘要而非主成分 / 每病人分层）。

## 7. 允许与禁止的措辞

允许：

- 「在**用状态挑选该病人自己哪一型发作**这个读出方式下、在 12–13 人 / 61–81 个留出事件的精度内，
  **没有看到**间期状态超过静态病人平均场的增量；无记忆对照给出同样的答案。」
- 「离发作还有多久这一条在当前估计量下**不可估计**。」

禁止：

- ❌「间期状态与发作无关 / 不含发作信息」——本轮只否证了一种读出方式下的一个增量。
- ❌「H2b 阴性」——B1 不可估计、`B_multiscale` 对照缺失，两条主任务未同时判决。
- ❌ 把 B1 那 94 格的负增量写成"状态有害"。
- ❌ 任何临床部署性能的说法；也不证明间期放电导致发作。

## 8. 工件

`support/`（8 张表）、`figures/`（起点对齐质检图 PNG+PDF+metadata+中文 README）、
`machine/`（133 格逐格 JSON + `cohort_summary.json`）、`CURRENT_HANDOFF.md`。
测试 98 项全过；改动纯增量（74 新增文件、0 既有文件修改）。
