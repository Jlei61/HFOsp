# Topic 5.2D v0.2 白话报告：把顺序挤进一个很小的状态之后，还剩下什么

> 日期：2026-08-18　|　结果根：`results/topic5_capacity_constrained_history_motif_v0_2/`
> 本文只用日常语言讲清楚测了什么、怎么测的、看见了什么；内部代号一律放在括号里。

## 0. 一句话

我们把「一次间期事件里触点被点亮的先后顺序」这件事，强行压缩成**只有几个数**的状态，再看这几个数能不能预测这次事件接下来会点亮哪些触点；并且事先堵死了两条不看顺序的抄近路，让任何优势都必须**在这两条抄近路之外**。

**结果是：那个低维状态确实带来了额外的预测力；但「先后顺序本身是否携带信息」这一问，本轮的对照给出的是零，而不是正。**

### 本轮判决（逐条，不可跨条外推）

| 命题 | 判决 |
|---|---|
| 低维有序前缀分支带来额外预测力 | **SUPPORTED**（n=28，中位 +0.0102，区间不跨零，含重训不确定度） |
| 该增益来自**先后顺序**本身 | **NOT ESTABLISHED**（同字典同容量的不看顺序对照打平，区间跨零） |
| 存在一个**共享动力学算子** | **NOT ESTABLISHED**（与逐步独立读出无可测差别；本轮未比较两者绝对精度） |
| 存在**患者特异传播方向** | **UNINFORMATIVE**（合成校准显示本设计对已知强轴向结构无检出力） |
| 原始的传播机制问题（终点、范围、两种模板、模板稳定而细节随机） | **OPEN**（本轮未测） |

## 1. 我们限制了哪些绕行路径？

一次事件里，触点是一批一批亮起来的。要预测「接下来亮谁」，其实有两类完全不同的线索：

- **不看顺序也能用的线索**：从哪儿开始、已经走了多远、以及「到目前为止都点亮过谁」。  这类线索完全不需要知道先后。
- **必须看顺序才有的线索**：先亮 A 再亮 B，和先亮 B 再亮 A，是不是会导向不同的后续。

我们先把第一类线索**全部交给一个专门的对照模型吃掉**，而且做了两档强弱：弱的那档只知道起点和进度；强的那档还额外知道到目前为止点亮过的全部触点集合。这两档模型被证明对「把中间几批的先后打乱」完全无感——输出逐位不变（内部：`U_MINIMAL` / `U_FULL_SET`，顺序不变性审计逐位通过；故意注入一个偷看顺序的 bug 后审计确实报错，证明这道检查不是摆设）。

有序模型只被允许在这两档之上**再加一点东西**，而且那一点东西必须先穿过一个只有几个数的瓶颈。没有自由的触点到触点转移表，没有第二条偷读顺序的网络，也没有给某个空间图案开小灶的偏置。

## 2. 低维模型学得会吗？

把最自由的那版低维模型（内部：`H1_FREE_LOW_RANK`，同一个算子一步步推）和「只有不看顺序的强抄近路」相比：28 位患者，中位 +0.0102（95% 区间 +0.0045 到 +0.0373，不跨零），正/负/近零 = 24/2/2，方向偏正。

**这个数字该怎么念**：它是「自由低维**有序前缀分支**相对那条选定的冻结无序基线的增益」，**不是**「有序历史的增益」。因为在这个自由字典上我们**没有**同时训一个「同样自由、但从来不看顺序」的模型，所以「读顺序」和「学到一套自己的空间图案」这两份贡献在这个数字里没有分开。能把这两份分开的对照只存在于患者对齐字典上（下一段），而它的结论是零。

**可以排除的一种解释是容量**：全模型参数量上，有序模型是几十到一百多个（自主 53、直接 153），被它击败的冻结无序基线是 1383 个。所以这个增益不可能来自「参数更多、表示更灵活」——方向正好相反。

另外一个直接问「是不是只要空间图案、不要顺序就够了」的对照：用**完全相同的空间图案**、但状态只由「点亮过谁」这个集合生成、完全不读先后（内部：`H1_ALIGNED_ORDERLESS_BAG`）。它和读顺序的同伴相比：26 位患者，中位 -0.0010（95% 区间 -0.0015 到 +0.0007，跨过零），正/负/近零 = 7/13/6，方向偏负。

## 3. 训练完的模型，真的在用顺序吗？

这里问两个**不同**的问题，很容易被合并，但它们不是一回事：

1. **把观察到的前缀中间两批调换先后**（起点、点亮过谁、走了几步、每批几个触点全都不变）。　→ 26 位患者，中位 +0.0004（95% 区间 +0.0001 到 +0.0015，不跨零），正/负/近零 = 10/1/15，方向偏正
2. **直接把那几个数清零**，只留下不看顺序的抄近路。　→ 26 位患者，中位 +0.0030（95% 区间 +0.0005 到 +0.0099，不跨零），正/负/近零 = 15/0/11，方向偏正；同一个操作作用在完全自由的低维模型上是 28 位患者，中位 +0.0048（95% 区间 +0.0015 到 +0.0323，不跨零），正/负/近零 = 21/1/6，方向偏正

两项都为正，但**这两项都是同一个训练好的模型的内部性质，不能推出「顺序带着信息」**：

- 能说的是：这个模型**不是**对输入顺序无所谓的——把中间两批调换先后，它的输出会变差，而且这个代价在换种子重训后依然为正。
- **不能**说的是：顺序里有额外的可预测信息。判这一条的是另一个对照——同一套空间图案、参数量相当、但**从来不看顺序**的模型，成绩与读顺序的同伴打平（见上一节，区间跨零）。一个模型内部用了顺序，和顺序是否携带信息，是两件事。
- 量级也要一起看：调换先后的代价比整条通路的贡献小约一个数量级。

因此本节的结论是「低维状态在被使用」，**不是**「用的就是先后本身」。

判读规则（写在看结果之前）：这两项都只能证明**模型内部**是否依赖顺序。「顺序是否携带额外可预测信息」只能由同字典、同容量、不看顺序的对照回答，而那个对照的区间跨零。两者不可互相替代。

### 两个家族之间那个差值，念的时候要小心两件事

第一，它比的是**两个家族各自的「方向轴带来多少好处」之差**，**不是**两个家族谁预测得更准。后者本轮没有测。

第二，两个家族各自结算在自己的目标上（自主家族算它能自己推出来的五步场，直接家族算它独立读出的整段后缀）——这是冻结设计要求，必须分开。但两个家族**之间**做差时，两边的第二项就落在不同目标上、尺度不同。因此这里同时给两版：按各自目标是 24 位患者，中位 -0.0008（95% 区间 -0.0015 到 +0.0007，跨过零），正/负/近零 = 6/12/6，方向偏负；把两边都换成同一个目标（五步场）重算是 24 位患者，中位 +0.0002（95% 区间 -0.0015 到 +0.0010，跨过零），正/负/近零 = 7/11/6，方向偏正。两版都跨零，判读一致——但换成同一把尺子后中位数的符号会翻，所以引用时请用共同目标那一版。

## 4. 未来只是能被直接解出来，还是能由同一个算子自己往前推？

这两件事我们从头到尾分开训练、分开评分，从不混写：

- **每个未来步各配一个独立读数**（内部：`DIRECT_HORIZON_UPPER_BOUND`）：这只回答「未来最多能被解出多少」，永远不能支持共享动力学的说法。对比把方向转掉的同容量对照：24 位患者，中位 +0.0002（95% 区间 -0.0005 到 +0.0008，跨过零），正/负/近零 = 7/4/13，方向偏正
- **同一个算子一步步往前推**（内部：`AUTONOMOUS_SHARED_OPERATOR`）：所有未来步共用一个状态推进算子和一个触点读数。对比同一个方向旋转对照：24 位患者，中位 +0.0004（95% 区间 -0.0002 到 +0.0017，跨过零），正/负/近零 = 9/6/9，方向偏正

只有下面这一条成立时，才允许写「同一个低维共享算子能生成多步未来」。

## 5. 患者对齐的空间图案，比植入几何、电极杆方向和错位方向更省吗？

- 对比**只看植入几何**：26 位患者，中位 -0.0022（95% 区间 -0.0135 到 +0.0006，跨过零），正/负/近零 = 7/16/3，方向偏负
- 对比**只看电极杆方向**：26 位患者，中位 -0.0005（95% 区间 -0.0058 到 +0.0038，跨过零），正/负/近零 = 11/12/3，方向偏负
- 对比**把方向转掉**：24 位患者，中位 +0.0004（95% 区间 -0.0002 到 +0.0017，跨过零），正/负/近零 = 9/6/9，方向偏正
- 对比**把触点身份错位**：26 位患者，中位 +0.0016（95% 区间 -0.0002 到 +0.0044，跨过零），正/负/近零 = 14/6/6，方向偏正
- 对比**保留局部性但重连**：26 位患者，中位 -0.0002（95% 区间 -0.0013 到 +0.0011，跨过零），正/负/近零 = 9/8/9，方向偏负

两点必须一起读，否则会误判：

- 只有「把方向转掉 / 把触点身份错位 / 保留局部性但重连」这三个对照与患者对齐是**严格同型**的（同一个局部核、同样的各向异性强度、同样先扣掉常数场和电极杆指示、同样的参数量）。
- 「只看植入几何」和「只看电极杆方向」**没有**扣掉常数场和电极杆指示，所以它们能表示的东西天然更多；患者对齐赢不了它们，不代表方向没用，只说明这道题设得更严。

### 判读前必须先看这个：效应比「同一条臂重跑一次」的漂移还小

我们把**完全相同**的那条患者对齐臂，只换随机种子重训了 3 次，再各自和同一批方向对照比。结果是：光是换个种子，测出来的「结构优势」就会在 **0.0055**（九成分位 0.0263）的范围里晃动（24 位患者）。而效应本身的中位只有 **+0.0004**。

**这意味着**：任何**单个患者**的结构优势数字在这个尺度上都不可解读——它主要反映的是优化器落在了哪个局部解，而不是这位患者的解剖。能读的只有**跨患者的配对中位数及其自助置信区间**。

**注意主区间的口径**：本文每条效应的 `median_ci95` 是先把每位患者的多次重训取中位、再只重采样患者算出来的，因此它反映的是**换一批患者**的不确定度，并**没有**把上面这份重训噪声算进去。为此每条承重效应额外算了一版**同时重采样患者和重训**的区间（证据矩阵里的 `*_seed_aware` 条目，字段 `median_ci95_seed_aware`）。逐条对照的结果见下表：

| 效应 | n | 中位 | 仅重采样患者 | 同时重采样患者+重训 | 结论是否改变 |
|---|---:|---:|---|---|---|
| 自由低维有序分支 − 冻结无序基线 | 28 | +0.01016 | [+0.00454, +0.03734] 不跨零 | [+0.00404, +0.03985] 不跨零 | 不变 |
| 同字典: 无序 bag − 有序（正=顺序有用） | 26 | -0.00098 | [-0.00154, +0.00075] 跨零 | [-0.00158, +0.00117] 跨零 | 不变 |
| 对齐 − 角度旋转对照（自主） | 24 | +0.00039 | [-0.00025, +0.00175] 跨零 | [-0.00092, +0.00185] 跨零 | 不变 |
| 调换前缀先后的代价（对齐） | 26 | +0.00038 | [+0.00005, +0.00155] 不跨零 | [+0.00005, +0.00137] 不跨零 | 不变 |
| 调换前缀先后的代价（自由） | 28 | +0.00030 | [+0.00011, +0.00108] 不跨零 | [+0.00009, +0.00096] 不跨零 | 不变 |
| 关掉低维状态的代价（对齐） | 26 | +0.00303 | [+0.00053, +0.00989] 不跨零 | [+0.00050, +0.00989] 不跨零 | 不变 |
| 关掉低维状态的代价（自由） | 28 | +0.00476 | [+0.00148, +0.03228] 不跨零 | [+0.00128, +0.03585] 不跨零 | 不变 |

**0 条**结论因为加入重训不确定度而改变。换句话说：这是一个真实的口径缺陷，但修正它不改变本轮任何判读。

（作为对照，单条臂验证目标本身的漂移是中位 0.0043、九成分位 0.0236，覆盖 218 条多种子臂。）

### 判读前必须先看的第二件事：那条「患者训练轴」有多少只是植入形状

所谓「按患者训练序列对齐」的方向，是这样定出来的：对每个训练事件，量一下「起点在哪」到「事件后段重心在哪」的位移，再取所有位移**外积的主特征方向**。这个统计量找的是位移**散布最大**的方向——如果一位患者的电极本身就排成狭长一片，那么散布最大的方向就是电极云自己的长轴，跟有没有东西沿它传播无关。

实测：28 位患者里，这条训练轴与**该患者电极云长轴**的夹角中位只有 **7.7°**，23/28 位在 20° 以内（与主要电极杆方向的夹角中位是 28.4°，12/28 位在 20° 以内）。而且植入越狭长，两者越重合（长宽比与夹角的秩相关 -0.87）；这条轴在只用 25% 训练事件时也几乎不变（中位差 0.26°），说明它是被几何定死的，不是采样噪声。

**这意味着**：下面「患者对齐 vs 把方向转掉」的比较，很大程度上是「植入的长轴」对「把它转掉之后的方向」的比较。方向对照本身是匹配的（同一个局部核、同样的各向异性强度、同样的秩和参数量，旋转/对齐的主奇异值比中位 1.00），所以比较是公平的；但**它不等于「患者真实的传播方向 vs 错误方向」**。

### 训练之前先问：这些空间图案在**表示层面**够用吗？

在训练任何模型之前，可以只问一个纯几何问题：如果允许每个留出事件单独挑最合适的系数，这套空间图案最多能张开多少留出残差？（这是天花板，不是能部署的预测器。）

- 对比 把方向转掉（严格同型）：14 位患者，中位 -0.0007（95% 区间 -0.0051 到 +0.0012，跨过零），正/负/近零 = 4/6/4，方向偏负
- 对比 只看植入几何（非同型，对患者对齐更严）：14 位患者，中位 -0.0211（95% 区间 -0.0828 到 -0.0078，不跨零），正/负/近零 = 2/12/0，方向偏负
- 对比 把触点身份错位（严格同型）：14 位患者，中位 +0.0277（95% 区间 +0.0045 到 +0.0415，不跨零），正/负/近零 = 10/2/2，方向偏正
- 对比 只看电极杆方向（非同型，对患者对齐更严）：14 位患者，中位 +0.0007（95% 区间 -0.0323 到 +0.0226，跨过零），正/负/近零 = 7/7/0，方向偏正
- 对比 TRAIN_ONLY_FREE_PCA（非同型，对患者对齐更严）：14 位患者，中位 -0.1203（95% 区间 -0.1474 到 -0.0987，不跨零），正/负/近零 = 0/14/0，方向偏负

只有候选触点数至少是状态维数两倍的患者进入这一层判读；其余患者候选太少，任何低维图案都能张满，天花板是平凡的。

## 6. 把不看顺序的抄近路削弱以后，结构优势会变大吗？

- 在**弱抄近路**下（只知道起点和进度）：24 位患者，中位 -0.0003（95% 区间 -0.0024 到 +0.0020，跨过零），正/负/近零 = 9/10/5，方向偏负
- 在**强抄近路**下（还知道点亮过谁）：24 位患者，中位 +0.0004（95% 区间 -0.0002 到 +0.0017，跨过零），正/负/近零 = 9/6/9，方向偏正
- 两者之差，也就是「抄近路被削弱后结构多赚了多少」：24 位患者，中位 -0.0006（95% 区间 -0.0038 到 +0.0020，跨过零），正/负/近零 = 9/11/4，方向偏负

正数意味着：强抄近路确实吸收掉了一部分本来属于空间结构的信息。

### 省状态、省数据吗？

- 状态只留 1 个数：26 位患者，中位 +0.0000（95% 区间 -0.0000 到 +0.0000，跨过零），正/负/近零 = 0/0/26，方向偏正
- 状态只留 2 个数：26 位患者，中位 -0.0000（95% 区间 -0.0004 到 +0.0005，跨过零），正/负/近零 = 6/7/13，方向偏负
- 状态只留 4 个数：24 位患者，中位 +0.0004（95% 区间 -0.0002 到 +0.0017，跨过零），正/负/近零 = 9/6/9，方向偏正
- 状态只留 8 个数：13 位患者，中位 +0.0012（95% 区间 -0.0007 到 +0.0045，跨过零），正/负/近零 = 7/3/3，方向偏正
- 只用 25% 训练事件（空间图案也跟着重估）：24 位患者，中位 +0.0002（95% 区间 -0.0003 到 +0.0016，跨过零），正/负/近零 = 9/3/12，方向偏正
- 只用 25% 训练事件（空间图案固定为全量版）：24 位患者，中位 -0.0003（95% 区间 -0.0010 到 +0.0005，跨过零），正/负/近零 = 7/7/10，方向偏负
- 只用 50% 训练事件（空间图案也跟着重估）：24 位患者，中位 -0.0005（95% 区间 -0.0017 到 +0.0005，跨过零），正/负/近零 = 6/8/10，方向偏负
- 只用 50% 训练事件（空间图案固定为全量版）：24 位患者，中位 -0.0003（95% 区间 -0.0006 到 +0.0016，跨过零），正/负/近零 = 8/7/9，方向偏负
- 只用 100% 训练事件（空间图案也跟着重估）：24 位患者，中位 +0.0004（95% 区间 -0.0002 到 +0.0017，跨过零），正/负/近零 = 9/6/9，方向偏正
- 只用 100% 训练事件（空间图案固定为全量版）：24 位患者，中位 +0.0004（95% 区间 -0.0002 到 +0.0017，跨过零），正/负/近零 = 9/6/9，方向偏正

## 7. 部分电极覆盖，让结论只能说到哪里？

我们只看到了临床植入的那些触点：28 位患者，触点数从 6 到 52（中位 10），事件数从 447 到 140337。
在有临床标注的 25 位患者里，被记录触点中带临床发作起始标注的比例中位是 0.62（这只是描述覆盖情况，不用来排除任何患者，也不叫“覆盖率”）。

**这意味着**：即使结果是正的，也只能说「在临床已经记录到的这些触点构成的问题里，某种空间先验提高了有序历史的预测效率」。它**不能**说我们找到了患者真实的神经连接，不能说电极覆盖了发作起始区或传播网络，也不能把阴性读成「脑内不存在有方向的传播」。

### 用已知答案的仿真校准：这套方法在什么条件下看得见？

我们造了一批「答案已知」的假数据：先规定好一条真实的方向轴和一个真实的低维算子，再让它生成事件，然后把和真实数据**完全相同**的分析流程套上去，看能不能把答案找回来。

以下用样本量更大的功效块（每格 24 个仿真 montage）来判读，而不是只有 3 个 montage 的正确性格子：

| 方向来源 | 真实沿轴强度 | 胜出/总数 | 二项 p | 效应中位 |
|---|---:|---:|---:|---:|
| axis estimated | 0 | 13/24 | 0.839 | +0.00005 |
| axis estimated | 1.2 | 13/24 | 0.839 | +0.00033 |
| axis estimated | 2.5 | 12/24 | 1.000 | +0.00006 |
| true axis given | 0 | 11/24 | 0.839 | -0.00062 |
| true axis given | 1.2 | 15/24 | 0.307 | +0.00147 |
| true axis given | 2.5 | 14/24 | 0.541 | +0.00196 |

**没有任何一格显著偏离掷硬币**——包括把**真轴**直接交给它、且仿真事件以最强强度（2.5）沿该轴推进的那一格。

**这意味着**：这套流程在当前的事件量、状态维数和对照设计下，对「已知且很强的沿轴结构」几乎**没有检出力**。因此真实数据在结构这一层既不能读成「存在有方向的传播」，也不能读成「不存在」——这一层是**看不清**，不是阴性。

唯一的方向性线索是：给真轴时，效应中位随真实沿轴强度单调上升（-0.00062 → +0.00196），而按 spec 估计轴时没有这种依赖。但这个幅度低于换一个随机种子重训所产生的漂移，只能当作方向提示，不能当作「机器能检出、只是估计器不行」的证据。
- S2_mask_kind：找回比例 0.33，实际观察到的触点占比中位 0.69
- S2_misspecification：找回比例 0.50，实际观察到的触点占比中位 0.79
- S2_patient_montage：找回比例 0.43，实际观察到的触点占比中位 1.00

仿真只用来划定「阴性能解释到哪里」，它不决定任何真实患者的纳入或后续实验。

## 8. 高密度网格电极的构造效度个案（两位患者，各自单独看）

这两位患者用的是贴在皮层表面的方格电极，物理上下左右邻接比深部电极的距离图更明确。我们用**完全相同**的任务和抄近路对照，只把状态换成「整片触点场按冻结网格的少参数多项式往前推」。

- E958：127 个训练单元；把网格换成错位网格后代价 0.011189736846969112，换成同度数随机重连后代价 0.011077293601781335（换图时参数一个都没动：True）
- E1084：127 个训练单元；把网格换成错位网格后代价 0.05164945376957242，换成同度数随机重连后代价 0.04717294568890029（换图时参数一个都没动：True）

两位患者分别报告，不合并算 p 值；一位为正不构成「网格电极的普遍机制」，两位不一致也不能反过来否定深部电极那 28 位的结论。

## 9. 模型完全没见过的那部分数据

所有选择（模型、状态维数、数据量、空间图案、正则、对照族）全部冻结之后，我们才解封最后一段数据，而且只评分一个事先锁死的组合。实际访问 708 个单元，2819 个因为在锁定范围之外被拒绝。

- 患者对齐 vs 把方向转掉：{"n": 24, "median": 0.0012505007338782237, "n_positive": 17, "n_negative": 7, "median_ci95": [0.0002751574179993277, 0.002230150394719299], "crosses_zero": false}
- 完全自由的低维 vs 强抄近路：{"n": 28, "median": 0.008994136724351565, "n_positive": 20, "n_negative": 8, "median_ci95": [0.0004159113771715628, 0.02587041845256144], "crosses_zero": false}

这是「模型没见过」的确认，**不是**前瞻性验证。

## 10. 工程完成情况

- 预注册单元：3527 个合格，3527 个完成，0 个未解决，0 个缺失
- 出现过非有限数值的单元：0

（内部归档代号：`U_MINIMAL` / `U_FULL_SET` 两级无序 baseline、`DIRECT_HORIZON_UPPER_BOUND` / `AUTONOMOUS_SHARED_OPERATOR` 两个模型族、`PREFIX_ORDER_COST` / `ORDERED_PATH_ABLATION_COST` / `BASIS_TRANSPLANT_COST` 三个使用期实验、`ANGLE_ROTATED_AXIS` / `IDENTITY_PERMUTED` / `LOCALITY_REWIRED` 三族对照、`RUNTIME_GRAPH_SWAP` 仅用于 ECoG。技术细节见同目录技术收口报告。）

