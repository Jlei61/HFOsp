# Topic 5.3：哪种动力学解释「走这么远需要多久」

> 日期：2026-08-19　|　结果根：`results/topic5_motif_time_targets_v0_3/`
> 冻结设计：`docs/superpowers/plans/2026-08-19-topic5-motif-time-targets-v0-3.md`

## 0. 这一轮问的是什么

一次间期事件里，触点是一批一批亮起来的。上一轮问的是「下一个亮的是谁」，四种传播规则分不出高下。这一轮换一个量：**从这一批亮到下一批亮，中间隔了多久**。

换这个量是有依据的：控制住「隔了几个名次」之后，两个触点之间的**空间距离**仍然预测它们的**时间差**，28 位患者里 27 位同向。也就是说时间里装着名次里没有的距离信息，而上一轮所有模型用的「时间」其实只是名次序号，把这份信息整轮扔掉了。

⚠️ 这里的「时间」是事件内谱质量中心的位置，**不是**临床上的招募时间，**不是**轴突传导延迟，**不能**拿它算传导速度。

## 1. 怎么比的

四种传播规则依次嵌套，后一种是前一种加一个机制；每一层都从**前一层已经训好的模型**接着练，新加的那个旋钮从零开始，所以「加这个机制有没有用」问得干净。

- **各向同性局部扩散（往四面八方一样扩）**（内部 `M0_ISOTROPIC_DIFFUSION`）
- **轴向走廊（沿这位患者的一条方向拉长）**（内部 `M1_AXIAL_CORRIDOR`）
- **方向输运（由事件早期的位移选一个方向，然后一直往那边推）**（内部 `M2_DIRECTED_TRANSPORT`）
- **轴向前馈的有限时程放大**（内部 `M3_AXIAL_FEEDFORWARD_TRANSIENT`）
- **自由低秩替代算子（同一个细胞，秩 4–7 的不受约束算子）**（内部 `MFREE_LOW_RANK_UPPER_BOUND`）

**最关键的一点是参照要公平。** 模型读的是「实际亮起来的那个触点」上的场，也就是它被告知了目的地。如果参照只知道「走到第几步」，那么「目的地有多远」带来的好处就会被白送给模型——实测这一项**单独**就值 0.2%–5.5% 的方差，和模型全部效果同一量级。所以参照逐级加信息：只知道第几步 → 再知道实际走了多远 → 再知道那个触点是否惯常早到。

规模：{'complete': 28}，共 140 条臂记录，每条轴向臂 6 个优化起点。

> **2026-08-20 更正（不重跑）**：原文写的是「3 个轴角起点 × 2 种继承方式」。
> **轴角那一半不成立**。`MotifRNN` 不论哪个 model_id 都会建出全部四个 motif 参数，
> 所以没有轴的父模型也存着 `theta = 0`；本轮 runner 先设角度、后继承父模型，
> `load_warm_start` 最后把 `theta` 盖回 0，三个角度全塌成一个。
> 证据：本轮 `PER_ARM_SCORES.csv` 的 `chosen_theta_init` **28/28 全为 0.0**。
>
> 更正的范围：**数值不变**——下文每条「换一个优化起点的散布」都是真实的非零量
> （28/28 非零），但它衡量的是**读出继承方式**（继承父模型的读出 vs 重新拟合），
> **不含轴角**。**错的是标签，不是结论**。轴向走廊层实际拟合出的角度跨 −1.09 到 +1.53，
> 说明梯度自己把角度从 0 推开了，该层没有被锁死。**仍然不知道的是**：
> 换别的角度起点会不会找到更好的解——本轮回答不了，也没有回答过。
>
> 该 bug 在后续轮次发现并修复（继承之后再恢复本层引入的参数），
> 见 `docs/superpowers/plans/2026-08-20-topic5-motif-rnn-v0-4-autonomous-propagation.md` §13–14。

### 参照是怎么定的（这一条决定了下面的判决）

参照固定为「知道第几步 + 走了多远 + 该触点是否惯常早到」那一条，理由是**结构性的**：它严格包含另外两条所知道的一切。按「它知道什么」选，不按「它考了多少分」选。

两个被否掉的做法，各自的理由：

- **逐患者在测试集上取三条基线的最小值**：这是看过测试误差之后再决定参照，违反「测试集只评一次」。（核实过：26/28 位患者会选同一条，改掉后中位数一位不差。）
- **给参照加一个验证集选出的岭正则**：我实现并跑了，结果第二层从全部跨零**翻成三条显著**。查下来是：岭让参照的中位恰好不变，却把 3 位患者的参照显著削弱（有一位从 0.828 变成 0.893）。去掉那 3 位后全部塌回不显著，用无岭参照也全部不显著。数据并不支持「无岭在过拟合」——无岭版在 18/28 位患者上测试误差更低或相等。**一个让对照变弱的正则化不予采用**；它作为敏感性记录在证据文件的 `comparator_sensitivity_validation_selected_ridge` 字段。

## 2. 第一层：距离本身能解释多少

- 知道走了多远，比只知道第几步好多少：28 位患者，中位 +0.01084（95% 区间 +0.00562 到 +0.02004，不跨零），正/负 = 26/2，符号检验 p=0.000
- 再知道那个触点惯常早到还是晚到，又好多少：28 位患者，中位 +0.01179（95% 区间 +0.00705 到 +0.02482，不跨零），正/负 = 26/2，符号检验 p=0.000

**距离确实携带时间间隔信息**，所以后面每一层都必须先跨过这条线，而不是跨过「只知道第几步」那条低得多的线。

## 3. 第二层：递归场是否比距离更多

- **各向同性局部扩散（往四面八方一样扩）** 相对最强参照：28 位患者，中位 -0.00253（95% 区间 -0.01300 到 +0.01094，跨过零），正/负 = 12/16，符号检验 p=0.572；换一个优化起点的散布是 0.00000（效应是它的 inf 倍）
- **轴向走廊（沿这位患者的一条方向拉长）** 相对最强参照：28 位患者，中位 +0.00364（95% 区间 -0.00478 到 +0.01846，跨过零），正/负 = 17/11，符号检验 p=0.345；换一个优化起点的散布是 0.00454（效应是它的 0.8 倍）
- **方向输运（由事件早期的位移选一个方向，然后一直往那边推）** 相对最强参照：28 位患者，中位 +0.00492（95% 区间 -0.00563 到 +0.01659，跨过零），正/负 = 17/11，符号检验 p=0.345；换一个优化起点的散布是 0.00254（效应是它的 1.9 倍）
- **轴向前馈的有限时程放大** 相对最强参照：28 位患者，中位 +0.00502（95% 区间 -0.00415 到 +0.02023，跨过零），正/负 = 17/11，符号检验 p=0.345；换一个优化起点的散布是 0.00111（效应是它的 4.5 倍）
- **自由低秩替代算子（同一个细胞，秩 4–7 的不受约束算子）** 相对最强参照：28 位患者，中位 -0.00485（95% 区间 -0.01408 到 +0.00363，跨过零），正/负 = 10/18，符号检验 p=0.185；换一个优化起点的散布是 0.00968（效应是它的 0.5 倍）

## 4. 第三层：每个机制是否各自再增加解释

- 加上「轴向走廊（沿这位患者的一条方向拉长）」比「各向同性局部扩散（往四面八方一样扩）」好多少：28 位患者，中位 +0.00476（95% 区间 +0.00000 到 +0.01428，不跨零），正/负 = 18/5，符号检验 p=0.185；换一个优化起点的散布是 0.00454（效应是它的 1.0 倍）
- 加上「方向输运（由事件早期的位移选一个方向，然后一直往那边推）」比「轴向走廊（沿这位患者的一条方向拉长）」好多少：28 位患者，中位 +0.00017（95% 区间 +0.00000 到 +0.00411，不跨零），正/负 = 14/3，符号检验 p=1.000；换一个优化起点的散布是 0.00254（效应是它的 0.1 倍）
- 加上「轴向前馈的有限时程放大」比「方向输运（由事件早期的位移选一个方向，然后一直往那边推）」好多少：28 位患者，中位 +0.00000（95% 区间 +0.00000 到 +0.00089，不跨零），正/负 = 11/6，符号检验 p=0.345；换一个优化起点的散布是 0.00111（效应是它的 0.0 倍）

**每条都要连着「换一个优化起点的散布」一起看**。要说清楚的是：那个散布是**验证集上换个起点的敏感度**，与留出患者效应不是同一个随机量，**不是零分布**。所以它只用来给判决加标注，不用来生成判决。

### 优化器有没有把这些参数移离零（这不等于「机制被启用」）

各向异性强度和前馈强度是**非负**的、下界恰好在零；方向偏置是**有符号**的。参数离开零只说明优化器用上了它，**不能**说成某个机制在生物层面被启用：

- **轴向走廊（沿这位患者的一条方向拉长）**（非负、下界在零）：优化器在 18/28 位患者上把它移离了零（其中 0 位为负），移动时中位 0.1403
- **方向输运（由事件早期的位移选一个方向，然后一直往那边推）**（有符号）：优化器在 17/28 位患者上把它移离了零（其中 12 位为负），移动时中位 -0.0127
- **轴向前馈的有限时程放大**（非负、下界在零）：优化器在 14/28 位患者上把它移离了零（其中 0 位为负），移动时中位 0.1228

## 5. 候选的四种规则是不是太窄

- 自由低秩替代算子相对 各向同性局部扩散（往四面八方一样扩）：28 位患者，中位 -0.00488（95% 区间 -0.01939 到 +0.00755，跨过零），正/负 = 13/15，符号检验 p=0.851；换一个优化起点的散布是 0.00968（效应是它的 0.5 倍）
- 自由低秩替代算子相对 轴向走廊（沿这位患者的一条方向拉长）：28 位患者，中位 -0.01621（95% 区间 -0.02524 到 -0.00254，不跨零），正/负 = 8/20，符号检验 p=0.036；换一个优化起点的散布是 0.00968（效应是它的 1.7 倍）
- 自由低秩替代算子相对 方向输运（由事件早期的位移选一个方向，然后一直往那边推）：28 位患者，中位 -0.01709（95% 区间 -0.02601 到 -0.00229，不跨零），正/负 = 8/20，符号检验 p=0.036；换一个优化起点的散布是 0.00968（效应是它的 1.8 倍）
- 自由低秩替代算子相对 轴向前馈的有限时程放大：28 位患者，中位 -0.01955（95% 区间 -0.02411 到 -0.00390，不跨零），正/负 = 8/20，符号检验 p=0.036；换一个优化起点的散布是 0.00968（效应是它的 2.0 倍）

⚠️ **这条臂被我一开始错标成「上界」，它不是。** 它是秩 4–7 的低秩算子，而四个 motif 的结构化核在其局部支撑上是**满秩**的——所以它**不包含**那四种规则，是另一个族。因此赢过它**不能**说明候选够宽；「候选是否太窄」这个问题本设计**NOT ANSWERED BY THIS DESIGN**。结果文件里 `arm` 列仍是旧名，改名只对后续运行生效，纠正记录在证据文件的 `naming_correction` 字段。

能说的只有族间优劣：
- 相对 各向同性局部扩散（往四面八方一样扩）：**NOT ESTABLISHED**
- 相对 轴向走廊（沿这位患者的一条方向拉长）：**REVERSED**（自由算子更差）
- 相对 方向输运（由事件早期的位移选一个方向，然后一直往那边推）：**REVERSED**（自由算子更差）
- 相对 轴向前馈的有限时程放大：**REVERSED**（自由算子更差）

## 6. 判决（逐条，互不蕴含）

| 命题 | 判决 |
|---|---|
| 空间距离解释相邻时间间隔 | **SUPPORTED** |
| 各向同性局部扩散 提供简单基线之外的信息 | **NOT ESTABLISHED** |
| 轴向走廊 提供简单基线之外的信息 | **NOT ESTABLISHED** |
| 方向输运 提供简单基线之外的信息 | **NOT ESTABLISHED** |
| 轴向前馈的有限时程放大 提供简单基线之外的信息 | **NOT ESTABLISHED** |
| 轴向走廊在均匀扩散之上再有贡献 | **WEAK EXPLORATORY** |
| 方向输运在走廊之上再有贡献 | **WEAK EXPLORATORY / OPTIMISATION-SENSITIVE** |
| 轴向前馈在方向输运之上再有贡献 | **NOT ESTABLISHED** |
| 候选的四种规则太窄 | **NOT ANSWERED BY THIS DESIGN** |

## 7. 工程核对

- 逐层继承后父子状态最大差 0.0，逐位一致 = True
- 各向同性那层不记虚假轴角 = True
- 触点分数完全在训练目标之外，不参与任何选择
- 优化起点只在 validation 上挑，测试集只评一次

### 优化起点敏感性（不进任何统计，单独看）

> **接上面的更正**：轴向三层的「起点数 6 / 2 / 2」里**不含轴角差异**——三个角度被父模型的
> `theta = 0` 覆盖了，所以散布全部来自「哪种继承方式」这一列。自由低秩替代算子那行的
> 3 个起点是真实不同的随机初始化（它不继承父模型），不受此影响。
> 上面「逐层继承后父子状态最大差 0.0」这条通过，恰恰**部分是因为** `theta` 也被一起盖回去了。


| 规则 | 起点数 | 起点间散布中位 | 九成分位 | 哪种继承方式胜出 |
|---|---:|---:|---:|---|
| 各向同性局部扩散（往四面八方一样扩） | 1 | 0.00000 | 0.00000 | {'fresh': 28} |
| 轴向走廊（沿这位患者的一条方向拉长） | 6 | 0.00454 | 0.01865 | {'inherit_readout': 22, 'refit_readout': 6} |
| 方向输运（由事件早期的位移选一个方向，然后一直往那边推） | 2 | 0.00254 | 0.00859 | {'inherit_readout': 16, 'refit_readout': 12} |
| 轴向前馈的有限时程放大 | 2 | 0.00111 | 0.00620 | {'inherit_readout': 16, 'refit_readout': 12} |
| 自由低秩替代算子（同一个细胞，秩 4–7 的不受约束算子） | 3 | 0.00968 | 0.03863 | {'fresh': 28} |

## 8. 这一轮不能推出什么

- 本轮**只**回答相邻两步的时间间隔。未来 2–5 步的触点分布、落点、完整后续场、传播范围与事件长度**都没有测**，不得引用。
- 触点分数是目标之外的诊断量，不能当作空间预测能力的结论。
- 时间代理不是招募时间也不是传导延迟，任何速度换算都是错的。
- 各条判决互不蕴含，逐条读。

