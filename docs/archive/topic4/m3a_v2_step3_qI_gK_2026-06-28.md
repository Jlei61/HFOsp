# M3A-v2 Step 3 — q_I + g_K rescue scout = 机制不闭合（NEGATIVE, informative, 2026-06-28）

> **状态**：descriptive screen，用户 §8–§9 Step 3 设计（boundary-oriented rescue scout）。**禁**"发作机制/破轴已证"。**判据锁（review P1-2）**：off-axis recruitment 必须 **F_off↑**，不是只 S_axis↓（轴读出崩 ≠ recruitment）。
> **可复现**：`scripts/run_m3a_v2_step3_qI_gK.py` → `results/topic4_m3a_v2_step3_qIgK/step3_results.json`（raw_rows 全 schema + per-seed + paired Δ vs 同点 q_I-only ref）。

## 朴素摘要（测了什么 / 怎么测 / 揭示了什么）

**测了什么**：Step 2 发现 q_I 单独把事件推大就 runaway（缺刹车）。Step 3 加第二张地图 g_K（"放电放累了会疲劳"，用得越多压得越狠的恢复刹车）。决定性问题：**g_K 能不能把 q_I 造成的失稳，变成"受控的、能平息的离轴招募"（= ictal-like candidate）？** 尤其那些 q_I-only 刚好失控的点，加 g_K 能不能 rescue 成 off-axis + returned。

**怎么测**：在 primary/sensitivity 稳健衬底上，选 q_I boundary 点（near/deep）× σ_q/σ_K∈{1⁺,1.5,2}（保 σ_q>σ_K）× **Γ_K∈{0(=q_I-only ref),0.5,1,1.5,2}**（疲劳/去抑制电流比，经 η_K 标定：g_K 是 0–1 场、k_K 设大让 g_K 在轴向饱和、η_K=Γ_K·ΔI_disinh 调比）× 4 seed。backup=低兴奋负控。每 run 量 R/S/F_off/returned + q_axis/off/global_min + **g_K,axis/off^peak** + paired Δ。

**揭示了什么——机制不闭合（干净 NEGATIVE）**：
- **432 个 g_K cell：298 B_gK_oversuppress、127 C2_still_axial、7 C1_still_runaway、A（受控 off-axis recruitment）= 0。RESCUED = 0。**
- **g_K 纯粹是个刹车**：它正确地把疲劳集中在轴（fatigue gap gK_axis−gK_off mean=+0.118，σ_K 机关 work），但效果是**刹住/缩小轴向事件**（Γ_K 越大 dR 越负：−0.16→−0.34），不是把活动重定向到离轴。
- **F_off 没达到 off-axis recruitment gate**（全 432 cell max dF=+0.083；只 4 个 cell dF>0.05，**且这 4 个全是 C2_still_axial、S_axis≈1、F_off max 0.212**）——只有极少 still-axial cell 出现**小幅** F_off 上升，没到 recruitment。
- **q_off 有轻–中度下降，但没进低-q permissive regime**（min q_off_min=0.735，**从未低于 0.7**）——离轴 q_I 降了，但远没到"低-q 初始态"机制需要的范围。
- **g_K 能把 q-only runaway 转成 returned**（6 个 runaway ref → 加 g_K 后 10–12/12 returned），**但转成的是 suppressed / axis-dominant event（returned cell R 范围 0.05–0.50，有小事件也有中等轴向事件，非全"刹死"），不是 off-axis recruitment**。

## §1 为什么不闭合——缺的是"离轴许可度"，不是疲劳

机制链要的是：q_I 宽去抑制让**离轴 q_I 也降**（off-axis permissive）+ g_K 窄疲劳压轴 → 离轴比疲劳的轴更易兴奋 → 招募转离轴。Step 3 数据：
- **疲劳那半边 work**（gK_axis>gK_off，轴被压）。
- **但离轴许可度那半边不 work**：`q_off_min=0.735`（离轴 q_I 几乎没降）。**原因（timescale）**：SNN 的踢出事件**很短（~55ms）**，q_I 耗竭 ∝ 活动×时间；即使 σ_q 宽，这么短的轴向事件也**来不及把离轴 q_I 耗深**。所以轴一疲劳，事件就**直接死掉**（离轴没被许可、接不住），而不是转去离轴。
- 对比：**field-only probe（prescribed 持续 corridor 活动 400ms）**能把离轴 net 兴奋度推上来——因为它持续耗竭。**closed-loop 单个 55ms 事件做不到。** 这就是 field-sanity 正、closed-loop 负的差距所在。

## §2 关键数（详 step3_results.json）

| 量 | 值 |
|---|---|
| g_K cells | 432（B 298 / C2 127 / C1 7 / **A 0**） |
| RESCUED（runaway→off-axis+returned） | **0** |
| max dF（off-axis 证据） | +0.083（需 >+0.05；只 4/432） |
| min q_off_min（离轴许可度） | 0.735（几乎没降） |
| fatigue gap gK_axis−gK_off | mean +0.118（疲劳正确压轴） |
| Γ_K **target vs achieved** | 随 **target** Γ_K↑ 事件更易 suppress（dR −0.16→−0.34）；但 **achieved** Γ_K 远低于 target 且非单调（target 2.0→achieved median **0.082**；B_oversuppress achieved median 0.055）——**事件被压短 → g_K 来不及充分积累**，故 target/achieved 须同时报 |
| 6 runaway refs | 加 g_K → 10–12/12 returned，但全 B_oversuppress |

## §3 判读 + fork（用户 C2 路）

- **Step 3 能支持的**（窄）：在**当前单事件 + q_init=1 + dq≤0.30 + target Γ_K≤2 + primary/sensitivity** 范围内，**q_I+g_K 没有闭合成 ictal-like off-axis recruitment；g_K 提供终止/抑制，但没把活动重定向到离轴**。映射用户判据 = **C2（仍只沿轴）+ B（oversuppress）主导** → "q_offaxis permissiveness 不够"（g_K 调到 oversuppress 都没出 off-axis）。
- **Step 3 不能支持的**：**不能**说"M3A-v2 整体机制已被证伪"——**因为 fork A（低-q 初始态）还没测**，那是 Step 2/3 最大的未测边界。**field-only 载体仍正。**
- **最该试的下一步 = 低-q 初始态 / 持续事件 regime**（Step-2 review 标的"未测低-q 初始态"）：先用**多次事件/持续 drive 把 q_I（含离轴）耗到低-q 态（秒级）**，再 probe——这正是 field-probe 持续活动模拟的、closed-loop 单事件缺的。如果离轴 q_I 真降下去了，机制才有机会。**这是直接补 §1 那个 timescale 缺口的测试。**
- **次选 fork = D_EE(x,t)**（E→E relay depression，延后的 Stage-2 杠杆）：直接削轴向 relay scaffold 优势，让离轴能竞争。但 D_EE 是新机制，先试低-q 更直接。

**锁**：descriptive screen；机制不闭合 = scope-limited（单事件 q_init=1）；**未测低-q 初始态**（最可能的补救 regime）；破轴/发作未证；M3A-v2 closed-loop 机制在当前 regime **未闭合**，field-only sanity 仍正（载体能造地形，但 SNN 单事件不触发）。
