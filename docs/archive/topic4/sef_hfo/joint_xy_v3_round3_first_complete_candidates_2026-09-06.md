# v3 第三轮：先完成的五个完整八网络候选

本记录只包含先完成全部预设八网络的五个候选，不能代替本轮六候选的完整比较。最后一个候选继续执行，提名、损失、参数、验收和独立确认规则均未改动。

| 候选 | 八网络事件数 | 初筛联合距离 | 八网络联合距离 | 验收 |
|---|---:|---:|---:|---|
| `joint_r001_022` | 87 | 0.10437 | 0.17253 | 分布不匹配 |
| `xy_interior_007` | 104 | 0.08555 | 0.12587 | 分布不匹配 |
| `replicated_r003_003` | 58 | 0.07678 | 0.09755 | 事件不足且分布不匹配 |
| `replicated_r001_003` | 70 | 0.10260 | 0.08376 | 分布不匹配 |
| `replicated_r002_009` | 63 | 0.09518 | 0.12514 | 事件不足且分布不匹配 |

五个候选均保持完整双 core、无 early runaway、条件指标可估计。前四个参与／rank／时滞及派生方向均未通过对应患者训练校准；第五个通过旧参与摘要 D_support，但参与核、rank、时滞、联合核及方向仍失败。不能把能产生事件或形状看起来合理写成患者分布复现。

本轮新位置 `replicated_r003_003` 的八网络事件数依次为 8、9、7、2、7、7、9、9，总数 58，低于 64 的预设要求；其中 seed 2514 只有两个事件，原样计入，未删除或替换。联合距离的修正搜索分数为 0.0873269，较初筛的 0.0456959 变差，也未改善此前完整候选最佳参考的 0.0639434。事件不足之外，它的参与、rank、时滞核距离分别为 0.12886、0.09622、0.10643，也均超过自身样本量对应的校准要求。因此不能仅靠补足事件数就宣称合格。

第四个候选 `replicated_r001_003` 的原始联合距离在追加网络后改善，但修正搜索分数为 0.0749391，仍差于此前最佳完整参考 0.0639434；联合距离约为自身患者校准上限的 10 倍。它有 70 个事件、每网络 5–12 个事件，数量达标，分布仍不合格。追加评估可能使分数变好或变差，不能把这种变化单独视为位置优化进展。

第五个候选 `replicated_r002_009` 总计 63 个事件，不能因只差一个而放宽 64 的门槛。它的旧参与摘要 D_support 达标是应保留的部分改善，但参与核距离 0.020733 仍超过对应上限 0.017289；时滞核距离 0.126343、联合距离 0.125137 均明显不匹配。修正搜索分数为 0.115246，未改善最佳参考。即使单独解决事件数量，现有分布失败仍在。

这些结果支持继续使用八网络成绩筛选局部搜索中心，也提示局部新位置的初筛优势不稳定；它们不能证明所有 XY 都无解，不能将失败唯一归因为局部最优或模型容量。最后一个候选完成后，再核对本轮最佳分数与既定随机重启触发条件。

证据：`results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/003/completed_candidate_review/summary.json` 与同目录五个候选 JSON。入口 `scripts/summarize_topic4_xy_replicated_completed_racers.py --round 3` 已运行成功，重新读取并校验轨迹来源、确认恰为预设 2511–2518 八个网络；这是训练网络扩展，尚非独立确认。
