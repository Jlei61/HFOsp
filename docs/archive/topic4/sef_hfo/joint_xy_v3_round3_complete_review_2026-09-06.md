# v3 第三轮完整复盘与第四轮随机重启

第三轮 48 个初筛和 36 个追加任务全部完成。六候选均未通过患者分布验收，没有进入独立确认；最佳搜索参考仍为 `xy_whole_sheet_062`。第四轮已自动启动并提高全局随机抽取概率，Goal 保持 active。

## 完整结果

| 候选 | 八网络事件数 | 联合距离 | 数量达标 | 完整验收 |
|---|---:|---:|---|---|
| `joint_r001_022` | 87 | 0.17253 | 是 | 未通过 |
| `xy_interior_007` | 104 | 0.12587 | 是 | 未通过 |
| `replicated_r003_003` | 58 | 0.09755 | 否 | 未通过 |
| `replicated_r001_003` | 70 | 0.08376 | 是 | 未通过 |
| `replicated_r002_009` | 63 | 0.12514 | 否 | 未通过 |
| `xy_whole_sheet_040` | 76 | 0.08173 | 是 | 未通过 |

六候选均无 early runaway、双 core 完整、条件指标可估计。`replicated_r002_009` 的旧参与摘要 D_support 达标，其余候选该项失败；所有候选的参与核、rank 核、时滞核、联合核及派生方向验收均失败。不能因只有一个事件的缺口或某一摘要改善而改变完整验收。

本轮最低原始联合距离为 `xy_whole_sheet_040` 的 0.0817270，修正搜索分数 0.0736128，仍差于此前最佳参考的 0.0715789、0.0639434。本轮没有改善搜索最佳分数。已经检查前三轮提名与完整验收，并逐项验证完成记录、快照与候选轨迹哈希；该结论不是从图上估计。

## 失败诊断与实际调整

事件数不足只解释两个候选的一项资格缺口，不能解释四个数量达标候选的分布失配。初筛与八网络分数可向两个方向变化，本轮新局部位置在扩展后变差，说明短时两个网络不足以确认位置优势。已有时间尺度、观测权重、VTH 实现和实际延迟审查仍作为方法诊断；本轮没有新证据要求修改损失，也没有证明全部 XY 无解。

当前安全且预设的调整是扩大优化覆盖：连续观察点最佳修正分数为 0.0767510、0.0639434、0.0639434、0.0639434，满足停滞触发条件。第四轮每次尝试抽取全局随机方案的概率从 0.5 提高到 0.85。几何过滤后的实际 24 个新位置为 **17 个全局随机、7 个局部随机**，不是硬性 85% 配额。完整设计可由种子序列 `[2395812204, 4, 0]` 重现，24 个几何均不重复。

第四轮实际局部中心为 `xy_whole_sheet_062`、`replicated_r002_014`、`xy_whole_sheet_040`、`replicated_r001_003`；后两个来自本轮扩展。这些中心只是按八网络表现选出的搜索参考，均不表示合格基底。初筛数据差或数量不足的候选不会成为可信局部中心。

## 证据与衔接

`scripts/audit_topic4_xy_round_transition.py --round 3` 已成功重放全部提名、第三轮验收和第四轮随机设计，1607 个冻结源码／输入条目通过核验。记录为 `results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/003/round_transition_review.json`，完整科学分数为同目录 `analysis.json` 与 `combined_*.json`。第四轮实际 worker 进程已核验，未修改运行中的损失或患者留出边界。

同目录 `figures/candidate_after_event_expansion.{png,pdf,svg}` 与中文 README 已生成，PNG 和 PDF 渲染图已检查，哈希匹配生成记录；图底注较密，仅作为诊断图，不代表作者接受。58、63 事件使用不同样本量校准，不能仅按归一化柱高排序。均值 rank 曲线只作说明，拟合目标仍是完整事件分布。

当前没有独立确认通过的基底，Fig.5 尚未启动。后续继续既定搜索与容量审查，不能以批次完成替代目标完成。
