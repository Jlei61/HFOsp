# 全局固定状态 s × 原生 Z：历史手放双 core pilot

状态：COMPLETED_CLOSED_NO_EXPANSION（2026-09-09）。18/18 完成，配对输入、零接口与精确计数检测检查通过。按用户最新决定收口此支线，后续患者 s(t) 推断与 Z/M 分开；本轮 M 始终关闭。运行完成不等于临床机制或人工图面验收。

## 本轮收口判断

原生 Z 的 9/9 次运行均达到持续高活动判据；固定 Z 的 9/9 次在 20 秒内均未达到。原生 Z 下，负状态相对零状态的确认时间分别提前 0.47、1.66、1.10 秒；正状态分别晚 0.06 秒、早 1.30 秒、早 0.01 秒。s 对进入时刻有有限影响，不能称完全无影响，也没有证据把它推广为 M 的效应。

原生 Z 下共同时窗 1.5–8 秒的 M0 比例为 12.5%、48.9%、88.9%（s 从负到正）；该共同时窗是事后敏感性检查，冻结主分析保留。主要支持状态调节模式偏好；不能把分类器标签等同于患者传播相容性。统计重复是每条件 3 个噪声运行，同一张图。

全部高活动运行在后续最多 2 秒的观察内未恢复安静。本轮没有自主终止、动态 s 或 s 反馈的检验。保留现有代码、配置、逐事件数据和三组结果图，不加幅度、不换图、不追加 M 或 Z 条件。详细结果见输出目录的 scientific_interpretation.md、scientific_audit.json 和 analysis_qa.json；图面人工检查仍待用户完成。

## 科学问题和父版本

在相同快网络与噪声输入下，s 对传播选择的作用是否会经由 Z 的活动历史改变进入持续高活动的时间？父版本严格使用用户所贴图的 `historical_manual_hard_native_z_v1`：图 seed 6101、A=(4.1992,9.1289)、B=(16.4792,3.9655) mm、半径1.5 mm、历史 manual_hard 阈值构造及原 C 快速连接。1112个E细胞位于实际core，使用保存数组身份验证；不是连续状态 R1 的另一张图或参数版本。

先重放同图的原生 Z 开关基线，状态接口为零时必须与父引擎逐步一致。接入的是连续状态 R1 已验证的局部 I Poisson 输入重分配接口：幅度0.2，A正、B负、按人数归一化，预期总 I 输入守恒。作用几何固定在本图半径1.5 mm内；不改变阈值、连接、细胞数或E输入。背景全局/空间OU及Poisson按噪声种子配对。

## 六条件及预算

- s：−0.5、0、+0.5，全程固定；前1秒不施加调制，随后200毫秒线性进入，传播统计从1.5秒开始。
- Z：固定为1，或原方程自主演化；tau_Z=5000毫秒，I_th_EI=95.19851312666987。I靶端Z固定为1，M关闭。
- 噪声：9108401（原图）、9108402、9108403。总计18次运行，同一张图。最多3个worker并行。
- 每次最大20秒。沿原图判据：全E每10毫秒平均率连续200毫秒≥200Hz，记录确认时间与该连续区间起点；达到判据后最多自主延续2秒，或先到20秒即止。
- 不补回Z、不重置s、不在患者事件时刻施加kick。原图人工补回仅保留为既有干预参考，不能与本轮自主轨迹混为同一实验臂。

## 统计、读出与可区分解释

运行是统计重复单位，事件不是18次以上的独立重复。未触发属于20秒右截尾；触发后停止观察也不能视为活动停止。持续高活动是模型运行判据，不是临床发作分类器。

保存真实样本raster、全E/I率、core内E/I及核外群体分箱spike计数、逐区原始/有效抑制、Z均值、抑制负荷超阈值比例、空间Z与发放图、末端逐神经元Z。模式读取复用冻结患者观察器和分类器，不重拟合标签。只把窗口完整位于1.5秒之后、持续高活动区间开始之前的合格事件计入间期模式统计；保留其他检测及排除原因。两core启动读出使用事件窗口内2毫秒率首次达到各自峰值20%的时间，称rise20，不称单神经元真实起燃。

决定性比较是同噪声下s作用在Z固定与Z演化两组是否不同，并对照原始抑制—Z—有效抑制的时序。若仅模式偏好变化，支持访问作用；若Z轨迹与进入时间也变化，支持状态—资源耦合；它本身尚不足以唯一证明中介因果链，必要时后续再做匹配初态的冻结Z反事实。不能从单一Z全局均值推断统一临界阈值。

本轮最多观察触发后2秒，只能检验短期持续或自限，不能宣布长期终止或多稳态。若无可重复交互，不自动提高状态幅度、换图或打开M。若存在交互，下一版再单独设计M与自主恢复的试验。

## 交付与资格检查

路径：`/data/hfosp/topic4_sef_hfo/state_s_native_z_pilot_20260909`。工作树：`/home/honglab/leijiaxin/HFOsp/.worktrees/topic4-state-s-native-z-pilot`，分支 `codex/topic4-state-s-native-z-pilot`。

引擎与状态接口在独立目录保留源码快照。基底重建读取现有历史数据，核对父图的阈值、位置、AMPA/GABA拓扑与权重身份。资格检查覆盖Z开/关时零接口的全部spike及Z逐步一致性、与父图前250毫秒样本spike一致性、激活接口后共同外部输入与预期预算、流式分箱观察器与原适配器一致性。所有正式worker在运行前后核对资格时加载源码身份，发现外部版本改变则保留失败并停止该单元，不接着混用新版本。

队列完成后自动分析，输出`run_summary.csv`、`event_summary.csv`、`scientific_report.md`和三组PNG/PDF：六条件汇总、预指定第一噪声的六条件raster、原生Z下三状态的局部资源/抑制轨迹。人工图面与科学验收保持待审阅。
