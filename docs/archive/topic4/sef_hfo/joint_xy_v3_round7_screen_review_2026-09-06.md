# 第七轮初筛审查

第七轮 24 个新四维 XY 位置、每位置 seed 2511/2512 的 48 个仿真已全部完成。实际观察事件数为每候选 10–27 个，没有早期 runaway 标记。历史候选池累计 323 个位置；这只是已评价的有限候选集合，不能声称覆盖所有可行解或已经证明全局最优。

## 工程与选择规则核验

已核验 48 个 worker 及 NPZ 哈希、runtime snapshot，并验证 1607 个锁定输入/代码哈希。重放各轮历史候选替换及本轮提名，名单逐项一致；重放没有读取本轮追加仿真结果。患者 held-out 未打开，损失和资格阈值未改变。

按照既定规则，先保留未展开池中的 rank-space 最优和 timing-space 最优候选，再按探索分数与位置差异补齐六个。实际名单包括五个本轮新候选和一个旧池候选。六个位置均保持完整核心边界；没有因为最低分来自越界核心而纳入无效形状。

## 实际展开名单

| 候选 | 两网络事件数 | 联合误差 | 探索分数 | 两核心中心 (mm) |
|---|---:|---:|---:|---|
| joint_r001_006 | 16 | 0.151544 | 0.105388 | (5.50, 5.00) / (11.93, 10.67) |
| replicated_r007_008 | 25 | 0.068464 | 0.043957 | (2.59, 6.32) / (12.32, 7.06) |
| replicated_r007_009 | 10 | 0.092874 | 0.039537 | (4.58, 6.90) / (13.69, 6.95) |
| replicated_r007_010 | 20 | 0.072533 | 0.048148 | (7.07, 10.59) / (13.55, 17.32) |
| replicated_r007_005 | 13 | 0.082021 | 0.050842 | (3.87, 8.61) / (12.93, 7.78) |
| replicated_r007_023 | 21 | 0.080591 | 0.051088 | (3.89, 5.19) / (7.47, 10.76) |

`joint_r001_006` 因 rank-space 分量最优入选，并非联合误差最小；`replicated_r007_008` 保留 timing-space 最优，同时是本轮新位置中原始联合误差最低者。其余四个按探索分数和几何多样性入选。因此名单里存在联合误差偏高的旧候选，是预设分量探索规则的结果，不能误写成患者拟合更好。

## 当前能判断什么

新候选 r007_008 的两网络原始联合误差为 0.068464，25 个事件，双核仍在中下区域，但左侧核心距边界约 0.86–0.87 mm，未越界。不能将它和八网络训练最优的 0.070242 直接比较后宣称改进：样本量和网络覆盖不同。

r007_009 只有 10 个事件，原始联合误差 0.092874，但修正后的探索分数为 0.039537，排在本轮新候选首位。探索排序与患者分布资格是两个判据；该候选需要实际追加网络事件，不能将样本量相关的分数修正当成传播恢复。若展开后仍不足 64 个事件或任一分布项失败，它仍不能冻结。

展开名单同时包括中下、左侧斜向和上方位置。上方 r007_010 的中心约为 (7.07,10.59)/(13.55,17.32)，会与其他候选使用同样的追加 seeds 比较。双核并没有被固定只能出现在底部；当前尚不能判断上方候选会更好。

## 已启动的下一阶段

自动展开已启动：六候选各补 seed 2513–2518、每个 8 s，共 36 个任务。与初筛原始轨迹合并后，按实际事件数和全部患者分布门槛评估。通过后仍需新的独立网络确认以及原始传播检查；Fig.5 等待状态不变。

审查输入：`results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/007/screen_review.json`；SHA256 `517b197c02c3f4e4f9e7eb0a543bd6bfee83f90c84c60eec6db8622d73145e43`。
复现命令：CUDA Python 环境运行 `scripts/audit_topic4_xy_completed_screen.py --round 7`。
