# v3 第四轮初筛：扩大随机探索后的候选

第四轮 24 个不同位置、48 个初筛任务全部完成，累计探索 251 个不同位置。新位置各有 11–22 个实际事件，未检测到 early runaway。六个候选已自动进入各六个追加网络、共 36 个任务；真实 worker 进程已核验。

## 提名结果

| 候选 | 两网络事件数 | 联合距离 | 修正搜索分数 | 提名来源 |
|---|---:|---:|---:|---|
| `replicated_r004_016` | 17 | 0.24983 | 0.20549 | 本轮全局随机，rank 项提名 |
| `xy_whole_sheet_007` | 21 | 0.10078 | 0.07175 | 历史池，时滞项提名 |
| `replicated_r002_011` | 8 | 0.14628 | 0.06152 | 第二轮全局随机 |
| `replicated_r004_021` | 14 | 0.10579 | 0.06166 | 本轮局部随机 |
| `replicated_r002_002` | 22 | 0.09103 | 0.06400 | 第二轮局部随机 |
| `replicated_r004_013` | 21 | 0.09239 | 0.06466 | 本轮全局随机 |

本轮三个新位置进入追加评估，其中两个来自全局随机重启，一个来自局部随机。新位置 `replicated_r004_013` 位于 (4.802, 13.326)、(14.673, 14.578) mm，在平面上半部且两网络核心边界余量均超过 3 mm。搜索并未被限制在下方两个 core；但改变位置和通过患者分布验收仍是两回事。

## 需要区分的两类初筛现象

`replicated_r004_016` 的 rank 核距离为 0.03957，在尚未扩展的合格初筛池中按该单项被提名；其时滞核距离 0.26290、联合距离 0.24983 很差。该提名来自预设的分量探索规则，不能将它误称为联合拟合最优候选。此次追加用于检查 rank 改善是否跨网络保持，同时观察其他分量代价，不能因一项好就冻结。

`replicated_r002_011` 的两网络总事件只有 8 个，原始联合距离 0.14628，扣除样本内对角偏差后的分数 0.06152 使它进入提名。校正并不消除低事件数的高方差；这正是不能凭初筛分数选择可信局部中心的原因。按既定规则保留它的追加评估，并将初筛／八网络排名变化记入本轮复盘，不删除 seed 或放宽事件门槛。

本轮最小原始联合距离的新位置为 `replicated_r004_013`，但两网络成绩不足以证明它比此前八网络最佳参考好。扩大随机比例已实际带来新的候选位置，目前还不能宣称分布拟合或稳定性改善。

## 审计与衔接

`scripts/audit_topic4_xy_completed_screen.py --round 4` 已执行成功，重放前四轮提名并核验 48 个新 worker JSON、对应轨迹数组、阶段完成记录和快照哈希；1607 个冻结源码／输入条目通过检查。输出 `results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/004/screen_review.json` 保存数值及证据文件哈希。

方向仍是派生验证，未添加 core 角度或行为方向硬拟合项。第四轮初筛完成不代表八网络验证完成，也不代表获得合格基底；当前 Fig.5 尚未启动，Goal 继续。
