# 第六轮：首两个完整八网络候选

> 第六轮已完整结束，见[六候选完整分析及第七轮重放核验](joint_xy_v3_round6_complete_review_2026-09-06.md)。下文保留逐个完成时的阶段快照。

这是第六轮补算期间的两候选快照，不是完整轮次结论。其余四个候选仍在运行；提名、损失、网络、事件数门槛和后续规则均未修改。

## 已核验结果

| 候选 | 两网络事件数 | 八网络事件数 | 两网络联合误差 | 八网络联合误差 | 去对角偏差探索分数 | 原有验收 |
|---|---:|---:|---:|---:|---:|---|
| `replicated_r006_023` | 17 | 86 | 0.0500373 | 0.1045835 | 0.0970945 | 不通过 |
| `replicated_r006_013` | 15 | 61 | 0.0527256 | 0.0595421 | 0.0500154 | 不通过；事件数不足 |

`r006_023` 的两个圆心为 `(3.973, 13.357)`、`(7.523, 15.620)` mm。seed 2511–2518 事件数依次为 8、9、13、13、15、12、9、7。两个完整核、无 runaway、条件参与支持、事件数均通过；旧 D_support 汇总通过，但核参与分布、rank-space、timing-space、联合分布及方向均未通过。时序联合核距离 0.10570 明显偏高。上方位置有合法候选，但这个候选的两网络低误差不能外推为跨网络改善。

`r006_013` 的两个圆心为 `(4.336, 7.325)`、`(13.956, 6.931)` mm。seed 2511–2518 事件数依次为 10、5、10、5、7、8、9、7。两核完整、无 runaway、条件支持通过，其余原有分布检查仍不通过。61 事件低于固定最低 64 事件，因此不能作为合格基底，也不能按 v3 锚点规则引导局部细化。其原始联合误差低于此前训练锚点 `r005_020` 的 0.0702423，但不能凭这一点把它升为可用锚点；探索分数去除了对角均值偏差，仍未消除低事件数方差。

两个候选都是第六轮预设随机全域提案，不是根据补算结果移动过的位置。`r006_013` 恰好接近上一轮中下部位置不代表固定初始化；`r006_023` 明确位于上方。相应初筛提名已通过完整重放审计。

## 校准与结论边界

86 事件按下一较大校准网格 128 使用联合阈值 0.00838787；61 事件按 64 使用 0.01479791。两者联合误差均明显高于对应阈值。不能用不同样本量阈值下的相对接近程度排列几何优劣，也不放宽阈值补足事件数。

当前证据支持继续完成预定补算：一个初筛优势明显回落，另一个保留较低误差但仍不合格。尚不足以宣布 XY 模型容量不够、找到唯一双核位置或通过独立确认。这八个 seed 仍是共同训练网络，不是下一阶段的独立确认网络。

完成轮次后，原始场诊断服务将对六个候选分别生成全事件数值检查和按时间顺序预选示例 GIF；新增传播验收未完成前 Fig.5 自动链保持 hold。

## 证据与复现

工作目录：`/home/honglab/leijiaxin/HFOsp/.worktrees/topic4-substrate-autapse-fix`。

运行 `scripts/summarize_topic4_xy_replicated_completed_racers.py --round 6` 重新读取 worker JSON/NPZ、核验哈希和固定 seed 集，并重算八网络指标及原有门槛。输出为 `results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/006/completed_candidate_review/summary.json`，每个候选的完整计分缓存位于同目录下。

该 summary 后续可能增加已完成候选；本文保持本次两候选快照，不将尚未完成的候选补成结果。

## 后续更新：第三个候选完成

`replicated_r006_021` 随后完成八网络汇总。圆心为 `(3.183, 5.250)`、`(6.658, 9.270)` mm，seed 2511–2518 的真实事件数分别为 9、6、10、10、6、12、2、9，共 **64** 个。

初筛联合误差 **0.0810651**，八网络为 **0.0943344**；去对角偏差探索分数 **0.0839062**，未超过此前合格锚点集合中最好的训练分数。64 事件达到固定事件数下限，但联合误差仍为对应 q95 阈值 0.0147979 的约 6.37 倍。参与核距离 0.0371861、rank-space 0.0343389、timing-space 0.1067192，均未通过。旧 D_support 汇总通过不代表完整参与分布通过；D_order、D_lag 和方向检查也均未通过。

双核完整、无 runaway、条件支持和事件数检查通过，因此该候选在规则上可进入训练锚点候选集合；它并不因此成为合格基底，实际是否入选后续四个锚点仍由整轮完成后的排序与空间多样性规则决定。seed 2517 仅 2 个事件提示跨网络事件量变异；当前训练锚点规则没有每 seed 最低 5 事件门槛，不能把独立确认的门槛事后移入训练判定。

目前完整候选为三个、全部不合格，另三个候选继续计算。这是部分补算分析，不作整轮赢家选择或容量终判。

## 后续更新：第四个候选完成

`replicated_r006_019` 完成八网络核验。圆心为 `(4.447, 6.701)`、`(6.383, 11.662)` mm；seed 2511–2518 事件数为 4、7、13、7、10、10、3、8，总计 **62**，低于固定最低 64。

其联合误差由两网络的 **0.1078543** 降至八网络 **0.0906053**，去对角偏差探索分数为 **0.0816490**。改善后的联合误差仍约为对应阈值 0.0147979 的 6.12 倍；参与核距离 **0.1396582**、rank-space **0.1127367**、timing-space **0.1038741**，三项均不通过。旧 D_support、D_order、D_lag 和方向也均不通过；仅完整双核、无 runaway、条件支持通过。

因此该候选既不能冻结，也不满足 v3 训练锚点的事件量资格。这不是仅缺两个事件的问题：即使补足事件量，现有参与、顺序和时差不匹配仍需解决，不能把低事件数作为唯一失败原因。目前四个完整候选全部不合格，剩余两个继续补算。

## 后续更新：第五个候选完成

`replicated_r002_007` 是从此前两网络候选池按第六轮固定规则提名的旧候选，本轮首次补齐八网络。两个圆心 `(6.687, 9.972)`、`(13.814, 16.185)` mm 位于模型中上部。seed 2511–2518 的事件数为 8、7、9、10、14、9、9、10，共 **76**。

联合误差从两网络 **0.1051598** 降至八网络 **0.0704647**，去对角偏差探索分数 **0.0632982**。其原始误差接近此前 `r005_020` 的 0.0702423，但并未更低，探索分数也没有改善此前 0.0619027 的锚点最好值。联合误差仍为其对应 q95 阈值 0.00838787 的约 8.40 倍；参与、rank-space、timing-space 及全部旧分布/方向检查均未通过。完整双核、事件量、无 runaway 和条件支持通过，故它具备训练锚点的基础资格，是否入选四锚点需等整轮的排序和空间多样性检查。

这提供了一项有用的覆盖证据：中上部双核也能取得接近目前较低训练误差的表现，下方位置并非唯一有相近分数的区域。但两处都未达到患者分布标准，不能因此证明两种等价机制、定位真实双核或认定位置可辨识。短程排名有时回落、有时提高，支持继续使用完整共同网络结果而不挑两 seed 的最低分。

当前五个完整候选全部不合格，仅 `r006_014` 尚未完整结束；继续保留本轮未完成状态。
