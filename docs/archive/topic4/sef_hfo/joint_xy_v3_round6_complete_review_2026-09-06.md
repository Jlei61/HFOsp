# 第六轮完整检查：无合格双核，第七轮按预设规则继续

## 完整结果

第六轮 48 条初筛轨迹、36 条补算轨迹全部完成。累计探索 **299 个不同几何位置**；本轮 6 个候选均已获得同一固定 seed 集 2511–2518 的结果，**0/6 通过验收**。没有进入独立网络确认，也没有冻结模型。

| 候选 | 八网络实际事件数 | 原始联合误差 | 去对角偏差探索分数 | 最低事件数通过 | 总体验收 |
|---|---:|---:|---:|---|---|
| `r006_023` | 86 | 0.1045835 | 0.0970945 | 是 | 不通过 |
| `r006_013` | 61 | 0.0595421 | 0.0500154 | 否 | 不通过 |
| `r006_021` | 64 | 0.0943344 | 0.0839062 | 是 | 不通过 |
| `r006_019` | 62 | 0.0906053 | 0.0816490 | 否 | 不通过 |
| `r002_007` | 76 | 0.0704647 | 0.0632982 | 是 | 不通过 |
| `r006_014` | 71 | 0.0838943 | 0.0759253 | 是 | 不通过 |

候选完整 ID 均有 `replicated_` 前缀。原始联合误差用于验收，探索分数只用于搜索排序；去对角项不等于消除有限事件数方差。4 个候选通过事件数要求，6 个均通过完整双核、无 runaway、条件支持，但 **6 个均未通过参与核、rank-space 核、timing-space 核及联合核检查**。旧 D_support 汇总只有 `r006_023` 与 `r006_021` 通过，这不能覆盖完整参与分布的失败。

最后完成的 `r006_014` 圆心为 `(2.404, 4.735)`、`(9.218, 9.827)` mm；八个 seed 的事件数依次为 7、7、12、10、6、7、8、14。其联合误差较初筛 0.1134874 降为 0.0838943，但仍为对应验收阈值的约 10 倍，参与、rank、时滞和方向均不匹配。

## 为什么没有找到合格解

**不是一直用同一个固定起点。** 第六轮有 24 个新随机位置，本轮完整候选覆盖上方、中上方、左侧及中下方区域。中上部 `r002_007` 的联合误差已接近此前中下部候选 `r005_020`，说明相近误差并非下方位置独有；但都不合格，不能宣称空间解唯一或已定位真实病灶。

**短程排名仍存在明显波动。** `r006_023` 从 0.0500 回落为 0.1046，而 `r002_007` 从 0.1052 改善为 0.0705。因此不能凭两网络最低分选定工作点。原始误差更低的 `r006_013` 只有 61 事件，且所有分布检查仍失败；不能通过挑 seed、放宽 64 事件规则或只补足计数将其升级为成功。

**误差涉及整个事件分布。** 平均 rank 曲线较接近并不代表每个事件的参与和顺序组合正确；本轮六候选的三类核指标均失败。患者全窗 lagPatRaw、GIF 主增强区质心与模型 firing-density 质心的差别，及 readout 可能掩盖的全场多团活动，已在[原始传播检查](joint_xy_raw_propagation_first_review_2026-09-06.md)中分别保留。不能只靠调整方向让图变得顺眼。

当前证据增加了 VTH-only 模型或观察层容量不足的疑虑，但仍不足以证明所有 XY 都不可能拟合。继续执行已锁定的第七、八轮，之后依预设预算做容量复审；不把工程运行完成当成科学成功。

## 第七轮已启动且完全重放一致

第六轮后当前训练锚点最优仍为 `replicated_r005_020`：70 事件、原始联合误差 0.0702423、探索分数 0.0619027。它仍未通过患者分布要求，这里的“最优”仅指具备搜索锚点资格的已评估训练候选。

第七轮实际四个局部锚点为：

1. `replicated_r005_020`
2. `replicated_r002_007`
3. `xy_whole_sheet_062`
4. `replicated_r002_014`

预设随机序列 `[2395812204, 7, 0]` 生成 **24 个唯一位置：7 个全域随机、17 个局部随机**。全域随机选择概率为 0.5，不等于几何合法性筛选后必须保留 12 个全域位置的配额；没有修改种子去追求更有利的计数。第六轮的完整评估、所有提名、四锚点及第七轮全部坐标均精确重放一致。

第七轮为 48 条两网络短程轨迹，随后按相同规则补算六候选。仍未打开新的患者 held-out 数据或发作数据，方向不是拟合项。

## 工程与图像核验

- `audit_topic4_xy_round_transition.py --round 6` 通过：本轮完成记录、worker JSON/NPZ、八 seed 唯一性、评估、历史锚点、第七轮位置、提名全部核验，**1607 项输入锁一致**。
- 本轮六行诊断图 PNG 与渲染 PDF 均已目视查看，PNG/PDF/SVG 哈希已核验并写入 `figures/visual_qa.json`。作者验收仍为 false，模型资格不变。
- 图中 N=61/62/64 与 N=71/76/86 使用不同样本量校准档，不能用右侧除以阈值后的柱高直接给几何排序；平均 rank 线也不能替代联合分布。
- 原始场检查服务已实际启动 `r006_023` 的诊断，随后串行处理其余五候选。它输出全部事件的数值检查和固定时间顺序示例 GIF，自动完成不等于目视通过，更不释放 Fig.5 hold。
- 当前 XY worker、目标函数和模型参数未改；没有合格模型，后续 E→E/E→I 或 Fig.5 机制扫描尚未启动。

## 证据位置

工作目录：`/home/honglab/leijiaxin/HFOsp/.worktrees/topic4-substrate-autapse-fix`。

```text
results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/006/analysis.json
results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/006/round_transition_review.json
results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/006/combined_*.json
results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/006/figures/
results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/007/
results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/raw_propagation_audit/
```

此前[逐个候选完成时的分析](joint_xy_v3_round6_first_complete_candidates_2026-09-06.md)保留为阶段快照；完整轮次状态以本文和本轮 `analysis.json` 为准。
