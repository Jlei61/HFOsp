# 第七轮已完成八网络候选：阶段记录

完整第七轮结论已形成，见 [整轮完成审查](joint_xy_v3_round7_complete_review_2026-09-06.md)。下文保留逐步收齐数据的历史记录。

首次记录包含最先收齐六个追加 seeds 的两个候选；后续完成候选按时间追加在文末。两候选均按原始 seed 2511–2518 重新读取并合并实际事件，没有复制事件补足样本，也没有改变提名、损失或资格门槛。本阶段是预设训练网络展开，不是独立确认。

| 候选 | 初筛事件数 → 八网络事件数 | 初筛联合误差 → 八网络联合误差 | 八网络探索分数 | 结论 |
|---|---:|---:|---:|---|
| joint_r001_006 | 16 → 74 | 0.151544 → 0.076889 | 0.069006 | 事件量达标，患者分布未通过 |
| replicated_r007_008 | 25 → 67 | 0.068464 → 0.077738 | 0.068869 | 事件量达标，患者分布未通过 |

## r007_008：初筛优势没有稳定保留

八网络各有 12、13、7、3、11、3、8、10 个事件，共 67 个，达到训练合并样本的 64 事件要求。联合误差由 0.068464 升为 0.077738，探索分数 0.068869，也没有优于已展开训练最优 r005_020 的 0.061903。两个网络只有 3 个事件需要保留记录，但训练阶段没有新添加“每 seed 至少 5 个”的门槛；那是独立确认阶段的既定规则，不能移来事后淘汰训练候选。

参与、rank-space、timing-space 和 joint kernel 距离分别为 0.068648、0.045297、0.085192、0.077738，均超过固定校准门槛。D_order=0.040689、D_lag=14.926 ms、direction_distance=0.166371，也未通过对应验收。虽然部分顺序/方向指标较旧训练最优改善，联合分布没有同时改善，仍不能冻结。

## joint_r001_006：展开后明显改善，但仍不合格

八网络实际事件数为 10、6、13、6、8、9、14、8，共 74 个。联合误差从 0.151544 降至 0.076889，说明它初筛的坏分数没有在增加网络后原样保留。它因预设 rank-space 分量保留规则得到展开，而非根据新增网络结果事后补选。

参与、rank-space、timing-space 和 joint kernel 距离分别为 0.066944、0.052749、0.077584、0.076889，仍全部超过门槛；D_order=0.043633、D_lag=15.999 ms、direction_distance=0.239037。完整核心、无 runaway 和实际事件量达标并不等于患者传播分布已恢复。

## 当前判断与后续

两个候选的初筛误差变化方向相反，进一步支持必须做共同 seeds 展开，不能挑两个网络宣称找到最优。两个候选均用 128 样本档的固定校准上界（联合距离 0.00838787），并非因为事件数不足而未通过；数据分布偏差仍是主要障碍。

另外四个候选继续原定计算。没有根据这两例提前更改初始化、优化权重或宣布当前模型容量不足；整轮结束后再更新搜索历史和下一轮起点。独立网络确认及 Fig.5 均未触发。

本阶段不可变快照：`results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/007/completed_candidate_review/two_candidate_snapshot.json`，SHA256 `4a4637634a632584caeffe0fc5639b6ddaaae9240756b812b93e293654285094`。
逐候选输出：本目录的 `joint_r001_006.json` 和 `replicated_r007_008.json`；复现使用 `scripts/summarize_topic4_xy_replicated_completed_racers.py --round 7`。

## 第三个完成候选 r007_009：训练分布进一步改善，时滞仍是主要缺口

该候选八个 seeds 的实际事件数为 **5、5、9、4、14、10、10、12**，合计 **69 个**。初筛只有 10 事件，原始联合误差 **0.092874**；展开后降为 **0.065655**，探索分数 **0.057532**。这次低样本量下的探索提名确实找到了一个值得保留的训练候选，不能把先前低事件量本身当成探索规则无效的证明。

与既有参考 r005_020 的八网络结果作同层比较：

| 指标 | r005_020（70 事件） | r007_009（69 事件） |
|---|---:|---:|
| 联合误差 | 0.070242 | 0.065655 |
| 探索分数 | 0.061903 | 0.057532 |
| D_order | 0.060144 | 0.033163 |
| D_lag (ms) | 15.265701 | 15.253614 |
| D_support | 0.127704 | 0.130761 |
| direction_distance | 0.226747 | 0.185297 |

顺序和方向距离有改善，时滞几乎不变，D_support 略差；不能概括成所有数据层面同时改善。三个分量 kernel 距离为 support **0.058025**、rank-space **0.045056**、timing-space **0.071340**，也仍全部超过固定门槛。D_support 与 support kernel 定义不同，应分别报告，不能选其中改善者代表整体参与分布已经恢复。

它的实际事件量达到要求，完整双核和无 runaway 项通过，但患者分布的原有全部验收项仍失败。联合误差约为当前固定门槛 0.00838787 的 **7.83 倍**。因此，这项结果支持继续在有改善的区域开展规定的搜索，不支持冻结模型、进入独立确认，或用动画相似度越过患者分布门槛。

当前仍有三个候选未完成，整轮控制器尚未更新下一轮起点。本文只记录已完成候选的改善，不提前选择整轮胜者、不修改正在运行的计划。三个候选快照保存在 `rounds/007/completed_candidate_review/three_candidate_snapshot.json`；首次两个候选的快照仍保留。

三个候选快照 SHA256：`bfebf9e496c3b0625af809ec745815f5cc398a581f81cba00a1b7b63e3ba3bb8`。

## 第四、第五个完成候选：上方位置与另一个中下位置

两候选均使用相同的八个网络 seeds，均满足合并事件量和完整双核要求、无早期 runaway，但患者分布的全部原有验收项失败。

| 指标 | 上方 r007_010 | 中下 r007_005 |
|---|---:|---:|
| 实际事件数 | 88 | 77 |
| 初筛联合误差 | 0.072533 | 0.082021 |
| 八网络联合误差 | 0.080189 | 0.069507 |
| 八网络探索分数 | 0.073291 | 0.062717 |
| D_support | 0.183105 | 0.167526 |
| D_order | 0.047461 | 0.030345 |
| D_lag (ms) | 17.363629 | 13.877954 |
| direction_distance | 0.189601 | 0.210605 |
| support kernel | 0.100966 | 0.119652 |
| rank-space kernel | 0.077958 | 0.089033 |
| timing-space kernel | 0.089104 | 0.073175 |

r007_010 八网络的事件数为 9、11、13、13、10、10、8、14。它不是因事件不足而失败；该上方放置方案没有在完整展开后提供更好的总体拟合。这只评价这一个位置，不能外推成所有上方位置都不可能合格。

r007_005 八网络事件数为 9、4、11、10、10、10、12、11。相对本轮已完成候选，它的 D_order 和 D_lag 较低，但参与和 rank-space kernel 不佳；D_order 与 rank-space kernel 不同，不能把一个聚合顺序指标改善写成整个 rank 分布都改善。其联合误差虽略低于旧参考 r005_020，探索分数仍未优于旧参考，更未优于 r007_009。

截至本次更新，五个已完成候选实际事件数都达到要求，患者分布仍全部未通过。这进一步将当前问题定位在生成分布的偏差，而非仅仅缺少事件数；但最后一个候选 r007_023 仍在计算，完整本轮结论及下一轮起点尚待控制器统一生成。

新增不可变快照：`rounds/007/completed_candidate_review/five_candidate_snapshot.json`。此前两个、三个候选的快照保留，不覆盖历史阶段证据。

五候选快照 SHA256：`5c0a1be72f3c8c5c1ce48a5fb16d66cbe183143b2a1484f39a04831e41a5bc7f`。
