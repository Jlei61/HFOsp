# 执行 Agent prompt：当前 Fig.5 网络的 Z 状态边界里程碑

以下正文可直接复制给执行 Agent。本文件当前仅为任务文本，尚未派发或运行。

---

请在 `/home/honglab/leijiaxin/HFOsp` 完成一个有界科研里程碑：**当前 Fig.5 网络的 Z 依赖状态图，以及匹配近似模型解释同一状态边界的资格检验**。完成后交付审阅包并停止，交回原设计任务审阅。不要只给计划、只启动进程或只返回一个fold数值。

先完整阅读并遵循设计合同：

`/home/honglab/leijiaxin/HFOsp/docs/archive/topic4/fig5_current_network_z_state_milestone_design_2026-09-15.md`

同时阅读适用AGENTS.md、当前共享科学口径和设计列出的已有近似失败报告。本文与详细合同不一致时，以详细合同为准；本次用户后续明确修订优先。没有新授权不要扩大合同预算或进入下一里程碑。

## 科学目标

我们要解释：同一个网络中，Z的空间耗竭到什么阶段，自限burst变为持续活动；原图③附近是否能与当前近似网络的动力学边界对应。原SNN状态改变、确定性分岔和随机进入时刻是三个证据层，分别验收。不预设saddle-node，不拟合旧临界值，也不以复现某个漂亮分支作为目标。

## 唯一参考

- 指定图：`/home/honglab/leijiaxin/HFOsp/results/topic4_sef_hfo/fig5_preentry_event_audit_20260914/clean_panels_v2/review_square_complete_20260915/figures/fig5.pdf`
- 其元数据：同目录上层的 `fig5_metadata.json`。
- 主轨迹：`/home/honglab/leijiaxin/HFOsp/results/topic4_sef_hfo/fig5_preentry_event_audit_20260914/runs/eta0.0005_s9108401/`
- 第二个已看过的源噪声：同级 `eta0.0005_s9108402/`。
- 实际协议：`/home/honglab/leijiaxin/HFOsp/results/topic4_sef_hfo/fig5_preentry_event_audit_20260914/protocol.json`；各运行的 `applied_configuration.json`、`result.json`与保存源码确定真实身份。

固定topology6101、32000E+8000I、manual_hard物理半径1.5 mm双核、当前C快速连接、实际阈值/延迟/OU/Poisson。E阈值应为781降低、31219相等、0升高。M为eta=.0005、tau=1 s；Z为tau=5 s、E靶端、原电流阈值95.19851312666987。读出1.75 mm邻域不能替代物理核。

①1.235 s、②4.025 s、③9.420 s、④10.370 s。③是最后静息结束，高率起点9.870 s、确认10.070 s。不能将后续其他任务的新网络或新Z方程换进来；也不能沿用9月4日topology2542的分岔结论。

## 执行顺序与科学门槛

1. **重建与补录**：先核对真实身份。缺逐细胞Z/M和完整checkpoint时，准确回放两个原轨迹各≤12.5 s，补录场和输入，核对共同覆盖的原生观察。不能用区域均值捏造空间场；记录新输出但不修改旧轨迹。错误先修复，不放宽一致性要求。
2. **原生Z状态图**：按合同6个源场（8、9、9.3、9.42、9.87、10.37 s）×2完整历史（8/10.37 s）×2未来随机创新，完成24条10 s冻结Z、动态M续接。保持每个历史内M初值、快状态与未来输入相同；未来OU初态统一。冻结只停止状态更新，仍保留Z/M对电流的作用，不能用关闭use_z/use_m替代。旧历史移植时完整保留延迟事件。统计完整事件、静息、局部持续空间占据和高率终点，使用合同判据；不把细胞或事件当独立网络。
3. **必要延长及M对照**：仅依合同规则选最早一对承重/未解决的相邻Z场，最多8条从原末状态延至20 s一次；若仍存在一致自限/持续两侧，再做8条固定Z+M交叉对照。没有边界就报告未夹住，不向外扫描。
4. **当前近似模型**：从实际图和manual_hard阈值重建，保留靶端Z、联合异质性、滤波、时延与动态M。先检查动态响应，必要时执行合同的最多60条小集合检验。至多初版和一次有具体误差依据的数学修正，不改SNN物理参数。旧C近似曾将原生自限侧误判为持续，不能视为已验收。
5. **对应资格**：先用W1诊断，冻结版本后读取W2评分；近似不得按每个Z点挑最有利初态。对两侧逐配对检查类别、静息、空间持续占据、区域率、事件时长与延长/分辨率敏感性。数值阈值严格按合同，不按结果放宽。两个源轨迹的真实Z(t)回放单独检验①–④对应，不能称自主Z预测。
6. **条件性分岔分析**：只有对应资格通过才进入。主图沿真实空间Z路径，保留明确λ映射，不用旧对称s路径，不自动扩成多维大扫。先核查确定性近似与OU-on状态的关系；完整谱须包含突触、时延和M。saddle-node需真实折返、简单零模、横截和曲率；Hopf型需复模过界及其他根核查。不能从静态Jacobian或一条短轨迹宣布稳定性、极限环、SNIC或crisis。数学证据不足时写NOT_ESTABLISHED。

## 批次和范围

原生：最多2条12.5 s准确回放、24条10 s冻结Z、8条10 s固定Z+M，以及最多8条已有轨迹再延长10 s。另有最多60条2 s、2048细胞的小集合诊断。近似每版本最多48个完整轨迹/延长单元、最多2版本；平衡支最多400校正点、6个承重谱工作点。详细组成与触发条件见合同。

默认最多4个重型进程，保留64 GiB可用内存并考虑现有任务资源；不停止其他进程。先写实际job表、应用核对和耗时估计。不要擅自另开Codex任务或派发子Agent。保护未提交文件、冲突与旧输出；用新的结果目录和独立脚本完成可逆工作。

近似失败时，继续完成原生状态图和失败诊断，不用错误近似生成正式机制结论；无边界或分岔未定型也应交付具体反例和已测边界。不能通过追加版本、换网络、换噪声、改门槛或新生物变量争取成功。

## 交付、停止与交回审阅

输出根：`/home/honglab/leijiaxin/HFOsp/results/topic4_sef_hfo/fig5_current_network_z_state_v1/`。

按合同交付manifest、真实方程与降阶假设、每条运行结果、状态图、逐配对近似评分、条件性的分岔证据、PNG/PDF及中文图说明、`scientific_report.md`和`review_packet.md`。图要Agent目检，但标为待用户人工检查；不覆盖指定Fig.5、不冻结患者模型。

审阅包首先回答：网络是否一致；Z是否控制两侧；M/历史是否改变解释；近似是否通过同一边界；③是否真正映射到该边界；分岔类型证据是否充分。逐层给PASS/FAIL/NOT_ESTABLISHED/NOT_ESTIMABLE/NOT_RUN_DEPENDENCY，不用一个总PASS掩盖失败。列明完成量、失败量、延长、仍活跃进程、图件和决定性反例。

完成所有已派发轨迹及必要收尾后停止。最终回复给出`review_packet.md`、科学报告、关键图的绝对路径，以及验收建议和未解决的一条最关键联系。明确写“交回原设计任务审阅，未自动开启下一里程碑”。你不能代表原设计任务完成独立科学审阅，也不能代表用户做人工图形验收。
