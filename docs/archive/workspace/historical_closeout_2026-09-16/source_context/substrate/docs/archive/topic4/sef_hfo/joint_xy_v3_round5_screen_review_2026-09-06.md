# v3 第五轮初筛：出现较低联合距离，等待八网络检验

第五轮 24 个新几何、48 次 8 秒模拟全部完成，累计评分池为 275 个不同位置。本轮新提案使用随机序列 `[2395812204, 5, 0]`，实际 17 个全局随机、7 个局部随机，已在上一轮衔接审计中完整重放。新候选的两网络总事件数为 8–24，没有 early runaway。以下只是初筛，不能据此冻结双核或进入 Fig.5。

## 新候选与扩展名单

| 预设提名候选 | 两网络事件数 | 原始联合距离 | 修正搜索分数 | 提案来源 |
|---|---:|---:|---:|---|
| `replicated_r001_015` | 18 | 0.105498 | 0.066204 | 第一轮全局随机，rank 分量探索 |
| `replicated_r005_020` | 17 | 0.055627 | 0.025023 | 本轮局部随机，时滞分量探索及低联合距离 |
| `replicated_r005_017` | 12 | 0.090533 | 0.038299 | 本轮全局随机 |
| `replicated_r005_015` | 17 | 0.092630 | 0.058886 | 本轮局部随机 |
| `replicated_r005_002` | 21 | 0.088700 | 0.063037 | 本轮局部随机 |
| `replicated_r004_003` | 20 | 0.096682 | 0.064115 | 第四轮局部随机 |

四个提名来自本轮、两个来自既往尚未扩展的候选池。名单由完整池和预设分量探索、修正分数及空间多样性规则产生；不是只取本轮分数前六。每个候选追加相同六个网络 2513–2518，与原 2511、2512 一起组成八网络检验，共 36 次新模拟，已自动启动。

`replicated_r005_020` 是本轮原始联合距离及修正搜索分数最低的新候选。双核中心为 (4.3059, 7.9893)、(13.2694, 7.6448) mm，相距 8.9701 mm，中心连线角 -2.2006°，相对固定 E→E 长轴 -22.8054° 的轴差约 20.60°。1499 个 VTH 节点分为 733、766 个，两圆盘互不相交且完整，边界净距约 2.61、2.56 mm。因此它是一个平面中部左右分开的候选，但其角度没有被损失强行拟合。局部随机提案来自第四个完整参考 `replicated_r001_003` 周围，并非复用同一中心。

该候选两个网络分别只有 12、5 个事件；参与、rank、时滞核距离分别为 0.078309、0.058044、0.066433，旧顺序摘要 D_order=0.097457、时滞摘要 D_lag=11.6525、派生方向距离=0.187207。原始联合距离 0.055627 低于当前完整八网络参考的 0.071579，但两者网络数和实际事件数不同，不能直接宣布跨网络改进。第四轮已经观察到低事件数修正分数的优势在扩展后消失，本候选必须经过相同检验。较低的联合距离也不替代每个分量的固定验收。

## 当前判断与复核

本轮初筛仍可能找到新的低分位置，所以暂时不能把前三轮停滞解释为“XY 优化完全没有改进空间”。但结果尚不支持一个稳定且符合患者数据的固定双核，必须等完整八网络事件分布。接下来比较修正分数是否保留、参与和 rank/时滞残差是否同时缩小，以及事件数、完整 core、无 runaway 等条件是否满足；验收失败时按既定规则继续，不改患者阈值、不挑 seed。

`scripts/audit_topic4_xy_completed_screen.py --round 5` 执行成功，核验 48 个新 worker 输出与轨迹哈希、完成记录、全部提名重放和 1,607 个锁定输入。损失未变，患者留出数据未打开。本记录不修改运行中的搜索，不作独立确认或模型容量结论。

证据：`results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/005/{design.json,scores.json,race_nomination.json,screen_review.json}`；实际模拟在同根 `execution/race_005/`。
