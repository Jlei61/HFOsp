# v3 第三轮初筛与追加网络衔接

第三轮 24 个新位置、48 个初筛任务全部完成，累计探索 227 个不同位置。每个新位置在两个共同网络中得到 12–29 个实际事件，未检测到 early runaway；这些短时事件数不能代替八网络稳定性验证。

## 提名与当前判断

控制器从累计尚未完成八网络评估的候选中提名六个，而非强制只从本轮选择。按冻结规则重放前三轮提名，均与保存结果完全一致。

| 候选 | 初筛事件数 | 联合距离 | 修正搜索分数 | 来源 |
|---|---:|---:|---:|---|
| `joint_r001_022` | 19 | 0.10437 | 0.06978 | 历史池，rank 项提名 |
| `xy_interior_007` | 24 | 0.08555 | 0.06158 | 历史池，时滞项提名 |
| `replicated_r003_003` | 17 | 0.07678 | 0.04570 | 本轮新位置 |
| `replicated_r001_003` | 14 | 0.10260 | 0.05805 | 第一轮候选 |
| `replicated_r002_009` | 17 | 0.09518 | 0.05834 | 第二轮候选 |
| `xy_whole_sheet_040` | 18 | 0.09264 | 0.05965 | 历史池 |

本轮仅一个新位置进入追加评估，不能据此认定全局随机搜索无用：本轮筛选噪声仍大，而且提名会同时保留已有候选。新候选 `replicated_r003_003` 的中心为 (4.386, 5.322)、(7.646, 10.418) mm，两个网络的最小核心边界余量分别为 2.640、2.670 mm，未发生核心越界。它来自多中心局部随机探索；其两网络分数不能直接宣称优于此前八网络的最佳参考。

## 衔接与可复现性

六个候选已自动进入各六个追加网络（2513–2518），共 36 个任务；实际新 worker 进程已核验。追加后才汇总每个候选的八个共同网络，判断事件数量、完整分布和后续独立确认资格。当前仍无可冻结基底，尚未启动 Fig.5。

新增只读审查入口 `scripts/audit_topic4_xy_completed_screen.py --round 3` 已运行成功，重放提名并核验 48 个 worker JSON、轨迹数组哈希、阶段完成记录与运行快照。1607 个冻结源码／输入条目核验通过。报告位于 `results/topic4_sef_hfo/joint_rank_space_dual_core_search_v3/rounds/003/screen_review.json`，含证据文件哈希；未改变损失、参数、阈值或患者留出边界。
