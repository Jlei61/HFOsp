# Topic 4 rev10-D4.1 execution plan

1. Freeze the two source coordinates, four packet fractions, fresh seeds, input hashes, classifier, detector, and decision rule in one manifest.
2. Run one sentinel network under a managed `systemd-run --user -> nohup` unit and parse peak RSS.
3. Use at most half of available memory and no more than six one-thread workers for the remaining networks; wait 180 seconds between status checks.
4. Aggregate only complete, hash-matched worker JSON/NPZ pairs. Report per-dose A and B expected-mode coverage, OOD, joint-shaft recruitment, return, and runaway.
5. Freeze the smallest dose with at least `5/6` clean A and `5/6` clean B responses. Send a desktop notification after aggregation.
6. If confirmed, use the lower-bound packet scale to design a translationally invariant continuous accessibility mechanism. Do not place a new field/core at the selected A coordinate and do not replace Fig.4 with forced responses.
