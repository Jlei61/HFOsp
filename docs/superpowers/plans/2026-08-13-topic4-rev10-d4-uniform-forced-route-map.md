# Topic 4 rev10-D4 execution plan

- [x] Close D3 without parameter interpolation or Fig.4 replacement.
- [x] Freeze an observation-invariant `5 x 5` source grid and paired-sham
  decision rule.
- [x] Implement per-network sham plus 25 deterministic forced sources.
- [x] Verify pre-trigger bit identity, injected-frame exclusion, contact order,
  frozen Node hash and patient-classifier provenance.
- [x] Aggregate by identical source location across three network seeds.
- [x] Produce a diagnostic Node-field/source map with separate A and B support
  panels and a Chinese figure README.
- [x] Run a measured-RSS sentinel, then remaining workers via managed nohup with
  180 s waits and completion notification.
- [x] Observe shared forced A at `(18,6) mm` in 3/3 networks and shared B at
  three separate positions; classify the current gap as nucleation/accessibility
  rather than total route absence.
- [ ] Freeze a fresh-network packet-dose confirmation at the selected A source
  and matched B source before designing a nucleation-specific mechanism.
