# Topic 5 / Figure 6 v0.3 execution delta

1. 冻结并归档 symmetric-only v0.2 diagnostic，不继续 306-unit run。
2. 实现 shared rank-2 symmetric/skew operators 与 causal source-direction state；补解析单元测试。
3. 三位 development patients 只用 interictal validation20 做三学习率短审计，冻结一个值。
4. 在新结果根运行 34 × 3 seeds × structured/ordinary/rank-shuffle；保持 target sealed。
5. 汇总 test20 contact-choice endpoint；同时报告 31 人确认与 34 人描述。
6. 用 structured seed-ensemble shared scaffold 定义两端 source pools；structured 与 ordinary 共用，
   exact-k rollout 后冻结唯一 first-arrival earliness fields。
7. manifest 写完后才读取 15 人 primary + E1146 supportive early-ictal target。
8. 生成 Figure 6 A--E、source-data CSV、中文 README，并逐张目视 QA。
