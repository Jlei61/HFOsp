# Topic 4 Phase 2 H4 v1.1 — Rank-based endpoint geometry drift (Stage B)

> **Origin**: 2026-05-23 user-return catch — H4 v1.0 用 `compute_local_endpoint(events_bool.mean)` 取 participation field top-k/bottom-k，不是 propagation rank endpoint。当前 PASS 信号 (p=9.5e-7, d=1.50) 改口径为 supplementary "participation field stability"；主线 H4 重做。
>
> **Status**: B0 plan，advisor checked，pending B1-B6 execution.
> **Framework target**: v1.0.6 with Phase 2 完结 marker.
> **Module target**: `src/sef_itp_phase2.py` v1.1.0 (bump from v1.0.0).
> **Cohort**: n=23 (Phase 1 cohort) for main H4; n=9 (strict ∪ candidate swap_class) for decision-k drift.
> **Hypothesis tier**: H4 = primary cohort claim — SEF-ITP 区分性预测之一（"rate ≫ geometry"）。

---

## 0. 计科学合同：要测的是什么

朴素话三段式（CLAUDE.md §8）：

- **测了什么**：把每个录制切成 30 分钟的小段，看 **传播 endpoint 通道集合**（哪些通道**最先点火**，不是哪些通道**经常被激活**）在不同 epoch 间漂得多厉害；同时看这些 endpoint 通道在脑里**占多大空间**（centroid RMS / mean pairwise / min enclosing ball）漂不漂；最后对有 swap 信号的 subject，看**核心病理区的通道数量**（decision-k）在不同 epoch 间是否变化。SEF-ITP 区分性预测：rate ≫ geometry，rank-based endpoint identity 稳，空间半径稳，decision-k 稳（病理空间结构不进展）。
- **怎么测的**：per epoch 把该 epoch 内事件按 cluster 分组，对每 cluster 算 **masked mean rank**（只在 `bools=True` 的事件上算，phantom int rank 排除），top-k 通道（mean rank 最低）= source，bottom-k 通道（mean rank 最高）= sink。然后算 4 个 spatial radius 指标（source / sink 分开算），跟 global PR-6 anchoring 算 Jaccard。9 subject 的子集还要 per-epoch 重跑 `rank_displacement.compute_swap_score_sweep` 取 `decision_k`，看 std。
- **揭示什么**（pre-registered）：
  - **PASS**: rate Wilcoxon p<0.05 + Cohen's d ≥ 0.3 vs rank-endpoint Jaccard drift；spatial radius drift 同方向；decision-k 稳。
  - **NULL**: rate / geom drift 同尺度。
  - **FAIL**: geom > rate drift 显著反向。

---

## 1. 合同（CLAUDE.md §6 多子句不变量审查）

下表对每个新函数列出 prose 不变量，避免 §6 五大陷阱。

### 1.0 Canonical helper — `_per_cluster_template_rank(ranks, bools, cluster_evt_idx)` — shared B1 + B3

> **2026-05-23 user-review correction** (was: `_masked_mean_rank` with arithmetic mean):
> Calibration with PR-6 anchoring requires the **same template estimator PR-6 uses**
> (`_legacy_hist_mean_rank` 3-bin modal histogram from `src.interictal_propagation`),
> not a simple masked arithmetic mean. The simple-mean version produced 21/23 mismatch
> on cohort calibration before refactor (different statistic = can't reproduce PR-6).
> After refactor with the canonical helper below + per-cluster valid_mask: 0/23 mismatch.

```python
def _per_cluster_template_rank(
    ranks: np.ndarray,
    bools: np.ndarray,
    cluster_evt_idx: np.ndarray,
) -> Tuple[np.ndarray, np.ndarray]:
    """Per-cluster template rank using PR-2's legacy estimator (same as PR-6 consumes).

    Returns (template_rank, valid_mask) per channel:
      - template_rank: argsort(argsort(_legacy_hist_mean_rank(masked_ranks))) — int 0..n_ch-1.
        Same estimator PR-2 stores in adaptive_cluster.clusters[k].template_rank.
        `_legacy_hist_mean_rank` already filters by `bools[ci] > 0` per channel AND drops
        NaN via `vals = vals[np.isfinite(vals)]` (NaN-safe by construction).
      - valid_mask: per-channel "any participation in cluster's epoch events"
        — mirrors PR-6 anchoring runner's `per_cluster_masks` (cluster_bools.any(axis=1)).
    """
    n_ch = ranks.shape[0]
    if cluster_evt_idx.size == 0:
        return np.zeros(n_ch, dtype=int), np.zeros(n_ch, dtype=bool)
    cl_ranks = ranks[:, cluster_evt_idx]
    cl_bools = bools[:, cluster_evt_idx]
    template = _legacy_hist_mean_rank(cl_ranks, cl_bools)
    template_rank = np.argsort(np.argsort(template, kind="stable"), kind="stable")
    valid_mask = cl_bools.any(axis=1)
    return template_rank, valid_mask
```

(Old `_masked_mean_rank` removed 2026-05-23 — was dead code after refactor; had a latent
NaN bug `cl_ranks * cl_bools` would propagate NaN for missing-block padding. The canonical
helper above goes through `_legacy_hist_mean_rank` which is NaN-safe.)

### 1.1 `compute_local_rank_endpoint(ranks, bools, labels, k, valid_mask)` — B1

| # | 不变量 | 测试方法 | 失败后果 |
|---|---|---|---|
| C1 | mean rank 必须 **mask** by `bools=True`，phantom int rank 不能进 mean | TDD test: 注入 phantom int rank (events 该通道 bools=False) → mean 必须忽略它 | 测的是 phantom + 真 rank 混合，silent science contamination |
| C2 | 通道 0 参与（cluster events 中没有任何 bools=True）→ **excluded**，不能 NaN sort 到 top/bottom | TDD test: 给 1 通道全 False bools → 输出 source/sink 不含该通道 | NaN 排序到极端 → 该通道被错误判为 endpoint |
| C3 | `valid_mask` filter 强制 — 即使 mean rank 低的通道，若 `valid_mask=False` 也排除 | TDD test: valid_mask=[T,T,F,T,...] + 通道 2 mean rank lowest → source 不含通道 2 | unmapped channel 进 spatial radius 计算 → coords lookup KeyError 或错坐标 |
| C3b | **external mask 必须与 derived_valid 取 INTERSECTION**（不是 replace）— PR-6 full-data per-cluster mask 不能替代 epoch-derived 参与 mask；否则 epoch 没参与的通道经 `_legacy_hist_mean_rank` 的 `template[ci]=ci` fallback 仍可能被选成 source/sink | 2 TDD tests: (a) external is SUPERSET of derived → derived 决定 (intersection 排除 epoch-absent channels); (b) external 进一步收紧 derived → intersection 生效 | runner per-epoch endpoint 被 full-data 池子污染 → silent participation-field re-introduction; user-review catch 2026-05-23 — 修复后 production runner 不传 external mask, 让 derived_valid 直接生效 |
| C4 | pool < 2k → k 退化到 `max(1, pool // 2)`，输出 source/sink 大小一致 | TDD test: 5 个 eligible 通道 + k=3 → k_eff=2，输出 source=2 sink=2 | source / sink 不等长 → 下游 Jaccard 不可比 |
| C5 | **calibration**: `compute_local_rank_endpoint(full_dataset_as_one_epoch)` 必须复现 PR-6 anchoring `per_template[].source/sink`（per cluster） — **k 从 PR-6 实际输出长度推（不 hardcode k=3）**，以兼容 PR-6 在小 valid pool 下产出 < 3 endpoint 的 subject | TDD test: 实数据 yuquan_chengshuai full epoch → 对比 `_global_endpoint_from_pr6` 输出 set-equality per cluster，k_per_cluster = len(pr6.source) | per-epoch 测的是 impl bug 不是 geometry drift |
| C5b | **cohort calibration gate**: B1 写完后立即跑全 cohort 23 subject 的 B1(full-data) vs PR-6 set-equality 检查，写 `results/topic4_sef_itp/phase2_temporal_x_geometry/diagnostics/b1_calibration_<date>.csv`（columns: subject, cluster_id, b1_matches_pr6, n_source_b1, n_source_pr6） | 跑一个 CLI 脚本 | ≥2 subject 失败 → **STOP** debug channel-ordering / masking 前不进 B2/B3 |
| C6 | empty cluster (该 cluster 在 epoch 内无 events) → 该 cluster key 不出现在输出 dict | TDD test: labels 全是 cluster 0 → 输出 dict 只有 key 0 | 下游 Jaccard 计算 KeyError |

**实现模板**（phantom mask discipline 来自 lagPatRank cross-PR contract）：

```python
def compute_local_rank_endpoint(
    ranks: np.ndarray,      # (n_ch, n_events_total)
    bools: np.ndarray,      # (n_ch, n_events_total)
    labels: np.ndarray,     # (n_events_total,)
    event_indices: np.ndarray,  # epoch event indices into the global arrays
    k: int = 3,
    valid_mask: np.ndarray = None,
) -> Dict[int, Dict[str, List[int]]]:
    n_ch = ranks.shape[0]
    if valid_mask is None:
        valid_mask = np.ones(n_ch, dtype=bool)
    out = {}
    for c in np.unique(labels[event_indices]):
        cluster_evt_idx = event_indices[labels[event_indices] == c]
        if cluster_evt_idx.size == 0:
            continue
        # masked mean rank per channel
        cl_ranks = ranks[:, cluster_evt_idx]          # (n_ch, n_cluster_evt)
        cl_bools = bools[:, cluster_evt_idx]          # (n_ch, n_cluster_evt)
        n_part = cl_bools.sum(axis=1)                 # (n_ch,)
        sum_rank_masked = (cl_ranks * cl_bools).sum(axis=1)
        with np.errstate(invalid="ignore", divide="ignore"):
            mean_rank = np.where(n_part > 0, sum_rank_masked / np.maximum(n_part, 1), np.nan)
        # eligible = valid_mask AND n_part > 0 (zero-participation channels excluded — C2)
        eligible = valid_mask & (n_part > 0)
        elig_idx = np.flatnonzero(eligible)
        if elig_idx.size == 0:
            continue
        elig_mean = mean_rank[elig_idx]
        k_eff = k if elig_idx.size >= 2 * k else max(1, elig_idx.size // 2)
        # low mean rank = early firing = source; high mean rank = late firing = sink
        order = np.argsort(elig_mean, kind="stable")
        top_local = order[:k_eff]
        bot_local = order[-k_eff:]
        out[int(c)] = {
            "source": [int(elig_idx[i]) for i in top_local],
            "sink": [int(elig_idx[i]) for i in bot_local],
        }
    return out
```

### 1.2 `compute_endpoint_spatial_radius(endpoint_indices, coords)` — B2

| # | 不变量 | 测试方法 |
|---|---|---|
| C7 | coords 单位 = mm（assert at boundary，Phase 1 已 lock） | TDD test: assert raises on cm input |
| C8 | k=1 → centroid_rms=0, mean_pairwise=NaN, min_enclosing_radius=0（degenerate cases 明确） | TDD test: 单点输入 |
| C9 | n=3 points forming equilateral triangle (边长 d) → centroid_rms = d/sqrt(3), mean_pairwise = d, min_enclosing_radius = d/sqrt(3) | TDD test: 已知几何 |
| C10 | min_enclosing_ball — 用 Welzl 不切实际 (k≤5)；用 brute force on subsets of 2 or 3 points 即可 | TDD test: 共线 → min_enclosing = half max pairwise distance |

**实现模板**：

```python
def compute_endpoint_spatial_radius(
    endpoint_indices: List[int],
    coords: np.ndarray,  # (n_ch, 3) mm
) -> Dict[str, float]:
    pts = coords[endpoint_indices]
    k = len(pts)
    if k == 0:
        return {"centroid_rms": float("nan"), "mean_pairwise": float("nan"), "min_enclosing_radius": float("nan")}
    if k == 1:
        return {"centroid_rms": 0.0, "mean_pairwise": float("nan"), "min_enclosing_radius": 0.0}
    centroid = pts.mean(axis=0)
    centroid_rms = float(np.sqrt(((pts - centroid) ** 2).sum(axis=1).mean()))
    pdist = np.linalg.norm(pts[:, None, :] - pts[None, :, :], axis=-1)
    iu = np.triu_indices(k, k=1)
    mean_pairwise = float(pdist[iu].mean())
    # Welzl 太重；min_enclosing_ball brute force on small k (≤ 5)
    min_enclosing_radius = _min_enclosing_ball_radius(pts)
    return {
        "centroid_rms": centroid_rms,
        "mean_pairwise": mean_pairwise,
        "min_enclosing_radius": min_enclosing_radius,
        "n_points": int(k),
    }


def _min_enclosing_ball_radius(pts: np.ndarray) -> float:
    """For k ≤ 5: try (a) sphere through 2 antipodal points; (b) circumsphere of all triples;
    return min radius that contains all points (within numeric tolerance)."""
    k = pts.shape[0]
    if k <= 1:
        return 0.0
    candidates = []
    # (a) 2-point spheres
    for i in range(k):
        for j in range(i + 1, k):
            c = 0.5 * (pts[i] + pts[j])
            r = 0.5 * float(np.linalg.norm(pts[i] - pts[j]))
            if _all_within(c, r, pts):
                candidates.append(r)
    # (b) circumsphere of 3 points (in 3D = circle in plane, sphere passes through those 3)
    for i in range(k):
        for j in range(i + 1, k):
            for l in range(j + 1, k):
                try:
                    c, r = _circumsphere_three_points(pts[i], pts[j], pts[l])
                    if _all_within(c, r, pts):
                        candidates.append(r)
                except np.linalg.LinAlgError:
                    continue
    if not candidates:
        # fallback: use farthest-point heuristic
        c = pts.mean(axis=0)
        return float(np.max(np.linalg.norm(pts - c, axis=1)))
    return min(candidates)
```

### 1.3 `compute_source_sink_centroid_distance(source_indices, sink_indices, coords)` — B2

`d_axis = ||centroid(source) - centroid(sink)||`. 单元测试：3 个 source 在原点 / 3 个 sink 在 (10,0,0) → d=10。

### 1.4 `compute_decision_k_drift(per_epoch_ranks_a, per_epoch_ranks_b, per_epoch_valid_a, per_epoch_valid_b, n_perm=500)` — B3

每 epoch 调 `compute_swap_score_sweep` 取 `decision_k` (or `None` if `swap_class != "strict"` per epoch — 与 cohort-level swap_class 用同一定义)。返回 `decision_k_per_epoch + std(decision_k) + cohort-style null`。

| # | 不变量 | 测试 |
|---|---|---|
| C11 | per-epoch rank_a/b 必须来自 **masked mean rank**（与 B1 同模式），不是 raw lagPatRank | TDD test: phantom 通道在 rank vector 中应该被 valid_mask 排除 |
| C12 | epoch 内 cluster events < `min_events_per_cluster=20` → drop epoch（不 carry-forward） | TDD test: 给 cluster A 只有 5 events → 该 epoch 输出 decision_k=None |
| C13 | swap_sweep `n_perm=500` for perf budget (advisor catch 4: full n_perm=1000 太贵) | code review |

**Runner-level gating correction (user-return v2 catch 2026-05-23)**: do **NOT** filter
at runner level by `swap_class in {strict, candidate}`. Compute drift for **all 23 subjects**
(swap_class as context only; summarizer stratifies). For swap-positive subjects drift is
informative; for swap=none drift is the noise/control baseline. Filtering at runner level
over-dilutes the cohort and prevents within-cohort baseline comparison.

实现概要（注意：`cluster_a` / `cluster_b` IDs **必须** 从 `primary_pair` JSON 传入，**不能** 用 `np.unique(labels)`，advisor catch (3) — 显式记录测的是哪一对 cluster）：

```python
def compute_decision_k_drift(
    ranks: np.ndarray,
    bools: np.ndarray,
    labels: np.ndarray,
    epochs: List[Dict],
    cluster_a: int,             # from primary_pair JSON
    cluster_b: int,             # from primary_pair JSON
    min_events_per_cluster: int = 20,
    n_perm: int = 500,
    seed: int = 0,
) -> Dict[str, Any]:
    decision_k_per_epoch = []
    for ei, ep in enumerate(epochs):
        evt_idx = ep["event_indices"]
        idx_a = evt_idx[labels[evt_idx] == cluster_a]
        idx_b = evt_idx[labels[evt_idx] == cluster_b]
        if len(idx_a) < min_events_per_cluster or len(idx_b) < min_events_per_cluster:
            decision_k_per_epoch.append(None)   # don't carry-forward (advisor catch 4)
            continue
        # Use the canonical template estimator (same as B1 calibration-passing helper)
        rank_a, valid_a = _per_cluster_template_rank(ranks, bools, idx_a)
        rank_b, valid_b = _per_cluster_template_rank(ranks, bools, idx_b)
        sweep = compute_swap_score_sweep(rank_a.astype(float), rank_b.astype(float),
                                          valid_a, valid_b,
                                          n_perm=n_perm, seed=seed + ei)
        decision_k_per_epoch.append(sweep.get("decision_k"))
    finite_k = [k for k in decision_k_per_epoch if k is not None]
    return {
        "decision_k_per_epoch": decision_k_per_epoch,
        "n_epochs_with_decision_k": len(finite_k),
        "decision_k_std": float(np.std(finite_k)) if len(finite_k) >= 2 else float("nan"),
        "decision_k_mean": float(np.mean(finite_k)) if finite_k else float("nan"),
        "decision_k_range": [int(min(finite_k)), int(max(finite_k))] if finite_k else None,
    }
```

---

## 2. Runner schema bump (B4)

`scripts/run_sef_itp_phase2.py` JSON schema v1 → v2:

```json
{
  "schema_version": "sef_itp_phase2_v2_2026_05_24",
  "h4": {
    "epoch_hours": 0.5,
    "n_epochs": N,
    "endpoint_k": 3,

    "rank_endpoint": {
      "per_epoch_local": [...],                  // per-cluster source/sink indices
      "per_epoch_jaccard": [...],                // vs PR-6 global per cluster, mean over clusters
      "I_geom_rank": {...}                       // normalized rank-endpoint drift
    },
    "spatial_radius": {
      "per_epoch_source_centroid_rms": [...],
      "per_epoch_sink_centroid_rms": [...],
      "per_epoch_source_mean_pairwise": [...],
      "per_epoch_sink_mean_pairwise": [...],
      "per_epoch_source_min_enclosing": [...],
      "per_epoch_sink_min_enclosing": [...],
      "per_epoch_source_sink_centroid_dist": [...]
    },
    "decision_k_drift": {
      "applicable": bool,                         // true iff swap_class ∈ {strict, candidate}
      "decision_k_per_epoch": [...],              // null for skipped epochs
      "decision_k_std": float,
      "decision_k_mean": float
    },

    // I_rate keep both null variants from v1.0 (no change)
    "I_rate_epoch_order_shuffle": {...},
    "I_rate_circular_shift": {...},

    // v1.0 backward-compat fields, demoted to supplementary
    "supplementary_participation_field": {
      "per_epoch_local": [...],
      "per_epoch_jaccard": [...],
      "I_geom_participation": {...}
    }
  }
}
```

Cohort summarizer additions: same Wilcoxon + Cohen's d on `rank_endpoint.per_epoch_jaccard` instead of participation_field 版本；spatial_radius 各 4 metric source/sink 分开报 Wilcoxon + Cohen's d on within-subject (epoch_std / matched-null_std)；decision_k_drift 子集 (n≈9) 报 std distribution。

---

## 3. 输入数据来源 (CLAUDE.md §6.2 source-of-truth resolution levels)

| 数据 | 来源 | 层级 | B 阶段用法 |
|---|---|---|---|
| `ranks[n_ch, n_events]` per-event lag rank | `loaded["ranks"]` from `load_subject_propagation_events` | event-level (raw) | B1/B3 — phantom-mask discipline 必须用 `bools` 同时 mask |
| `bools[n_ch, n_events]` per-event participation | `loaded["bools"]` | event-level | B1/B3 — phantom mask 来源 |
| `labels[n_events]` cluster id | PR-2 masked `adaptive_cluster.labels` | event-level (post-cluster) | B1/B3/B4 |
| `coords[n_ch, 3]` mm 3D | Phase 1 `load_subject_coords` (`src.seeg_coord_loader`) | channel-level (anatomy) | B2 — assert mm + provenance |
| `global_endpoint` per-cluster source/sink | PR-6 anchoring `per_template[].source/sink` (channel names → indices) | cluster-level (PR-6 anchored) | B1 calibration + per-epoch Jaccard |
| `swap_class / decision_k / rank_a_dense_full / rank_b_dense_full / joint_valid` | rank_displacement `primary_pair` (masked) | per-subject pair-level | B3 — 决定 subject 是否 in decision-k drift subset |

---

## 4. Test plan (TDD strict — write tests first, watch fail, then code)

`tests/test_sef_itp_phase2.py` add new tests (preserve all 39 existing):

```python
# B1
def test_compute_local_rank_endpoint_calibration_vs_pr6():  # C5 — first test, decisive
def test_compute_local_rank_endpoint_phantom_mask():        # C1
def test_compute_local_rank_endpoint_zero_participation_excluded():  # C2
def test_compute_local_rank_endpoint_valid_mask_filter():   # C3
def test_compute_local_rank_endpoint_k_degradation():       # C4
def test_compute_local_rank_endpoint_empty_cluster():       # C6

# B2
def test_compute_endpoint_spatial_radius_single_point():    # C8
def test_compute_endpoint_spatial_radius_equilateral():     # C9
def test_compute_endpoint_spatial_radius_collinear():       # C10
def test_compute_source_sink_centroid_distance():           # 1.3

# B3
def test_compute_decision_k_drift_min_events_drop():        # C12
def test_compute_decision_k_drift_phantom_mask():           # C11
def test_compute_decision_k_drift_perm_budget_500():        # C13 (just signature/arg pass-through)
```

---

## 5. Perf budget (advisor catch 4 — revised)

- B1 + B2 全 cohort: 23 subjects × ~50 epochs × cheap ops ≈ < 5 min
- B3 only 9 subjects × ~50 epochs × `swap_sweep(n_perm=500)` ≈ 9 × 50 × ~3s = 22 min
- B5 cohort rerun total ≈ 30 min, run in background

---

## 6. Backward compatibility / supplementary tier

- v1.0 `compute_local_endpoint(events_bool, labels, k, valid_mask)` 保留不动 — runner 仍调用以填 `supplementary_participation_field`
- v1.0 cohort_run_2026-05-23.md banner 已加 cross-cite
- spec_amendment_2026-05-23.md 关于 H4 I_rate null 仍 pending user — v1.1 同样并跑 epoch_order_shuffle (degenerate) + circular_shift (proposed)，user decision 不变
- framework v1.0.6 §3.4 prose: rank-based endpoint drift = primary; participation-field stability = sensitivity

---

## 7. Stage B 执行顺序

1. **B0** (this doc) — DONE
2. **B1** — TDD compute_local_rank_endpoint (calibration test first — C5)
3. **B2** — TDD compute_endpoint_spatial_radius + source_sink_centroid_distance
4. **B3** — TDD compute_decision_k_drift
5. **B4** — runner update + schema v2
6. **B5** — cohort rerun in background; summarizer; new archive cohort_run_2026-05-24
7. **B6** — framework v1.0.6 single commit (consolidates A3+A5+A6 + Phase 2 完结 marker)

---

## 8. Pre-registration discipline checks (CLAUDE.md §5 + §6 cross-check)

- ✅ H4 仍是 primary cohort claim (framework v1.0.5 §3.4 tier 不变)
- ✅ 新主指标（rank-endpoint Jaccard drift + spatial radius drift）是 prose **clarification**（"endpoint geometry drift" 已在 v1.0.5 §3.4 原文，只是 v1.0 impl 错用了 participation-field 取 endpoint），不是新假设
- ✅ Cohen's d ≥ 0.3 floor 不变
- ✅ decision-k drift 是 SEF-ITP 区分性预测的精细化（"病理空间结构有不有动态进展"），写进 v1.0.6 §3.4 as secondary metric per primary
- ✅ 旧 participation-field 实现保留作 sensitivity，cohort 数字不删
- ✅ I_rate null 决策（circular_shift vs epoch_order_shuffle）user 拍板不变 — B5 同样并跑

---

## 9. 代号映射 (CLAUDE.md §8 朴素话)

- rank-based endpoint = "哪些通道在事件里最先点火"（mean masked rank lowest = source）
- participation field endpoint = "哪些通道这段时间常常出现在事件里"（mean events_bool top）
- decision_k = rank-displacement family-wise null 给出的"该 subject 自适应最稳的 swap-endpoint 大小"
- spatial radius RMS = "endpoint 通道到自己 centroid 的均方距离" — 该侧 endpoint 在脑里多紧
- source-sink centroid distance = "传播轴长度" — source 中心和 sink 中心隔多远
- min enclosing ball = "装下该侧 k 个 endpoint 的最小球半径" — 该侧 endpoint 占多大空间
