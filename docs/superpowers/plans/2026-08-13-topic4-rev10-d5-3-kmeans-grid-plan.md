# Topic 4 rev10-D5.3 execution plan

1. Freeze the 13-candidate library at a clean commit: exact off plus 12 local continuous-OU cells.
2. Verify candidate IDs, input hashes, zero edge coefficients, forbidden-input contract, new network seeds, and the frozen KMeans score in tests.
3. Launch through `systemd-run --user -> nohup`; first run one nonzero sentinel, read peak RSS once, and cap parallel workers from measured memory with a maximum of nine.
4. Wait in 180-second controller intervals. Each worker writes JSON, NPZ, status, timing log, hashes, and provenance; the controller sends a desktop notification on completion or failure.
5. Aggregate returned-only events with network seeds as independent units.
6. Audit every candidate with canonical masked-rank KMeans, hierarchical network/mode balancing, supervised patient correlation matrix, OOD and detector occupancy.
7. Select the minimum frozen continuous score. Do not open `beta`, topology, Gaussian components, contact-conditioned drive, or an optimizer comparison.
8. If the selected candidate improves the D5.2 anchor and retains the patient matrix sign geometry, freeze a separate selection/confirmation manifest before reading those networks. Otherwise stop this dose/persistence branch and report the bounded negative result.
