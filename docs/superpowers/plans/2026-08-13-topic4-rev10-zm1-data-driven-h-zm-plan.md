# Topic 4 rev10-ZM1 execution plan

1. Add the minimal E-cell Z/M slow protocol without changing guarded LIF,
   connectivity, delay, or observation modules.
2. Test current-path parity, E-only action, bounded Z/non-negative M, learned
   threshold passthrough, and trace-sampling invariance.
3. Freeze the D5.2 learned node field, local OU candidate, direction classifier,
   patient target, detector, and six fresh network seeds before execution.
4. Run one full-duration sentinel, measure peak RSS, reserve half of available
   RAM, and launch up to six paired workers with one numerical thread each.
5. Aggregate networks as the statistical unit. Produce a paired slow-off versus
   Z/M audit containing activity burden, TA/TB support, patient matrix, canonical
   KMeans, and slow-state trajectories.
6. Render the established data-driven SNN direct-waveform and Fig.4C KMeans
   figure pair from the frozen arm outputs after aggregation.
7. Report the result as mechanism transfer only. Do not infer seizure onset,
   termination, postictal protection, recovery, or mechanism identifiability.
