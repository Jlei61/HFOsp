# Topic 4 rev10-D5.4 execution plan

1. Freeze exact off, local `0.075/40`, and exact-marginal permuted `0.075/40` before reading seeds `1311-1313`.
2. Run nine workers through measured-RSS `systemd-run -> nohup`, with 180-second controller waits and completion notification.
3. Aggregate returned-only events by network, then compute balanced canonical KMeans and the supervised patient matrix.
4. If the predeclared local rule passes, freeze the same three arms on seeds `1321-1326`; otherwise stop this mechanism branch.
5. Only the later confirmation can generate a replacement Fig.4 direct-waveform and KMeans pair.
