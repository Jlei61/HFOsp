# Engine patch: kick_center kwarg for single-end / off-center kick (Increment 2/3).
# Target: results/topic4_sef_hfo/lif_snn/engine/kick_probe.py  (coworker Zou's engine;
# that tree is GITIGNORED in this repo, so this patch-note is the tracked record.)
# Backward-compatible: kick_center=None -> [L/2, L/2] (sheet center) = byte-identical to before.
# Applied 2026-06-07.
#
# 1) simulate_kick signature:
#      def simulate_kick(p, net, KICK_BOOST, slow=None, nu_signal_fn=None, verbose=False):
#    ->
#      def simulate_kick(p, net, KICK_BOOST, slow=None, nu_signal_fn=None, verbose=False, kick_center=None):
#
# 2) kick-mask center (~line 93):
#      center = np.array([p.L / 2, p.L / 2])
#    ->
#      center = (np.array([p.L / 2, p.L / 2]) if kick_center is None
#                else np.asarray(kick_center, float))   # default = sheet center (back-compat)
#
# 3) fresh_run forwards it (~line 304):
#      def fresh_run(p, net, KICK_BOOST):
#          net["rng"] = np.random.default_rng(p.seed)
#          return simulate_kick(p, net, KICK_BOOST=KICK_BOOST)
#    ->
#      def fresh_run(p, net, KICK_BOOST, kick_center=None):
#          net["rng"] = np.random.default_rng(p.seed)
#          return simulate_kick(p, net, KICK_BOOST=KICK_BOOST, kick_center=kick_center)
#
# NOTE: outside_mask/spk_inside/spk_outside/classify/compute_metrics remain CENTER-disk-relative;
# for an off-center kick they are no longer "seed vs spread" -- the Increment-2/3 read goes straight
# off E_spk_bool + posE (direction estimator), not those center-relative bookkeeping fields.

# --- 2026-06-07 addendum: current-based LFP at custom sites (formal §13.4 LFP) ---
# lfp.py LFPRecorder.__init__: add `sites=None` -> self.sites = make_grid(p) if sites is None
#   else np.asarray(sites, float)   (custom montage at real electrode spacing)
# kick_probe.simulate_kick: add `lfp_recorder=None`; allocate lfp_trace=(nsteps,n_sites) if given;
#   inside loop after I_E/I_I: if lfp_trace is not None: lfp_trace[t]=lfp_recorder.sample(I_E,I_I);
#   return dict adds lfp_trace + lfp_sites. Backward-compatible (None -> identical to before).
# Validated: tiny net, 10 custom 4mm sites -> lfp_trace (1850,10), per-site peaks vary (non-degenerate).

# ---------------------------------------------------------------------------
# Engine patch: r_kick kwarg (kick-disk radius override). Applied 2026-06-07.
# WHY: the default R_KICK=0.15mm is BELOW the excitable medium's critical
# nucleation radius (~0.6-0.8mm, measured in the rate field at ratio=0.6/g=3.6:
# r=0.15 -> extinction, r>=0.8 -> self_limited_propagation). cm-scale (large-N)
# ignition needs a SUPRA-NUCLEUS kick; finite-size noise only masks this at small N.
# Backward-compatible: r_kick=None -> R_KICK (byte-identical to before).
#
# 1) simulate_kick signature: ...verbose=False, kick_center=None, lfp_recorder=None):
#    -> ...verbose=False, kick_center=None, lfp_recorder=None, r_kick=None):
#
# 2) kick-mask radius (~line 97):
#      kick_mask = is_E & (dist_c <= R_KICK)
#    ->
#      rk = R_KICK if r_kick is None else float(r_kick)   # patched: kick radius override
#      kick_mask = is_E & (dist_c <= rk)
