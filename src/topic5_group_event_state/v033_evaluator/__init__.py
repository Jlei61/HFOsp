"""Group-Event State v0.3.3 -- Workstream A: canonical evaluator, assays, data contract.

One pure per-anchor proper score (``canonical``) that the training branch, the
independent evaluator and the figure payload all call; a row-level audit of the
v0.3.2 E1146 discrepancy; synthetic oracle / power assays on real time axes;
estimability from real coverage segments; the frozen data-boundary contract.

Nothing in this package trains a human model or reads the sealed partition.
"""

PACKAGE_VERSION = "0.3.3-evaluator-assay"
