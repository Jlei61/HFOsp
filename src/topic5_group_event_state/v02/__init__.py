"""Group-Event State v0.2 — Agent A (H1/H2a predictive-state identification).

The v0.1 modules one directory up stay untouched: they are still the frozen
data semantics, and an old queue is running against a copy of them.  Everything
v0.2 changes about *time* -- the split coordinate, the carry boundaries, the
anchor grid and the future-block targets -- lives here.
"""
