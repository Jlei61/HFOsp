"""Group-Event State v0.3.3 -- Workstream B: Persistent Training Laboratory.

Target-agnostic training service (T0-T6) for ``S_N`` / ``S_G`` / R0 / R1 and
explicitly approved exploratory models.  It decides *how* a model is trained
fairly, never *what* is trained: scientific target, split, explicit history,
endpoint reduction and H2b label discipline are owned by Agents A/C.
Training adequacy is never an H1/H2 result.
"""

PACKAGE_VERSION = "0.3.3-training-lab"
