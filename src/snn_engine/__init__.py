"""Spatially-structured E–I LIF wave engine (SEF-HFO Topic 4).

Provenance: scaffolded by coworker Zou; previously lived gitignored under
results/topic4_sef_hfo/lif_snn/engine/ and pinned via a sha256 checksum guard
(src/sef_hfo_snn_engine_guard.py). Moved into git tracking 2026-06-15 — git
history now provides the integrity/recoverability the checksum guard approximated.

Consumers import the modules by name after putting this dir on sys.path
(`sys.path.insert(0, "src/snn_engine")`), e.g. `from connectivity_rot import ...`.
"""
